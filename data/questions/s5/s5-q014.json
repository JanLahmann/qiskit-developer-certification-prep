{
  "id": "s5-q014",
  "section": "s5",
  "objectives": [
    "s5o1",
    "s5o2"
  ],
  "type": "mcq",
  "difficulty": 2,
  "stem": "Hoping to turn on error mitigation for measurement counts, a developer writes `sampler.options.resilience_level = 1` on a `SamplerV2`. What actually happens?",
  "code": "from qiskit_ibm_runtime import SamplerV2\n\nsampler = SamplerV2(mode=backend)\nsampler.options.resilience_level = 1",
  "options": [
    {
      "key": "A",
      "text": "It raises an error — `SamplerV2` has no `resilience_level`; that option belongs to `EstimatorV2`"
    },
    {
      "key": "B",
      "text": "It enables T-REx readout error mitigation on the sampled counts"
    },
    {
      "key": "C",
      "text": "It is accepted but silently ignored, because the sampler has no mitigation"
    },
    {
      "key": "D",
      "text": "It is an alias that turns on measurement twirling"
    }
  ],
  "answer": [
    "A"
  ],
  "explanation": {
    "correct": "`resilience_level` is an EstimatorV2 option — resilience levels drive expectation-value mitigation such as TREX/ZNE. SamplerV2 exposes no `resilience_level`; assigning it raises a validation error ('no such attribute'). The Sampler's error-handling options are `dynamical_decoupling`, `twirling`, and execution/simulator settings — it returns raw samples and leaves mitigation to you.",
    "distractors": {
      "B": "Readout mitigation on expectation values (TREX) is reached through Estimator resilience settings, not through a Sampler option — the Sampler has none.",
      "C": "It is not silently ignored: the options object validates attributes and raises on an unknown one, so the line throws immediately.",
      "D": "There is no aliasing. Measurement twirling is set explicitly via `options.twirling.enable_measure`; `resilience_level` simply does not exist on the Sampler."
    },
    "citations": [
      "https://quantum.cloud.ibm.com/docs/guides/runtime-options-overview",
      "https://quantum.cloud.ibm.com/docs/api/qiskit-ibm-runtime"
    ]
  },
  "proof": {
    "status": "executed",
    "artifact": "proofs/s5-q014.json"
  },
  "verification": {
    "mode": "script",
    "proof_script": "\nfrom qiskit_ibm_runtime import SamplerV2\nfrom qiskit_ibm_runtime.fake_provider import FakeManilaV2\n\nbackend = FakeManilaV2()\nsampler = SamplerV2(mode=backend)\n\nraised = False\nerr = \"\"\ntry:\n    sampler.options.resilience_level = 1\nexcept Exception as e:\n    raised = True\n    err = f\"{type(e).__name__}: {str(e)[:140]}\"\n\nhas_attr = hasattr(sampler.options, \"resilience_level\")\n\nevidence, proven = {}, []\n# A: it raises; SamplerV2 has no resilience_level (that's an Estimator option)\nA = raised and (not has_attr)\n# B: it enables readout mitigation\nB = (not raised) and has_attr\n# C: silently does nothing\nC = (not raised)\n# D: it sets measurement twirling\nD = (not raised)\nevidence[\"A\"] = f\"setting resilience_level raised={raised} ({err}); hasattr={has_attr}\"\nevidence[\"B\"] = f\"no silent success: raised={raised}\"\nevidence[\"C\"] = f\"no silent success: raised={raised}\"\nevidence[\"D\"] = f\"no silent success: raised={raised}\"\nfor k, val in [(\"A\", A), (\"B\", B), (\"C\", C), (\"D\", D)]:\n    if val:\n        proven.append(k)\n\nemit_verdict({\"answer\": proven, \"evidence\": evidence,\n              \"observed\": {\"raised\": raised, \"error\": err, \"has_resilience_level\": has_attr}})\n"
  },
  "freshness": {
    "verified_against": {
      "qiskit": "2.5.0",
      "qiskit-aer": "0.17.2",
      "qiskit-ibm-runtime": "0.48.0",
      "numpy": "2.5.1"
    },
    "verified_on": "2026-07-20",
    "stale": false
  },
  "provenance": {
    "generated": "2026-07-19",
    "model": "claude-opus (generation agent, section s5)",
    "adversarial_rounds": 0,
    "reviewed": false
  }
}
