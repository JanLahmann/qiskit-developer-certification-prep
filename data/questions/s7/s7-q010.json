{
  "id": "s7-q010",
  "section": "s7",
  "objectives": [
    "s7o1"
  ],
  "type": "mcq",
  "difficulty": 2,
  "stem": "Your kernel is about to shut down and you want to keep a completed SamplerV2 `result` on disk so a later session can reload it as a real result object. Which save/load pair round-trips it?",
  "code": "import json\nfrom qiskit_ibm_runtime import RuntimeEncoder, RuntimeDecoder\n\n# result = sampler.run([isa_circuit]).result()\n# ... write result to result.json, read it back in a new kernel ...",
  "options": [
    {
      "key": "A",
      "text": "`json.dump(result, f, cls=RuntimeEncoder)`, then `json.load(f, cls=RuntimeDecoder)`"
    },
    {
      "key": "B",
      "text": "`json.dump(result, f)`, then `json.load(f)` -- a PrimitiveResult is plain JSON data"
    },
    {
      "key": "C",
      "text": "`json.dump(result, f, cls=RuntimeDecoder)`, then `json.load(f, cls=RuntimeEncoder)`"
    },
    {
      "key": "D",
      "text": "`result.to_json()` to a string, then `PrimitiveResult.from_json(...)` to rebuild the object"
    },
    {
      "key": "E",
      "text": "`json.dump(result, f, cls=RuntimeEncoder)`, then plain `json.load(f)` -- decoding is automatic"
    },
    {
      "key": "F",
      "text": "`json.dumps(result, f, cls=RuntimeEncoder)`, then `json.loads(f, cls=RuntimeDecoder)`"
    }
  ],
  "answer": [
    "A"
  ],
  "display_count": 4,
  "explanation": {
    "correct": "A V2 result is not plain JSON: `PrimitiveResult`, `DataBin` and `BitArray` are custom containers. `RuntimeEncoder` is a `json.JSONEncoder` subclass (so it goes with `dump`) and `RuntimeDecoder` is a `json.JSONDecoder` subclass (so it goes with `load`). The reloaded object is a real PrimitiveResult again -- `result[0].data.meas.get_counts()` still works, with identical counts.",
    "distractors": {
      "B": "Plain `json.dump` raises `TypeError: Object of type PrimitiveResult is not JSON serializable`; the V2 containers need the Runtime encoder.",
      "C": "Encoder and decoder are swapped. `json.dump(..., cls=RuntimeDecoder)` fails because `json` hands encoder keyword arguments to the class (`TypeError: JSONDecoder.__init__() got an unexpected keyword argument 'skipkeys'`).",
      "D": "`to_json`/`from_json` do not exist on `PrimitiveResult` (`AttributeError`); Runtime serialization goes through the encoder/decoder classes with the standard `json` module.",
      "E": "Only half the round-trip is custom. `RuntimeEncoder` writes the tagged JSON, but a plain `json.load` returns nested dicts, not the containers: the proof reloads and immediately fails with `KeyError: 0` because a dict has no pub index. `RuntimeDecoder` is what rebuilds `PrimitiveResult`.",
      "F": "`dumps`/`loads` are the STRING forms: they take no file handle. `json.dumps(result, f, ...)` raises `TypeError: dumps() takes 1 positional argument but 2 positional arguments ... were given`. Use `dump`/`load` with a file object, or feed `dumps`' return string to `loads`."
    },
    "citations": [
      "https://quantum.cloud.ibm.com/docs/guides/save-jobs",
      "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.primitives.PrimitiveResult"
    ]
  },
  "proof": {
    "status": "executed",
    "artifact": "proofs/s7-q010.json"
  },
  "verification": {
    "mode": "script",
    "proof_script": "import json\nfrom qiskit import QuantumCircuit\nfrom qiskit.primitives import StatevectorSampler\nfrom qiskit_ibm_runtime import RuntimeEncoder, RuntimeDecoder\n\nqc = QuantumCircuit(2)\nqc.h(0); qc.cx(0, 1); qc.measure_all()\nresult = StatevectorSampler(seed=42).run([qc], shots=400).result()\norig = result[0].data.meas.get_counts()\n\ndef roundtrip(dump_kwargs, load_kwargs, path):\n    with open(path, \"w\") as fh:\n        json.dump(result, fh, **dump_kwargs)\n    with open(path) as fh:\n        return json.load(fh, **load_kwargs)\n\nevidence = {}; proven = []\ndef check(key, fn):\n    try:\n        back = fn()\n        got = back[0].data.meas.get_counts()\n        ok = (got == orig)\n        evidence[key] = (f\"round-trip returned {type(back).__name__}; reloaded counts {got} \"\n                         f\"== original {orig} -> {ok}\")\n        if ok: proven.append(key)\n    except Exception as e:\n        evidence[key] = f\"raised {type(e).__name__}: {str(e)[:110]}\"\n\ncheck(\"A\", lambda: roundtrip({\"cls\": RuntimeEncoder}, {\"cls\": RuntimeDecoder}, \"a.json\"))\ncheck(\"B\", lambda: roundtrip({}, {}, \"b.json\"))\ncheck(\"C\", lambda: roundtrip({\"cls\": RuntimeDecoder}, {\"cls\": RuntimeEncoder}, \"c.json\"))\ndef _d():\n    s = result.to_json()\n    from qiskit.primitives import PrimitiveResult\n    return PrimitiveResult.from_json(s)\ncheck(\"D\", _d)\ncheck(\"E\", lambda: roundtrip({\"cls\": RuntimeEncoder}, {}, \"e.json\"))\ndef _f():\n    with open(\"f.json\", \"w\") as fh:\n        json.dumps(result, fh, cls=RuntimeEncoder)\n    with open(\"f.json\") as fh:\n        return json.loads(fh, cls=RuntimeDecoder)\ncheck(\"F\", _f)\nemit_verdict({\"answer\": proven, \"evidence\": evidence,\n              \"observed\": {\"original_counts\": orig}})\n"
  },
  "provenance": {
    "generated": "2026-07-19",
    "model": "claude-opus (generation agent, section s7)",
    "adversarial_rounds": 1,
    "reviewed": true,
    "notes": "adversarial s7: REWRITTEN — old version duplicated q024 (pub indexing) and echoed a community item; now tests RuntimeEncoder/RuntimeDecoder round-trip (executed proof, kills the 1.42x length tell) | pool wave: +E encoder-only round-trip (plain json.load returns dicts); +F dumps/loads take a file handle"
  },
  "freshness": {
    "verified_against": {
      "qiskit": "2.5.0",
      "qiskit-aer": "0.17.2",
      "qiskit-ibm-runtime": "0.48.0",
      "numpy": "2.5.1"
    },
    "verified_on": "2026-07-25",
    "stale": false
  }
}
