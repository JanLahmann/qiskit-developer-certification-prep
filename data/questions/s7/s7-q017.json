{
  "id": "s7-q017",
  "section": "s7",
  "objectives": [
    "s7o1"
  ],
  "type": "predict-output",
  "difficulty": 3,
  "stem": "A 3-qubit circuit applies `X` to qubit 0 (qubits 1 and 2 stay |0>), then `measure_all()`. From the resulting `bits` you call `bits.slice_bits([2])`. What does the sliced array's `get_counts()` return?",
  "code": "from qiskit import QuantumCircuit\nfrom qiskit.primitives import StatevectorSampler\n\nqc = QuantumCircuit(3)\nqc.x(0)\nqc.measure_all()\n\nbits = StatevectorSampler().run([qc], shots=300).result()[0].data.meas\nprint(bits.slice_bits([2]).get_counts())",
  "options": [
    {
      "key": "A",
      "text": "`{'1': 300}`"
    },
    {
      "key": "B",
      "text": "`{'001': 300}`"
    },
    {
      "key": "C",
      "text": "`{'100': 300}`"
    },
    {
      "key": "D",
      "text": "`{'0': 300}`"
    },
    {
      "key": "E",
      "text": "`{'00': 300}` -- `[2]` selects the bits up to index 2"
    },
    {
      "key": "F",
      "text": "It raises `IndexError`: bit index 2 is out of range"
    }
  ],
  "answer": [
    "D"
  ],
  "display_count": 4,
  "explanation": {
    "correct": "`slice_bits([2])` keeps only bit index 2, which is qubit 2 (measured 0), and returns a 1-bit BitArray. Bit indices match qubit indices (little-endian), so index 2 is NOT the flipped qubit 0.",
    "distractors": {
      "A": "This assumes index 2 refers to the flipped qubit; bit index 2 is qubit 2, which is 0.",
      "B": "`slice_bits` reduces the width to the selected bit(s); it does not keep all three bits.",
      "C": "This both keeps full width and mis-reads endianness.",
      "E": "The argument is a LIST of bit indices, not an upper bound: `[2]` keeps exactly one bit. The proof prints the single-character keys the call actually produces.",
      "F": "Bit indices run `0..num_bits-1`, so index 2 is the third (and last) bit of a 3-bit register. The proof calls it and observes counts, not an exception."
    },
    "citations": [
      "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.primitives.BitArray",
      "https://quantum.cloud.ibm.com/docs/guides/primitive-input-output"
    ]
  },
  "proof": {
    "status": "executed",
    "artifact": "proofs/s7-q017.json"
  },
  "verification": {
    "mode": "script",
    "proof_script": "from qiskit import QuantumCircuit\nfrom qiskit.primitives import StatevectorSampler\n\nqc = QuantumCircuit(3); qc.x(0); qc.measure_all()\nbits = StatevectorSampler(seed=9).run([qc], shots=300).result()[0].data.meas\nobs = bits.slice_bits([2]).get_counts()\ncand = {\"A\": {\"1\": 300}, \"B\": {\"001\": 300}, \"C\": {\"100\": 300}, \"D\": {\"0\": 300},\n        \"E\": {\"00\": 300}}\nevidence = {}; proven = []\nfor k, v in cand.items():\n    m = (obs == v)\n    evidence[k] = f\"slice_bits([2]) counts={obs}; option claims {v} -> match={m}\"\n    if m: proven.append(k)\ntry:\n    bits.slice_bits([2])\n    F_ok = False\n    evidence[\"F\"] = (f\"slice_bits([2]) returned counts {obs} without raising; index 2 is a valid \"\n                     f\"bit index of a 3-bit register -> {F_ok}\")\nexcept IndexError as e:\n    F_ok = True\n    evidence[\"F\"] = f\"slice_bits([2]) raised IndexError: {str(e)[:60]} -> {F_ok}\"\nif F_ok: proven.append(\"F\")\nemit_verdict({\"answer\": proven, \"evidence\": evidence,\n              \"observed\": {\"full_counts\": bits.get_counts(), \"sliced_counts\": obs}})\n"
  },
  "provenance": {
    "generated": "2026-07-19",
    "model": "claude-opus (generation agent, section s7)",
    "adversarial_rounds": 1,
    "reviewed": true,
    "notes": "adversarial s7 round 1: survived unchanged (citations re-fetched 200, proof evidence discriminates, no second defensible answer, no meta-pattern flag) | pool wave: +E slice_bits([2]) reads as an upper bound; +F bit index 2 is out of range"
  },
  "freshness": {
    "verified_against": {
      "qiskit": "2.5.0",
      "qiskit-aer": "0.17.2",
      "qiskit-ibm-runtime": "0.48.0",
      "numpy": "2.5.1"
    },
    "verified_on": "2026-07-26",
    "stale": false
  }
}
