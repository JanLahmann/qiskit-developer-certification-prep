{
  "id": "s7-q027",
  "section": "s7",
  "objectives": [
    "s7o1",
    "s7o2"
  ],
  "type": "multi",
  "difficulty": 2,
  "stem": "You want to review results from jobs you ran last week but did not record their IDs. Which approaches let you locate and retrieve those past jobs? (Select all that apply.)",
  "code": null,
  "options": [
    {
      "key": "A",
      "text": "Call `service.jobs(...)` with filters such as `backend_name` or `created_after` to list matching jobs"
    },
    {
      "key": "B",
      "text": "Iterate the returned jobs and call `.result()` on the one you want"
    },
    {
      "key": "C",
      "text": "Look up the job and its ID on the Workloads page of IBM Quantum Platform"
    },
    {
      "key": "D",
      "text": "Call `service.job()` without a job ID -- it then returns your whole job history"
    },
    {
      "key": "E",
      "text": "Results live in local RAM, so a kernel restart discards them for good"
    }
  ],
  "answer": [
    "A",
    "B",
    "C"
  ],
  "explanation": {
    "correct": "`service.jobs()` returns your recent jobs and accepts filters (`backend_name`, `created_after`, `session_id`, `pending`, ...); you then call `.result()` on the job you want. The IBM Quantum Platform Workloads page lists your submitted jobs together with their IDs (the save-jobs guide points there for exactly this purpose). Results are stored server-side, so a kernel restart does not lose them.",
    "distractors": {
      "D": "`service.job()` is the singular lookup and requires a job ID (`TypeError` without one); listing many jobs is what the plural `service.jobs()` does, and it pages with `limit` (default 10) rather than dumping everything.",
      "E": "IBM Quantum stores the results of every job server-side; that is the whole point of `service.job(job_id)` and of the save-jobs guide."
    },
    "citations": [
      "https://quantum.cloud.ibm.com/docs/guides/save-jobs",
      "https://quantum.cloud.ibm.com/docs/guides/monitor-job"
    ]
  },
  "proof": {
    "status": "conceptual",
    "artifact": null
  },
  "verification": {
    "mode": "none"
  },
  "provenance": {
    "generated": "2026-07-19",
    "model": "claude-opus (generation agent, section s7)",
    "adversarial_rounds": 1,
    "reviewed": true,
    "notes": "adversarial s7: D/E rephrased without absolute wording (kills absolute-distractor tell); Workloads-page option C confirmed against the live save-jobs guide"
  }
}
