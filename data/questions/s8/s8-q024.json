{
  "id": "s8-q024",
  "section": "s8",
  "objectives": [
    "s8o4"
  ],
  "type": "mcq",
  "difficulty": 1,
  "stem": "You are driving the Qiskit Runtime REST API from your own HTTP client. How do you submit a Sampler primitive job?",
  "code": null,
  "options": [
    {
      "key": "A",
      "text": "Send an HTTP `POST` to `/api/v1/jobs` with `\"program_id\": \"sampler\"` in the JSON request body."
    },
    {
      "key": "B",
      "text": "Send an HTTP `GET` to `/api/v1/jobs?primitive=SamplerV2` and read the new job from the response."
    },
    {
      "key": "C",
      "text": "Send an HTTP `POST` to a dedicated `/api/v1/sampler` endpoint, with the PUBs in the body."
    },
    {
      "key": "D",
      "text": "Send an HTTP `POST` to `/api/v1/jobs` with `program_id` set to the class name `\"SamplerV2\"`."
    }
  ],
  "answer": [
    "A"
  ],
  "explanation": {
    "correct": "Every primitive job is created by posting to the single `/api/v1/jobs` endpoint. The JSON body selects the primitive through `program_id`, whose documented values are the lower-case service names `sampler` and `estimator`; alongside it go `backend` and a `params` object holding the PUBs, with each circuit supplied as an OpenQASM 3 string. The response returns the job `id`, which you then poll for status and results.",
    "distractors": {
      "B": "Creating a job changes server state, so it is a `POST`. `GET /api/v1/jobs` is the *listing* endpoint, and the primitive is never chosen through a query parameter.",
      "C": "There is no per-primitive endpoint. One job endpoint serves both primitives, distinguished only by the `program_id` field.",
      "D": "`SamplerV2` is the Python class name in `qiskit-ibm-runtime`. Over REST the field takes the service-side program name, which is the lower-case `sampler`."
    },
    "citations": [
      "https://quantum.cloud.ibm.com/docs/guides/sampler-rest-api",
      "https://quantum.cloud.ibm.com/docs/api/qiskit-runtime-rest/tags/jobs"
    ]
  },
  "proof": {
    "status": "conceptual",
    "artifact": null
  },
  "verification": {
    "mode": "none"
  },
  "provenance": {
    "generated": "2026-07-19",
    "model": "claude-opus (generation agent, section s8)",
    "adversarial_rounds": 1,
    "reviewed": true,
    "notes": "adversarial s8 r1: carried-forward flag CONFIRMED and fixed — the cited guides/primitives-rest-api returns 200 but redirects to guides/primitives ('Introduction to primitives'), which says nothing about REST and does not support the keyed answer. Replaced with guides/sampler-rest-api, which shows the exact POST to https://quantum.cloud.ibm.com/api/v1/jobs with 'program_id': 'sampler' and params.pubs, and with the REST reference's Jobs tag (POST /v1/jobs, program_id body field). Length tell removed. | length rank rebalanced (correct option no longer shortest)"
  }
}
