{
  "section": "s3",
  "version": 2,
  "primers": [
    {
      "objective": "s3o1",
      "title": "Only the classical bits you asked for",
      "body_md": "A circuit has exactly the classical bits you declared, and `measure` needs one to write into.\n\n```python\nqc = QuantumCircuit(2, 2)   # 2 qubits AND 2 clbits, registers auto-named q and c\nqc.measure(0, 1)            # qubit first, clbit second: qubit 0's result lands in c[1]\n```\n\nBuilding from named `QuantumRegister(3, 'data')` and `ClassicalRegister(2, 'c')` objects is purely organizational — it lets you write `data[0]` — and buys nothing at execution or transpile time. Auto-naming is why `add_register(QuantumRegister(2, 'q'))` on a `QuantumCircuit(2, 2)` fails on the duplicate name.\n\n| Method | Measures | Writes into |\n| --- | --- | --- |\n| `measure(qubit, cbit)` | the qubits you name (ints, `Bit`s, registers or parallel lists) | the clbits you name — the mapping is yours, and the keyword is `cbit`, not `clbit` |\n| `measure_all()` | every qubit | a FRESH `ClassicalRegister` named `meas`, sized to the qubit count |\n| `measure_active()` | only qubits carrying operations | the same fresh `meas` register |\n\nResults are little-endian: bitstrings print as c_(n−1) … c_0, with classical bit 0 on the RIGHT.",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/guides/construct-circuits",
        "https://quantum.cloud.ibm.com/docs/guides/measure-qubits",
        "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.circuit.QuantumCircuit"
      ]
    },
    {
      "objective": "s3o1",
      "title": "compose returns, append mutates, tensor widens",
      "body_md": "`compose` is functional by DEFAULT: it returns a new circuit and leaves the caller untouched, so `base.compose(add_on)` with the result discarded is a silent no-op.\n\n```python\nbig = base.compose(sub, qubits=[0, 2])   # keep the return value...\nbase.compose(sub, inplace=True)          # ...or mutate in place\n```\n\n| Call | Mutates the caller? | Returns | Notes |\n| --- | --- | --- | --- |\n| `a.compose(b)` | only with `inplace=True` | a new circuit | `b` may be narrower; `qubits=` maps its qubits onto `a` positionally; `front=True` prepends instead of appending |\n| `a.append(instr, qargs)` | yes | an `InstructionSet` | — |\n| `a.tensor(b)` | no | a widened circuit | the CALLER `a` takes the HIGHER-index qubits, so an X in `a` against an identity in `b` prepares `\\|10⟩` |",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/guides/construct-circuits",
        "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.circuit.QuantumCircuit"
      ]
    },
    {
      "objective": "s3o2",
      "title": "Dynamic circuits: four context managers",
      "body_md": "A dynamic circuit is one whose later quantum operations depend, in real time, on a mid-circuit measurement result — a teleportation correction, syndrome handling. Sweeping an angle over many jobs or post-selecting shots afterwards is classical work outside the QPU, so neither needs one.\n\nQiskit supports exactly four such constructs, each a context manager on the circuit, and every condition is a `(classical_resource, value)` TUPLE.\n\n```python\nwith qc.if_test((cr[0], 1)):   # one tuple, not two arguments\n    qc.x(1)\n```\n\n| Construct | Opened with | Notes |\n| --- | --- | --- |\n| `if_test` | `with qc.if_test((cr[0], 1)):` | the resource may be a single `Clbit` OR a whole `ClassicalRegister` |\n| else branch | `with qc.if_test((cr[0], 0)) as else_:` then `with else_:` | the else branch is the manager's return value |\n| `for_loop` | `with qc.for_loop(range(3)):` | `as i` is optional; the body may hold any number of gates and runs three times |\n| `switch` | `with qc.switch(cr[0]) as case:` then `with case(0):` | add a fallback `with case(case.DEFAULT):`, which may be declared last |\n| `while_loop` | a context manager like the others | the fourth supported construct |\n\nFor anything richer than equality with a literal, build the condition from `qiskit.circuit.classical.expr`.",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/guides/classical-feedforward-and-control-flow",
        "https://quantum.cloud.ibm.com/docs/api/qiskit/circuit_classical",
        "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.circuit.QuantumCircuit"
      ]
    },
    {
      "objective": "s3o2",
      "title": "The old conditional API is removed, not deprecated",
      "body_md": "Everything the pre-2.0 conditional API offered is gone, so there is no older spelling to fall back on.\n\n| What you might write | What Qiskit 2.x does |\n| --- | --- |\n| `instr.c_if(cr[0], 1)` | `AttributeError` — `InstructionSet.c_if` no longer exists |\n| a `condition=` gate keyword | there is no such keyword |\n| assigning to `.condition` | there is no such attribute |\n| `qc.if_test(cr[0] == 1)` | `TypeError: 'bool' object is not subscriptable`, because `Clbit.__eq__` returns an ordinary Python bool |\n| `if cr[0] == 1:` in plain Python | evaluates to False at BUILD time: no gate is added and nothing warns you |\n\nThe last row is the dangerous one, because it is not an error at all: the circuit you get back is simply missing the gate.",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/guides/classical-feedforward-and-control-flow",
        "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.circuit.QuantumCircuit"
      ]
    },
    {
      "objective": "s3o3",
      "title": "Parameters sort alphabetically; binding is functional",
      "body_md": "`qc.parameters` is a `ParameterView` sorted ALPHABETICALLY by name, not by insertion order: add `gamma`, then `alpha`, then `beta`, and you get `['alpha', 'beta', 'gamma']`. `ParameterVector` elements are the one refinement — they sort by vector name and then by numeric index, so `x[10]` follows `x[9]`.\n\n```python\nt = Parameter('theta')\nqc.rx(2 * t, 0)                            # a ParameterExpression, evaluated at bind time\nbound = qc.assign_parameters({t: np.pi})   # qc keeps theta; bound has none\n```\n\n| Binding form | Behaviour |\n| --- | --- |\n| `assign_parameters([0.5, 1.5])` | binds POSITIONALLY against that sorted view; needs one value per parameter, and a short list raises `ValueError` |\n| `assign_parameters({th: 1.0})` | binds only the parameters it names, so partial binding is legal |\n| dict keys | `Parameter` objects or their name strings, but matched by IDENTITY: a different `Parameter('th')` object built elsewhere raises `CircuitError` |\n| `{x: [0.1, 0.2, 0.3]}` | binds a whole `ParameterVector` in one entry |\n| `inplace=True` | mutates the circuit instead of returning a new one |\n| `bind_parameters(...)` | removed in Qiskit 2.x — `AttributeError`, not a deprecation warning |\n\nThat sorted view is what makes a list treacherous and a dict safe.",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/guides/construct-circuits",
        "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.circuit.Parameter",
        "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.circuit.ParameterVector"
      ]
    },
    {
      "objective": "s3o4",
      "title": "ISA circuits and the six preset stages",
      "body_md": "Hardware runs ISA circuits only: every instruction must be in the backend's `Target` AND every two-qubit gate must sit on a pair connected in its `CouplingMap`. Both conditions, every time.\n\n```python\npm = generate_preset_pass_manager(optimization_level=2, backend=backend)\nisa = pm.run(qc)          # a single circuit in, a single circuit out\n```\n\n| Stage | What it does |\n| --- | --- |\n| `init` | unroll to one- and two-qubit gates |\n| `layout` | choose which physical qubits the virtual ones start on |\n| `routing` | insert SWAPs so every two-qubit gate lands on a connected pair |\n| `translation` | rewrite every remaining gate into the basis |\n| `optimization` | simplify |\n| `scheduling` | timing; the slot exists even with no scheduling method and simply runs empty |\n\n`pm.stages` reports exactly those six names in that order, and ROUTING — not layout — is what inserts SWAPs. Hand `pm.run` a LIST and you get a list back, not a circuit. A circuit wider than the device has no valid layout at all and fails in the layout stage.",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/guides/transpile",
        "https://quantum.cloud.ibm.com/docs/guides/transpiler-stages",
        "https://quantum.cloud.ibm.com/docs/guides/transpile-with-pass-managers"
      ]
    },
    {
      "objective": "s3o4",
      "title": "Four optimization levels, one target",
      "body_md": "Levels run 0 through 3; anything else, including 4, raises `ValueError`. All four aim at the SAME target constraints, so they differ in effort, not in whether the output is a valid ISA circuit.\n\n| Level | Layout | Optimization added |\n| --- | --- | --- |\n| 0 | `TrivialLayout` | none at all |\n| 1 | trivial or VF2 layout search | light: one-qubit optimization plus `InverseCancellation` |\n| 2 | no trivial layout | `CommutativeCancellation` |\n| 3 | as level 2 | KAK resynthesis of two-qubit blocks, plus unitarity-breaking passes on measurements |\n\nHigher is not automatically better: a higher level costs compile time and is not guaranteed to give fewer two-qubit gates. The docs' own example shows identical two-qubit counts at levels 1 and 2, with only level 3 improving on them. The guide pages still call `optimization_level` a required positional argument, while the 2.5 signature carries a default.",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/guides/set-optimization",
        "https://quantum.cloud.ibm.com/docs/guides/transpile-with-pass-managers"
      ]
    }
  ],
  "facts": [
    {
      "objective": "s3o1",
      "group": "core",
      "fact_md": "Counts are little-endian — flipping only qubit 0 of a 3-qubit register and calling `measure_all()` yields `001` on every shot.",
      "source": {
        "type": "proof",
        "ref": "s3-q010"
      }
    },
    {
      "objective": "s3o1",
      "group": "core",
      "fact_md": "`qc.compose(sub, qubits=[0, 2])` maps `sub`'s qubit 0 → 0 and qubit 1 → 2, so `sub.cx(0, 1)` becomes a CX with control 0 and target 2.",
      "source": {
        "type": "proof",
        "ref": "s3-q012"
      }
    },
    {
      "objective": "s3o1",
      "group": "core",
      "fact_md": "`width()` is qubits + clbits (3 + 2 = 5), `size()` counts all operations including measures (5), and `depth()` is the critical path (4) — three different numbers.",
      "source": {
        "type": "proof",
        "ref": "s3-q015"
      }
    },
    {
      "objective": "s3o1",
      "group": "trap",
      "fact_md": "`QuantumCircuit(2)` has 2 qubits and ZERO classical bits, so `measure([0,1],[0,1])` raises `CircuitError`; naming the arguments never rescues it.",
      "source": {
        "type": "proof",
        "ref": "s3-q047"
      }
    },
    {
      "objective": "s3o1",
      "group": "trap",
      "fact_md": "`measure_all()` appends its fresh `meas` register even when clbits already exist — two registers, space-separated counts keys — and the barrier it inserts goes BEFORE the measurements.",
      "source": {
        "type": "proof",
        "ref": "s3-q011"
      }
    },
    {
      "objective": "s3o2",
      "group": "core",
      "fact_md": "Each control-flow construct is stored as a SINGLE composite instruction, so `count_ops()` on a conditional circuit shows `if_else: 1` (or `for_loop`, `switch_case`), never the gates inside the body.",
      "source": {
        "type": "proof",
        "ref": "s3-q029"
      }
    },
    {
      "objective": "s3o2",
      "group": "core",
      "fact_md": "`expr.equal(cr, 3)` is a valid `if_test` condition: it compares the whole register as an integer and lifts the bare Python `3` for you, with no `expr.lift` required.",
      "source": {
        "type": "proof",
        "ref": "s3-q053"
      }
    },
    {
      "objective": "s3o2",
      "group": "core",
      "fact_md": "The drawer renders a `for_loop` as ONE boxed region labelled with the index set verbatim (`For-0 range(0, 3)`), spanning every wire the body touches; a statement after the block is drawn outside the box.",
      "source": {
        "type": "proof",
        "ref": "s3-q058"
      }
    },
    {
      "objective": "s3o2",
      "group": "trap",
      "fact_md": "The condition is ONE tuple: `if_test((cr[0], 1))` is right, `if_test(cr[0], 1)` — two positional arguments — is the classic wrong spelling.",
      "source": {
        "type": "proof",
        "ref": "s3-q024"
      }
    },
    {
      "objective": "s3o2",
      "group": "trap",
      "fact_md": "A `switch` value with no matching `case` falls through to DEFAULT: `c0 = 1` against branches `case(0)` and `case(case.DEFAULT)` runs the DEFAULT body.",
      "source": {
        "type": "proof",
        "ref": "s3-q052"
      }
    },
    {
      "objective": "s3o3",
      "group": "core",
      "fact_md": "`ParameterVector('x', 3)` creates the distinct parameters `x[0]`, `x[1]`, `x[2]`; it does not auto-grow, so `x[3]` raises `IndexError`.",
      "source": {
        "type": "proof",
        "ref": "s3-q022"
      }
    },
    {
      "objective": "s3o3",
      "group": "core",
      "fact_md": "Reusing ONE `Parameter` object on two gates keeps `num_parameters` at 1 and binds BOTH gates at once — the weight-sharing mechanism behind tied ansätze.",
      "source": {
        "type": "proof",
        "ref": "s3-q051"
      }
    },
    {
      "objective": "s3o3",
      "group": "core",
      "fact_md": "Parameters survive transpilation: transpile once into an ISA circuit, then bind as many times as you like — the bound circuit keeps its layout and needs no second transpile.",
      "source": {
        "type": "proof",
        "ref": "s3-q050"
      }
    },
    {
      "objective": "s3o3",
      "group": "trap",
      "fact_md": "A list binds positionally against the sorted view: `[0.5, 1.5]` on a circuit with `rx(theta)` then `rz(phi)` gives `phi = 0.5` and `theta = 1.5` — the reverse of gate order.",
      "source": {
        "type": "proof",
        "ref": "s3-q017"
      }
    },
    {
      "objective": "s3o3",
      "group": "trap",
      "fact_md": "`2 * th` is a `ParameterExpression`, not a `Parameter`: `isinstance(2 * th, Parameter)` is False, while `qc.parameters` still reports one free `Parameter`. Binding keeps the symbolic type — cast with `float(...)`.",
      "source": {
        "type": "proof",
        "ref": "s3-q054"
      }
    },
    {
      "objective": "s3o4",
      "group": "core",
      "fact_md": "`generate_preset_pass_manager` defaults to `optimization_level=2` in Qiskit 2.5 (a signature default, backend-independent) — level 1 was `transpile()`'s default back in 1.x.",
      "source": {
        "type": "proof",
        "ref": "s3-q031"
      }
    },
    {
      "objective": "s3o4",
      "group": "core",
      "fact_md": "Measured routing cost: one `cx(0, 4)` on a 5-qubit line gives 10 CX at level 0 (trivial layout `[0,1,2,3,4]`) versus 1 CX at level 1 — the win is LAYOUT, not gate cancellation.",
      "source": {
        "type": "proof",
        "ref": "s3-q032"
      }
    },
    {
      "objective": "s3o4",
      "group": "core",
      "fact_md": "`initial_layout=[2, 3, 4]` is an ORDERED virtual→physical map (virtual 0 → physical 2, 1 → 3, 2 → 4), not a set of permitted qubits.",
      "source": {
        "type": "proof",
        "ref": "s3-q043"
      }
    },
    {
      "objective": "s3o4",
      "group": "core",
      "fact_md": "Passes by stage in a level-2 preset: `VF2Layout`/`SabreLayout` in `layout`; `CheckMap`/`SabreSwap`/`VF2PostLayout` in `routing`; `BasisTranslator` in `translation` and `init`; `Optimize1qGatesDecomposition` in `optimization`; `ConsolidateBlocks` in `init`.",
      "source": {
        "type": "proof",
        "ref": "s3-q055"
      }
    },
    {
      "objective": "s3o4",
      "group": "core",
      "fact_md": "Level 0 schedules no optimization pass at all: nine gates in, nine out, where level 2 returns five — BOTH repeated pairs (`cx`-`cx` and `z`-`z`) cancel together, never one without the other.",
      "source": {
        "type": "proof",
        "ref": "s3-q057"
      }
    },
    {
      "objective": "s3o4",
      "group": "trap",
      "fact_md": "`decompose()` is not a route to ISA: it rewrites `h` into `u`, which is outside the IBM basis. An `h`, a `ccx`, a non-adjacent `cx` and an over-wide circuit all fail the check.",
      "source": {
        "type": "proof",
        "ref": "s3-q033"
      }
    },
    {
      "objective": "s3o4",
      "group": "trap",
      "fact_md": "A pass manager runs with `pm.run(qc)`; `pm.transpile(qc)`, `pm(qc)` and `qc.transpile(pm)` do not exist.",
      "source": {
        "type": "proof",
        "ref": "s3-q040"
      }
    },
    {
      "objective": "s3o4",
      "group": "trap",
      "fact_md": "Without a backend object, pass `basis_gates=` AND `coupling_map=`; `basis_gates` alone translates but does not route, so the output is not ISA.",
      "source": {
        "type": "proof",
        "ref": "s3-q035"
      }
    }
  ],
  "checklist": [
    "Declare the classical bits before you measure — `QuantumCircuit(2)` has none.",
    "Read every counts bitstring right-to-left: classical bit 0 is the rightmost character.",
    "Assign the result of `compose` and `assign_parameters`, or pass `inplace=True`.",
    "Write each condition as one `(resource, value)` tuple inside a `with` block; `c_if` is gone.",
    "Sort `qc.parameters` alphabetically in your head before binding a positional list — or bind a dict.",
    "Check BOTH ISA conditions on anything you send to hardware: basis gates and coupling map.",
    "Name the six preset stages in order, and say which one inserts the SWAPs.",
    "Run a pass manager with `pm.run(qc)`, at an optimization level between 0 and 3."
  ],
  "provenance": {
    "generated": "2026-08-15",
    "model": "claude-opus (v2 study rewrite agent)",
    "fact_checked": true,
    "notes": "v2 claim-preserving rewrite of fact-checked v1 (generated 2026-07-26, claude-opus-5 s2/s3 quick-study generation agent). No new factual claims: every sentence, table cell and code line restates content already present in the v1 primers or facts of this file, condensed. Every kept fact was re-read against its v1 wording before being shortened and keeps its original source unchanged; no fact was merged across two different sources. Structural changes only: the four long v1 primers became seven teaching units (s3o1 bits and measurement + composition; s3o2 the four control-flow context managers + the removed conditional API; s3o3 parameter ordering and binding; s3o4 ISA and the six stages + the four optimization levels), with the measure methods, compose/append/tensor, the control-flow constructs, the removed c_if spellings, the binding forms, the six stages and the four levels moved into pipe tables. Facts 43 -> 23, triaged into core/trap. Deleted because a primer or its table now carries the claim in full: measure(0,1)/measure(1,0) mapping (s3-q045, primer code fence), tensor endianness (s3-q046, table cell), the registers/find_bit citation (guides/construct-circuits, still cited by both s3o1 primers), the else-branch spelling (s3-q026), the for_loop repeat count (s3-q028), c_if removal (s3-q025), the dynamic-circuit definition and the four constructs (guides/classical-feedforward-and-control-flow, still cited by both s3o2 primers), the short-list ValueError (s3-q019), functional assign_parameters (s3-q020), partial dict binding (s3-q049), parameter-expression bind values (s3-q021), the alphabetical-order example (s3-q018, now the s3o3 primer's lead sentence), the six-stage tuple (s3-q030), the translation-stage example (s3-q037), optimization_level=4 (s3-q039, now the s3o4 level primer's lead), the layout-vs-routing citation (guides/transpiler-stages, still cited by the s3o4 stage primer), and both set-optimization citations — the level table and the 'higher is not always better' example — which became the level table and its closing paragraph (guides/set-optimization still cited there). Deleted as edge-case trivia: the mitigation/sampling non-stages fact (s3-q042), covered by the stage table. The s3-q033 fact was narrowed to its decompose()/ISA-check half and s3-q024 to its tuple-spelling half, because the primers now carry the rest. fact_checked stays true: the v1 file was fact-checked against stored proof artifacts and fetched documentation (v1 note preserved below), and this pass introduced no claim that was not already in it. Original v1 provenance note: \"v2 claim-preserving rewrite of fact-checked v1 (generated 2026-07-26, claude-opus-5 s2/s3 quick-study generation agent). No new factual claims: every sentence, table cell and code line restates content already present in the v1 primers or facts of this file, condensed. Every kept fact was re-read against its v1 wording before being shortened and keeps its original source unchanged; no fact was merged across two different sources. Structural changes only: the four long v1 primers became seven teaching units (s3o1 bits and measurement + composition; s3o2 the four control-flow context managers + the removed conditional API; s3o3 parameter ordering and binding; s3o4 ISA and the six stages + the four optimization levels), with the measure methods, compose/append/tensor, the control-flow constructs, the removed c_if spellings, the binding forms, the six stages and the four levels moved into pipe tables. Facts 43 -> 23, triaged into core/trap. Deleted because a primer or its table now carries the claim in full: measure(0,1)/measure(1,0) mapping (s3-q045, primer code fence), tensor endianness (s3-q046, table cell), the registers/find_bit citation (guides/construct-circuits, still cited by both s3o1 primers), the else-branch spelling (s3-q026), the for_loop repeat count (s3-q028), c_if removal (s3-q025), the dynamic-circuit definition and the four constructs (guides/classical-feedforward-and-control-flow, still cited by both s3o2 primers), the short-list ValueError (s3-q019), functional assign_parameters (s3-q020), partial dict binding (s3-q049), parameter-expression bind values (s3-q021), the alphabetical-order example (s3-q018, now the s3o3 primer's lead sentence), the six-stage tuple (s3-q030), the translation-stage example (s3-q037), optimization_level=4 (s3-q039, now the s3o4 level primer's lead), the layout-vs-routing citation (guides/transpiler-stages, still cited by the s3o4 stage primer), and both set-optimization citations — the level table and the 'higher is not always better' example — which became the level table and its closing paragraph (guides/set-optimization still cited there). Deleted as edge-case trivia: the mitigation/sampling non-stages fact (s3-q042), covered by the stage table. The s3-q033 fact was narrowed to its decompose()/ISA-check half and s3-q024 to its tuple-spelling half, because the primers now carry the rest. fact_checked stays true: the v1 file was fact-checked against stored proof artifacts and fetched documentation (v1 note preserved below), and this pass introduced no claim that was not already in it. Original v1 provenance note: \"v2 claim-preserving rewrite of fact-checked v1 (generated 2026-07-26, claude-opus-5 s2/s3 quick-study generation agent). No new factual claims: every sentence, table cell and code line restates content already present in the v1 primers or facts of this file, condensed. Every kept fact was re-read against its v1 wording before being shortened and keeps its original source unchanged; no fact was merged across two different sources. Structural changes only: the four long v1 primers became seven teaching units (s3o1 bits and measurement + composition; s3o2 the four control-flow context managers + the removed conditional API; s3o3 parameter ordering and binding; s3o4 ISA and the six stages + the four optimization levels), with the measure methods, compose/append/tensor, the control-flow constructs, the removed c_if spellings, the binding forms, the six stages and the four levels moved into pipe tables. Facts 43 -> 23, triaged into core/trap. Deleted because a primer or its table now carries the claim in full: measure(0,1)/measure(1,0) mapping (s3-q045, primer code fence), tensor endianness (s3-q046, table cell), the registers/find_bit citation (guides/construct-circuits, still cited by both s3o1 primers), the else-branch spelling (s3-q026), the for_loop repeat count (s3-q028), c_if removal (s3-q025), the dynamic-circuit definition and the four constructs (guides/classical-feedforward-and-control-flow, still cited by both s3o2 primers), the short-list ValueError (s3-q019), functional assign_parameters (s3-q020), partial dict binding (s3-q049), parameter-expression bind values (s3-q021), the alphabetical-order example (s3-q018, now the s3o3 primer's lead sentence), the six-stage tuple (s3-q030), the translation-stage example (s3-q037), optimization_level=4 (s3-q039, now the s3o4 level primer's lead), the layout-vs-routing citation (guides/transpiler-stages, still cited by the s3o4 stage primer), and both set-optimization citations — the level table and the 'higher is not always better' example — which became the level table and its closing paragraph (guides/set-optimization still cited there). Deleted as edge-case trivia: the mitigation/sampling non-stages fact (s3-q042), covered by the stage table. The s3-q033 fact was narrowed to its decompose()/ISA-check half and s3-q024 to its tuple-spelling half, because the primers now carry the rest. fact_checked stays true: the v1 file was fact-checked against stored proof artifacts and fetched documentation (v1 note preserved below), and this pass introduced no claim that was not already in it. Original v1 provenance note: \"v2 claim-preserving rewrite of fact-checked v1 (generated 2026-07-26, claude-opus-5 s2/s3 quick-study generation agent). No new factual claims: every sentence, table cell and code line restates content already present in the v1 primers or facts of this file, condensed. Every kept fact was re-read against its v1 wording before being shortened and keeps its original source unchanged; no fact was merged across two different sources. Structural changes only: the four long v1 primers became seven teaching units (s3o1 bits and measurement + composition; s3o2 the four control-flow context managers + the removed conditional API; s3o3 parameter ordering and binding; s3o4 ISA and the six stages + the four optimization levels), with the measure methods, compose/append/tensor, the control-flow constructs, the removed c_if spellings, the binding forms, the six stages and the four levels moved into pipe tables. Facts 43 -> 23, triaged into core/trap. Deleted because a primer or its table now carries the claim in full: measure(0,1)/measure(1,0) mapping (s3-q045, primer code fence), tensor endianness (s3-q046, table cell), the registers/find_bit citation (guides/construct-circuits, still cited by both s3o1 primers), the else-branch spelling (s3-q026), the for_loop repeat count (s3-q028), c_if removal (s3-q025), the dynamic-circuit definition and the four constructs (guides/classical-feedforward-and-control-flow, still cited by both s3o2 primers), the short-list ValueError (s3-q019), functional assign_parameters (s3-q020), partial dict binding (s3-q049), parameter-expression bind values (s3-q021), the alphabetical-order example (s3-q018, now the s3o3 primer's lead sentence), the six-stage tuple (s3-q030), the translation-stage example (s3-q037), optimization_level=4 (s3-q039, now the s3o4 level primer's lead), the layout-vs-routing citation (guides/transpiler-stages, still cited by the s3o4 stage primer), and both set-optimization citations — the level table and the 'higher is not always better' example — which became the level table and its closing paragraph (guides/set-optimization still cited there). Deleted as edge-case trivia: the mitigation/sampling non-stages fact (s3-q042), covered by the stage table. The s3-q033 fact was narrowed to its decompose()/ISA-check half and s3-q024 to its tuple-spelling half, because the primers now carry the rest. fact_checked stays true: the v1 file was fact-checked against stored proof artifacts and fetched documentation (v1 note preserved below), and this pass introduced no claim that was not already in it. Original v1 provenance note: \"Every proof-sourced fact was read back against the stored evidence in data/proofs/<qid>.json (verdict.evidence strings and observed values) before being written; nothing is asserted beyond what the artifact records. Four candidate facts originally drawn from s3-q027/q036/q038/q048 were RE-SOURCED to citations because those questions are proof.status=conceptual with no artifact — a proof-typed source would have rendered a false 'executed' badge. The five citation-sourced facts were each checked by curl -L fetching the page (final 200, slug match) and reading the supporting passage: set-optimization's optimization-level table and its 'increasing the level does not always make a difference' example; transpiler-stages on layout selecting qubits while routing injects SWAPs; construct-circuits on combining named registers and find_bit; classical-feedforward-and-control-flow on the definition of dynamic circuits and the four context-manager constructs. Primer claims without a proof of their own were re-executed on the pinned stack (qiskit 2.5.0 / aer 0.17.2 / runtime 0.48.0, PYTHONPATH=pipeline/harness): generate_preset_pass_manager's signature default is 2 and its no-argument output matches an explicit level 2; transpile()'s optimization_level default is None and resolves to the same level 2; compose(front=True) prepends while the default appends; append mutates in place and returns an InstructionSet; ParameterVector elements sort by numeric index (x[9] then x[10] then x[11]) while plain Parameter('x10')/Parameter('x2') sort as strings; pm.run([qc]) returns a list; measure_active names its register 'meas' and only measures active qubits; h.decompose() gives u. Remaining primer statements (bind_parameters removed, dict keys matched by identity with name strings also accepted, ParameterVector no auto-growth, duplicate register names rejected, plain-Python if silently False, basis_gates without coupling_map does not route, stages tuple constant) are pre-verified REVIEW_LEDGER entries from the s3 pool wave; no ledger entry is contradicted. Noted deliberately: guides/transpiler-stages and guides/set-optimization still describe optimization_level as a required positional argument while the 2.5 signature carries the default 2 — the primer states the library reality and flags the docs wording. | official-alignment fix wave (2026-07-26): two facts added, both sourced by NEW executed questions — s3-q054 (2*th is a ParameterExpression, isinstance(expr, Parameter) is False, qc.parameters still holds one free Parameter, and bind() keeps the symbolic type) and s3-q055 (stage membership walked out of a level-2 preset StagedPassManager: VF2Layout/SabreLayout in layout, CheckMap/SabreSwap/VF2PostLayout in routing, BasisTranslator in translation as well as init and the optimization loop, Optimize1qGatesDecomposition in optimization, ConsolidateBlocks in init at levels 2-3). Both artifacts were re-read after verification; nothing is asserted beyond the recorded evidence. | figure wave 2 (task #28, 2026-07-27): 2 fact(s) added, each sourced by a NEW executed figure question (s3-q057, s3-q058); every claim was read back against the stored evidence in data/proofs/<qid>.json before being written and nothing is asserted beyond it.\"\"\"\""
  }
}
