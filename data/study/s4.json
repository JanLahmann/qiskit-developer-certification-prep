{
  "section": "s4",
  "version": 2,
  "primers": [
    {
      "objective": "s4o1",
      "title": "Three modes, and the `mode=` that selects them",
      "body_md": "Qiskit Runtime schedules work in exactly three ways, and the mode is chosen by an ARGUMENT, never by lexical position.\n\n```python\nwith Batch(backend=backend) as batch:\n    sampler = SamplerV2(mode=batch)   # or omit mode= and inherit the context\n```\n\n| Mode | How you open it | Use it when |\n| --- | --- | --- |\n| job | `SamplerV2(mode=backend)`, no context manager | you are submitting a single primitive request |\n| batch | `with Batch(backend=backend):` | the jobs are independent and all known at the outset: you queue once, their classical pre-processing is threaded in parallel, and the executions are packed tightly, which limits device drift |\n| session | `with Session(backend=backend):` | the next circuit depends on the previous result, or PEC/PEA need a once-learned noise model to stay valid — the active window is exclusive, calibration jobs included |\n\nThe documented default is blunt: use batch unless your inputs are not all ready, use a session for iterative workloads or genuinely dedicated access, and always use job mode for a single request. Sessions are the expensive option, so \"wrap it in a session to be safe\" is the wrong instinct — and the Open Plan cannot submit session jobs at all. `mode=` accepts a Backend, a Session or a Batch object; writing `mode=backend` INSIDE a batch context silently puts you back in job mode.",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/guides/execution-modes",
        "https://quantum.cloud.ibm.com/docs/guides/choose-execution-mode",
        "https://quantum.cloud.ibm.com/docs/guides/run-jobs-session"
      ]
    },
    {
      "objective": "s4o1",
      "title": "Two timers govern a session or batch",
      "body_md": "The maximum TTL starts when the first job begins RUNNING — not when you open the context — and it does not pause.\n\n| Timer | What it measures | Configurable? |\n| --- | --- | --- |\n| maximum TTL | from the first running job until the workload ends | yes, with `max_time`; the defaults are 8 hours on paid plans and 10 minutes on the Open Plan |\n| interactive TTL | the idle gap allowed between consecutive jobs | no — one minute for batches |\n\nWhen the maximum TTL is reached the workload is terminated: jobs already running continue, and jobs still queued are put into a failed state. Exceeding the interactive TTL is milder — the workload is temporarily deactivated until a new job works its way back through the normal queue.\n\nClosing kills nothing. Calling `close()`, or simply exiting the `with` block, moves the workload to \"In progress, not accepting new jobs\": everything already submitted runs to completion and the results stay retrievable.",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/guides/max-execution-time",
        "https://quantum.cloud.ibm.com/docs/guides/run-jobs-batch",
        "https://quantum.cloud.ibm.com/docs/guides/run-jobs-session"
      ]
    },
    {
      "objective": "s4o2",
      "title": "ISA in, PUB results out",
      "body_md": "V2 primitives accept ISA circuits ONLY — every instruction native to the backend's target, every two-qubit gate on a connected pair — and they never transpile for you.\n\n```python\npm = generate_preset_pass_manager(backend=backend, optimization_level=1)\nisa = pm.run(qc)\nresult = SamplerV2(mode=backend).run([isa], shots=1024)   # run(pubs, *, shots=None)\n```\n\n| Step | What you do | What can go wrong |\n| --- | --- | --- |\n| map | write the problem as circuits and observables | — |\n| optimize | `pm.run(qc)` for an ISA circuit | non-ISA input raises `IBMInputValueError`, naming the offending instruction |\n| execute | `run([isa1, isa2])` — ONE iterable of PUBs, `shots` keyword-only | you get back a `PrimitiveResult` holding one PUB result per PUB, in order |\n| post-process | read `result[0].data.meas` (Sampler) or `result[0].data.evs` (Estimator) | a circuit with no measurement yields a `DataBin` with no fields |\n\nTranspiling also widens the circuit to the device's qubit count, and that is what breaks the Estimator half of the workflow: the observable must be mapped through the same layout with `obs.apply_layout(isa.layout)`, because nothing pads a narrow observable for you. Measurements, by contrast, survive transpilation untouched.",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/guides/transpile",
        "https://quantum.cloud.ibm.com/docs/guides/get-started-with-sampler",
        "https://quantum.cloud.ibm.com/docs/guides/primitive-input-output"
      ]
    },
    {
      "objective": "s4o2",
      "title": "Credentials, backend choice, rehearsing without a QPU",
      "body_md": "A saved account is what lets `QiskitRuntimeService()` work with no arguments.\n\n```python\nQiskitRuntimeService.save_account(token=\"<api-key>\", instance=\"<CRN>\",\n                                  name=\"me\", set_as_default=True)\nservice = QiskitRuntimeService()   # channel defaults to ibm_quantum_platform\n```\n\nThe parameter is `token`, not `api_key`, `instance` is a Cloud Resource Name, and the file written is `$HOME/.qiskit/qiskit-ibm.json` — do this only in a trusted environment. With several saved accounts, none marked default and none named at initialization, the account whose name comes LAST alphabetically is used.\n\n| Call | Gives you |\n| --- | --- |\n| `service.least_busy(operational=True, simulator=False)` | the operational real QPU with the shortest queue; `min_num_qubits` and a `filters` callable are also accepted |\n| `service.backends()` | a list that is NOT queue-sorted |\n| `service.backend(\"name\")` | one named backend — the name is positional |\n| a `qiskit_ibm_runtime.fake_provider` backend as `mode=` | a snapshot of a real QPU, with its coupling map, basis gates and noise model |\n| an Aer simulator as `mode=` | local execution; `QiskitRuntimeService(channel=\"local\")` does the same at service level |\n\nLocal testing mode promises that only the backend changes when you move to hardware. Two local quirks: `session.details()` and `session.session_id` come back `None`, and a fake backend falls through to the local 1024-shot default where the Runtime service uses 4096 — so state `shots` explicitly.",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/guides/initialize-account",
        "https://quantum.cloud.ibm.com/docs/guides/save-credentials",
        "https://quantum.cloud.ibm.com/docs/guides/local-testing-mode",
        "https://quantum.cloud.ibm.com/docs/guides/get-started-with-sampler"
      ]
    }
  ],
  "facts": [
    {
      "objective": "s4o1",
      "group": "core",
      "fact_md": "Batched jobs are NOT guaranteed to run in submission order and get no exclusive access — other users' jobs and QPU calibration jobs can interleave.",
      "source": {
        "type": "citation",
        "ref": "https://quantum.cloud.ibm.com/docs/guides/execution-modes"
      }
    },
    {
      "objective": "s4o1",
      "group": "core",
      "fact_md": "Queuing time does not decrease for the FIRST job submitted within a batch or a session, so neither mode benefits a single-job workload.",
      "source": {
        "type": "citation",
        "ref": "https://quantum.cloud.ibm.com/docs/guides/execution-modes"
      }
    },
    {
      "objective": "s4o1",
      "group": "trap",
      "fact_md": "The V1 spelling is gone: `SamplerV2(backend=backend)` raises `TypeError: ... unexpected keyword argument 'backend'`, and `run(..., mode=...)` fails the same way — `mode` is a CONSTRUCTOR argument only.",
      "source": {
        "type": "proof",
        "ref": "s4-q016"
      }
    },
    {
      "objective": "s4o1",
      "group": "trap",
      "fact_md": "A primitive with no mode and no enclosing context raises `ValueError: A backend or session must be specified.` in the CONSTRUCTOR — `run()` is never reached.",
      "source": {
        "type": "proof",
        "ref": "s4-q021"
      }
    },
    {
      "objective": "s4o1",
      "group": "trap",
      "fact_md": "`mode` does not resolve names: `SamplerV2(mode=\"ibm_manila\")` raises `ValueError: mode must be of type Backend, Session, Batch or None`.",
      "source": {
        "type": "proof",
        "ref": "s4-q032"
      }
    },
    {
      "objective": "s4o1",
      "group": "trap",
      "fact_md": "`Session.backend()` returns the QPU's NAME as a string (`'fake_manila'`), not a Backend object — so it cannot be fed straight back into `mode=`.",
      "source": {
        "type": "proof",
        "ref": "s4-q042"
      }
    },
    {
      "objective": "s4o2",
      "group": "core",
      "fact_md": "Transpiling widens the circuit to the device, so an unmapped observable breaks: `ValueError: The number of qubits of the circuit (5) does not match the number of qubits of the ()-th observable (2)`.",
      "source": {
        "type": "proof",
        "ref": "s4-q018"
      }
    },
    {
      "objective": "s4o2",
      "group": "core",
      "fact_md": "With `mapped = obs.apply_layout(isa.layout)` the same Estimator run returns `evs ≈ 0.878`; `SparsePauliOp(\"ZZ\", target=...)` is a `TypeError`, not a shortcut.",
      "source": {
        "type": "proof",
        "ref": "s4-q036"
      }
    },
    {
      "objective": "s4o2",
      "group": "core",
      "fact_md": "Device noise trims the signal rather than destroying it: a Bell state's ⟨ZZ⟩ through the Estimator on a noisy fake backend comes back at 0.874 — close to `+1`, never above it.",
      "source": {
        "type": "proof",
        "ref": "s4-q044"
      }
    },
    {
      "objective": "s4o2",
      "group": "core",
      "fact_md": "The guide names four broadcasting patterns: *broadcast single observable* (observables shape `()`, reused at every parameter value), *zip* (identically shaped arrays matched element by element), *outer/product* (`(1, 4)` parameters with `(3, 1)` observables give `(3, 4)`), and *standard nd generalization* (an extra leading axis on the observables).",
      "source": {
        "type": "proof",
        "ref": "s4-q045"
      }
    },
    {
      "objective": "s4o2",
      "group": "core",
      "fact_md": "Zip is the one pattern that does not multiply the work: `(3,)` parameters with `(3,)` observables give three estimates. Its look-alikes: one observable over the same parameters is *broadcast*, `(3, 1)` against `(3,)` is a product, and `(3,)` against `(4,)` is a `ValueError`.",
      "source": {
        "type": "proof",
        "ref": "s4-q046"
      }
    },
    {
      "objective": "s4o2",
      "group": "core",
      "fact_md": "Zip in the wild: five observables plus a `(5, 1)` value array on a ONE-parameter circuit coerce to shapes `(5,)` and `(5,)`, so `evs.shape` is `(5,)` — with one free parameter the trailing axis is the parameter axis, not a broadcast axis.",
      "source": {
        "type": "proof",
        "ref": "s4-q048"
      }
    },
    {
      "objective": "s4o2",
      "group": "trap",
      "fact_md": "Primitives reject non-ISA circuits: an untranspiled `h` raises `IBMInputValueError: The instruction h on qubits (0,) is not supported by the target system` — and a fake backend enforces this exactly like hardware, so local testing mode is no escape.",
      "source": {
        "type": "proof",
        "ref": "s4-q012"
      }
    },
    {
      "objective": "s4o2",
      "group": "trap",
      "fact_md": "Native basis gates alone are not enough: a `cx` between the non-adjacent qubits 0 and 4 of a line-topology device raises the same `IBMInputValueError` — an ISA circuit must satisfy the coupling map too.",
      "source": {
        "type": "proof",
        "ref": "s4-q026"
      }
    },
    {
      "objective": "s4o2",
      "group": "trap",
      "fact_md": "With no measurement in the circuit, `result[0].data.meas` raises `AttributeError`; call `qc.measure_all()` BEFORE transpiling.",
      "source": {
        "type": "proof",
        "ref": "s4-q023"
      }
    }
  ],
  "checklist": [
    "Pick job mode for a single request, batch for independent jobs, session for iterative work.",
    "Pass the execution target as `mode=` — a Backend, Session or Batch object, never a name string.",
    "Transpile with a preset pass manager before every primitive run; V2 primitives reject non-ISA circuits.",
    "Map every observable through `isa.layout` with `apply_layout` before handing it to the Estimator.",
    "Hand `run()` one iterable of PUBs, and state `shots` explicitly rather than trusting a default.",
    "Add measurements before transpiling, or the Sampler's `DataBin` comes back with no fields.",
    "Remember the maximum TTL starts at the first RUNNING job, not when you open the context.",
    "Rehearse on a fake backend or Aer, then change only the backend to reach hardware."
  ],
  "provenance": {
    "generated": "2026-08-15",
    "model": "claude-opus (v2 study rewrite agent)",
    "fact_checked": true,
    "notes": "v2 claim-preserving rewrite of fact-checked v1 (generated 2026-07-26, claude-opus-5 s4/s5 quick-study generation agent). No new factual claims: every sentence, table cell and code line restates content already present in the v1 primers or facts of this file, condensed. Every kept fact was re-read against its v1 wording before being shortened and keeps its original source unchanged; no fact was merged across two different sources. Structural changes only: the three long v1 primers became four teaching units (s4o1 the three modes + the two lifetime timers; s4o2 the ISA/PUB workflow + credentials, backend choice and local testing), with the execution modes, the TTL timers, the four workflow steps and the backend-choice/rehearsal options moved into pipe tables. Facts 26 -> 15, triaged into core/trap. Deleted because a primer or its table now carries the claim in full: the `mode=` accepted types (s4-q013, primer code fence and closing paragraph), the best-practice mode ordering (guides/choose-execution-mode, now cited by the s4o1 mode primer), the maximum-TTL start and queued-job failure (guides/max-execution-time, cited by the lifetime primer), the interactive TTL and default TTLs (guides/run-jobs-batch, citation moved onto the lifetime primer), the Open Plan session restriction and the close semantics (guides/run-jobs-session, cited by both s4o1 primers), the one-iterable-of-PUBs rule (s4-q043, now the execute row), the default channel and last-alphabetically tie-break (guides/initialize-account), save_account's token/path/trusted-environment note (guides/save-credentials), and the definition of local testing mode (guides/local-testing-mode) plus the least_busy call (guides/get-started-with-sampler, citation moved onto the credentials primer). The s4-q012 fact keeps its full error message and the fake-backend clause because the table row states only the error type; s4-q023 was narrowed to the `AttributeError` and its fix, since the table carries the empty `DataBin` and the primer carries measurement survival. fact_checked stays true: the v1 file was fact-checked against stored proof artifacts and fetched documentation (v1 note preserved below), and this pass introduced no claim that was not already in it. Original v1 provenance note: \"Every proof-sourced fact was checked against the stored evidence in data/proofs/<qid>.json and only EXECUTED questions with an artifact are cited (s4 conceptual qids q001/q010/q011/q014/q015/q017/q019/q020/q022/q024/q025/q027/q029/q030/q031/q033/q035/q037/q039/q040/q041 are cited through their guide instead). Every citation-sourced fact was confirmed by fetching the page text (execution-modes: batch ordering/exclusivity and the first-job queuing note; choose-execution-mode: batch-unless/always-job-mode/sessions-more-expensive; max-execution-time: TTL starts at first job, queued jobs fail; run-jobs-batch: interactive TTL 1 min, 8 h paid / 10 min Open; run-jobs-session: Open Plan cannot submit session jobs, close semantics; initialize-account: default channel ibm_quantum_platform and the last-alphabetically rule; save-credentials: token=, $HOME/.qiskit/qiskit-ibm.json; local-testing-mode: fake provider / Aer as mode=; get-started-with-sampler: least_busy(operational=True, simulator=False)). Primer claims not covered by a proof were re-executed on the pinned stack (qiskit 2.5.0 / aer 0.17.2 / runtime 0.48.0): least_busy accepts filters=, save_account has token= and no api_key, a no-mode primitive inside `with Session(...)` inherits the session and runs, Session/Batch public API is backend/cancel/close/details/from_id/service/session_id/status/usage, session.details() and session.session_id are None locally while session.backend() returns 'fake_manila', and a fake-backend run with no shots falls back to 1024 (vs the Runtime service's 4096). Consistent with the REVIEW_LEDGER s4 pool wave; no contradictions. | official-alignment fix wave (2026-07-26): two s4o2 facts added for the guide's NAMED broadcasting patterns (task 4.2 gap), sourced by the new executed questions s4-q045 and s4-q046. The pattern names are quoted from guides/primitive-input-output as fetched 2026-07-26 ('Broadcast single observable', 'Zip', 'Outer/Product', 'Standard nd generalization'); every shape claim in the facts was measured with StatevectorEstimator on a one-parameter circuit — (1,4)x(3,1) -> (3,4), (3,)x(3,) -> (3,), (3,)x() -> (3,), (3,1)x(3,) -> (3,3), (2,3)x(4,2,1) -> (4,2,3), and (3,)x(4,) raises ValueError. | figure wave 2 (task #28, 2026-07-27): 1 fact(s) added, each sourced by a NEW executed figure question (s4-q048); every claim was read back against the stored evidence in data/proofs/<qid>.json before being written and nothing is asserted beyond it.\""
  }
}
