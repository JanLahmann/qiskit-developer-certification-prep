{
  "section": "s5",
  "version": 2,
  "primers": [
    {
      "objective": "s5o1",
      "title": "The SamplerOptions tree — and the knob that is not in it",
      "body_md": "`SamplerOptions` is a validated pydantic dataclass, so every field name is checked — and **SamplerV2 has no `resilience_level`**. That knob belongs to EstimatorV2, where it drives expectation-value mitigation (TREX at level 1, ZNE at level 2). The exam blueprint's own wording, \"set sampler primitive options such as resilience levels\", is the bait.\n\n| Level | Fields |\n| --- | --- |\n| top | `max_execution_time`, `environment`, `simulator`, `default_shots`, `dynamical_decoupling`, `execution`, `twirling`, `experimental` |\n| `dynamical_decoupling` | `enable`, `sequence_type` (`XX` default, `XpXm`, `XY4`), `extra_slack_distribution`, `scheduling_method`, `skip_reset_qubits` |\n| `twirling` | `enable_gates`, `enable_measure`, `num_randomizations`, `shots_per_randomization`, `strategy` |\n| `execution` | `init_qubits`, `rep_delay`, `meas_type` — no `shots`, no `seed_simulator` |\n| `simulator` | `noise_model`, `seed_simulator`, `coupling_map`, `basis_gates` |\n\nThe nested groups are where the questions probe, so learn the DEPTH of each knob and not just its name. Anything misspelled is rejected outright: `experimental` is an opt-in dict you fill yourself, not a catch-all for unknown names.",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/guides/sampler-options",
        "https://quantum.cloud.ibm.com/docs/api/qiskit-ibm-runtime/options-sampler-options"
      ]
    },
    {
      "objective": "s5o1",
      "title": "Setting options, and where the shot count comes from",
      "body_md": "Options you never touch stay `Unset` and take the server default, so your code's behaviour does not drift with the client version.\n\n```python\nsampler = SamplerV2(mode=backend, options={\"default_shots\": 4096})\nsampler.options.dynamical_decoupling.enable = True\nsampler.options.update(default_shots=1024,\n                       dynamical_decoupling={\"sequence_type\": \"XpXm\"})\n```\n\nAll three forms are valid: a mapping or a `SamplerOptions` object in the constructor, per-attribute assignment afterwards, or a bulk `update()`. The options object is not read-only. What does NOT exist is the V1 habit `sampler.set_options(...)`.\n\nShots then follow a documented precedence chain, and it is a favourite question.\n\n| Rank | Where the shot count comes from |\n| --- | --- |\n| 1 | the shot count carried in the PUB's third slot |\n| 2 | the `shots=` keyword on `run()` |\n| 3 | with twirling on, `num_randomizations` × `shots_per_randomization` |\n| 4 | `options.default_shots` |\n\n`shots` must be an integer — a one-element list raises `TypeError`. The default is environment-dependent too: a local fake backend falls through to 1024, while the Runtime service's own default is 4096.",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/guides/runtime-options-overview",
        "https://quantum.cloud.ibm.com/docs/guides/sampler-options"
      ]
    },
    {
      "objective": "s5o2",
      "title": "Nothing to switch off — so roll your own",
      "body_md": "The Sampler is deliberately the low-level primitive: it hands back the sampled bitstrings, shot by shot, with no readout correction applied. You do not \"bypass\" Sampler mitigation by switching something off, because there is nothing on.\n\n| Technique | Where it lives | What it does |\n| --- | --- | --- |\n| dynamical decoupling | Sampler, `options.dynamical_decoupling` | suppression: pulse sequences cancel coherent errors on idling qubits |\n| Pauli twirling of gates and/or measurements | Sampler, `options.twirling` | suppression: randomizes over an ensemble of equivalent circuits so arbitrary noise becomes Pauli noise |\n| TREX, zero-noise extrapolation, PEC, error amplification | Estimator only, `options.resilience.*` or `resilience_level` | mitigation: corrects expectation values |\n\nNeither suppression option rewrites your counts into \"mitigated\" values, and mitigation has no Sampler counterpart because there is no single number to rescale — only a histogram. So code the correction yourself, in post-processing.\n\n| Scheme | How it works | Trade-off |\n| --- | --- | --- |\n| post-selection | drop the shots that violate a known symmetry of the ideal output — a Bell circuit can only produce `00` or `11` — then renormalize over what survives | cheap and needs no calibration, but only works when the ideal support is known, and it throws information away |\n| assignment-matrix (confusion-matrix) inversion | prepare each computational basis state with calibration circuits, record what you read back, assemble the matrix mapping true outcomes to observed ones, and invert it (or least-squares invert it) | rigorous; this is what packages such as M3/mthree automate, and the raw per-shot record is exactly their input |\n\nWork on the arrays rather than the dictionaries: a `BitArray` is faster than round-tripping through `get_counts()`.",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/guides/sampler-noise-management",
        "https://quantum.cloud.ibm.com/docs/guides/error-mitigation-and-suppression-techniques",
        "https://quantum.cloud.ibm.com/docs/guides/primitive-input-output"
      ]
    },
    {
      "objective": "s5o3",
      "title": "PUBs, shots, and broadcasting",
      "body_md": "The Sampler executes a circuit many times and records the measured register each shot — draws from the distribution the circuit defines over computational-basis states. It never returns amplitudes or expectation values; probabilities are frequencies you compute yourself, and their statistical error shrinks like 1/√N_shots, so halving the error costs four times the shots.\n\nInput is a **PUB**, a Primitive Unified Bloc: a tuple of at most three entries, `(circuit, parameter_values, shots)`, the last two optional. The circuit must contain measurements, and `run()` takes a LIST of PUBs.\n\n```python\nresult = sampler.run([(isa, params, 200)])   # never run(isa)\n# (isa, 256, vals)     -> TypeError: shots must be an integer\n# (isa, vals, 200, 1)  -> ValueError: The length of pub must be 1, 2 or 3\n```\n\nParameter binding follows NumPy broadcasting with one rule bolted on: the LAST axis of the parameter array is reserved for the circuit's free parameters and is consumed during binding, and whatever shape precedes it becomes the result's shape.\n\n| Parameter array (circuit has 2 parameters) | `data.meas.shape` |\n| --- | --- |\n| `(6, 2)` | `(6,)` |\n| `(3, 4, 2)` | `(3, 4)`, indexable as `data.meas[i, j]` |\n| `(5, 3)` | `ValueError: Length of ('p[0]', 'p[1]') inconsistent with last dimension` |\n\nTransposing, flattening or splitting into one PUB per row all raise that same error; reshaping to `(5, 2)` is the fix.",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/guides/primitive-input-output",
        "https://quantum.cloud.ibm.com/docs/guides/primitives",
        "https://quantum.cloud.ibm.com/docs/tutorials/multi-product-formula"
      ]
    },
    {
      "objective": "s5o3",
      "title": "Reading a SamplerPubResult",
      "body_md": "`run()` returns a `PrimitiveResult` whose length is the number of PUBs you submitted, in submission order.\n\n| Access path | What you get |\n| --- | --- |\n| `len(result)` | the number of PUBs submitted — three circuits in one `run()` give three PUB results |\n| `result[0].data` | a `DataBin` holding one `BitArray` per classical register, named after the register (`data.alice`, `data.bob`) |\n| `.get_counts()` | the histogram, as a plain `dict` |\n| `.get_int_counts()` | the same histogram with the bitstrings decoded as integers (`'11'` becomes the key `3`) |\n| `.get_bitstrings()` | the raw per-shot list, in the order the shots were taken |\n| `.get_counts(0)` on a broadcast result | that one parameter set's 500 shots; with no index, `get_counts()` POOLS every set (4 × 500 = 2000) |\n\n`measure_all()` is what creates a register called `meas`, which is the only reason `data.meas` usually works. The per-shot view is the one that lets you correlate outcomes across registers before aggregating, which a counts dict cannot do.",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/guides/primitive-input-output"
      ]
    }
  ],
  "facts": [
    {
      "objective": "s5o1",
      "group": "core",
      "fact_md": "What IS settable on a SamplerV2: `options.default_shots`, `options.dynamical_decoupling.enable` and `options.twirling.enable_gates` — `resilience_level` and `optimization_level` both fail validation.",
      "source": {
        "type": "proof",
        "ref": "s5-q020"
      }
    },
    {
      "objective": "s5o1",
      "group": "core",
      "fact_md": "A shot count inside the PUB beats the option: with `options.default_shots = 1000`, the PUB `(isa, None, 50)` runs exactly 50 shots.",
      "source": {
        "type": "proof",
        "ref": "s5-q026"
      }
    },
    {
      "objective": "s5o1",
      "group": "trap",
      "fact_md": "`sampler.options.resilience_level = 1` raises a pydantic `ValidationError` (\"Object has no attribute 'resilience_level'\"), and passing it through the constructor's `options={...}` mapping is rejected too.",
      "source": {
        "type": "proof",
        "ref": "s5-q014"
      }
    },
    {
      "objective": "s5o1",
      "group": "trap",
      "fact_md": "Configuring is not enabling: dynamical decoupling needs `options.dynamical_decoupling.enable = True`, and setting `sequence_type` alone is accepted but leaves `enable` Unset.",
      "source": {
        "type": "proof",
        "ref": "s5-q019"
      }
    },
    {
      "objective": "s5o1",
      "group": "trap",
      "fact_md": "The simulator seed lives at `options.simulator.seed_simulator`; `options.execution.seed_simulator`, a top-level `seed_simulator`, `run(seed=42)` and `run(options={...})` are all rejected.",
      "source": {
        "type": "proof",
        "ref": "s5-q031"
      }
    },
    {
      "objective": "s5o1",
      "group": "trap",
      "fact_md": "`SamplerV2(mode=backend, options={\"default_shots\": 4096})` is the constructor form; a `\"shots\"` key is rejected, and `SamplerV2(shots=...)` or `(default_shots=...)` is a `TypeError`.",
      "source": {
        "type": "proof",
        "ref": "s5-q033"
      }
    },
    {
      "objective": "s5o1",
      "group": "trap",
      "fact_md": "`twirling_level` and `readout` are not fields of the `twirling` group — those near-miss names fail validation.",
      "source": {
        "type": "proof",
        "ref": "s5-q027"
      }
    },
    {
      "objective": "s5o2",
      "group": "core",
      "fact_md": "Post-selection is something you code: a Bell run's raw counts `{'00': 1941, '11': 1822, '01': 90, '10': 147}` become `{'00': 1941, '11': 1822}` by dropping the odd-parity shots and renormalizing.",
      "source": {
        "type": "proof",
        "ref": "s5-q028"
      }
    },
    {
      "objective": "s5o2",
      "group": "core",
      "fact_md": "`get_bitstrings()` returns one entry per shot in the order taken (777 shots → a list of length 777) — the un-aggregated record an assignment-matrix or M3-style correction needs as input.",
      "source": {
        "type": "proof",
        "ref": "s5-q029"
      }
    },
    {
      "objective": "s5o2",
      "group": "core",
      "fact_md": "A `BitArray` supports boolean-mask post-selection on another register, `slice_bits`, `slice_shots`, `expectation_values` for diagonal observables, and `concatenate_bits` to merge registers.",
      "source": {
        "type": "citation",
        "ref": "https://quantum.cloud.ibm.com/docs/guides/primitive-input-output"
      }
    },
    {
      "objective": "s5o3",
      "group": "core",
      "fact_md": "Shots are per parameter set, not per PUB: broadcasting over 6 values with `shots=200` performs 6 × 200 = 1200 circuit executions.",
      "source": {
        "type": "proof",
        "ref": "s5-q035"
      }
    },
    {
      "objective": "s5o3",
      "group": "core",
      "fact_md": "`get_counts()` tallies every shot, so its values sum to the shot count used (`shots=1000` → 1000) no matter how many distinct bitstrings appear.",
      "source": {
        "type": "proof",
        "ref": "s5-q010"
      }
    },
    {
      "objective": "s5o3",
      "group": "core",
      "fact_md": "Bitstrings are little-endian: qubit 0 is the LAST character, so read `b[-1]`, not `b[0]` — after `x(0)` on two qubits the dominant outcome is `'01'`.",
      "source": {
        "type": "proof",
        "ref": "s5-q032"
      }
    },
    {
      "objective": "s5o3",
      "group": "core",
      "fact_md": "Runtime `SamplerV2` rejects a non-ISA circuit with `IBMInputValueError`; the fix is a preset pass manager transpiled for the backend, not a change to the run call.",
      "source": {
        "type": "proof",
        "ref": "s5-q013"
      }
    },
    {
      "objective": "s5o3",
      "group": "core",
      "fact_md": "The reference `StatevectorSampler` in `qiskit.primitives` still accepts abstract (non-ISA) instructions because it runs a local statevector simulation — the ISA requirement is a Runtime-primitive rule.",
      "source": {
        "type": "citation",
        "ref": "https://quantum.cloud.ibm.com/docs/guides/simulate-with-qiskit-sdk-primitives"
      }
    },
    {
      "objective": "s5o3",
      "group": "trap",
      "fact_md": "`result[0].data.meas` is a `BitArray` of per-shot outcomes, not a counts dict; `get_counts()` is the summarized view and returns a plain `dict`.",
      "source": {
        "type": "proof",
        "ref": "s5-q011"
      }
    },
    {
      "objective": "s5o3",
      "group": "trap",
      "fact_md": "`QuantumCircuit(2, 2)` names its register `c`, so the outcomes are in `data.c`; the name `meas` exists only when the circuit used `measure_all()`.",
      "source": {
        "type": "proof",
        "ref": "s5-q025"
      }
    },
    {
      "objective": "s5o3",
      "group": "trap",
      "fact_md": "`get_counts()` lists only the outcomes that actually occurred: a 40-shot two-qubit run in which `10` never appeared returns a three-key dict, so `plot_histogram` draws three bars — nothing pads the missing outcome to a zero-height bar.",
      "source": {
        "type": "proof",
        "ref": "s5-q041"
      }
    },
    {
      "objective": "s5o3",
      "group": "trap",
      "fact_md": "A fake backend is not an exact simulator: a seeded 2000-shot run of `h(0); cx(0,1); x(2)` on FakeManilaV2 puts weight on all eight outcomes, the two ideal peaks `100` and `111` holding about 43% and 40% of the shots.",
      "source": {
        "type": "proof",
        "ref": "s5-q040"
      }
    }
  ],
  "checklist": [
    "Place each SamplerV2 knob at its level: `dynamical_decoupling`, `twirling`, `execution`, `simulator` — and know SamplerV2 has no `resilience_level`.",
    "Enable a technique explicitly; setting a sub-option such as `sequence_type` leaves `enable` Unset.",
    "Recite the shots precedence: PUB slot, `run(shots=)`, twirling randomizations, then `options.default_shots`.",
    "Hand `run()` a LIST of PUBs, each at most `(circuit, parameter_values, shots)`, with measurements in the circuit.",
    "Derive the result shape: the parameter array's last axis binds parameters, the leading axes survive.",
    "Read outcomes from the register-named `BitArray`; `data.meas` exists only after `measure_all()`.",
    "Read every bitstring right-to-left — qubit 0 is the last character.",
    "Build readout correction yourself, by post-selection or assignment-matrix inversion; the Sampler ships none."
  ],
  "provenance": {
    "generated": "2026-08-15",
    "model": "claude-opus (v2 study rewrite agent)",
    "fact_checked": true,
    "notes": "v2 claim-preserving rewrite of fact-checked v1 (generated 2026-07-26, claude-opus-5 s4/s5 quick-study generation agent). No new factual claims: every sentence, table cell and code line restates content already present in the v1 primers or facts of this file, condensed. Every kept fact was re-read against its v1 wording before being shortened and keeps its original source unchanged; no fact was merged across two different sources. Structural changes only: the three long v1 primers became five teaching units (s5o1 the options tree + setting options and the shots chain; s5o2 the suppression/mitigation split and the two DIY correction schemes; s5o3 PUBs and broadcasting + reading a SamplerPubResult), with the SamplerOptions groups, the shots precedence chain, the noise-management surface, the post-selection/assignment-matrix comparison, the parameter-shape rule and the BitArray access paths moved into pipe tables. Facts 33 -> 19, triaged into core/trap. Deleted because a primer or its table now carries the claim in full: the documented shots precedence (guides/sampler-options, cited by both s5o1 primers), the Unset/attribute/update() setting rules (guides/runtime-options-overview, cited by the second s5o1 primer), Sampler's two-technique noise surface (guides/sampler-noise-management) and TREX/ZNE/PEC as Estimator resilience options (guides/error-mitigation-and-suppression-techniques), both cited by the s5o2 primer, the duplicate s5-q014 ValidationError fact that v1 carried under s5o2 as well as s5o1 (kept once, under s5o1), the Sampler-vs-Estimator split and the PUB definition (guides/primitives and guides/primitive-input-output, cited by the s5o3 primers), the three broadcasting shape results s5-q016 / s5-q017 / s5-q018 (now the parameter-shape table), and s5-q023 pooled get_counts, s5-q039 len(result), s5-q024 register-named DataBin fields and s5-q036 get_int_counts (now rows of the access-path table). fact_checked stays true: the v1 file was fact-checked against stored proof artifacts and fetched documentation (v1 note preserved below), and this pass introduced no claim that was not already in it. Original v1 provenance note: \"Every proof-sourced fact was checked against the stored evidence in data/proofs/<qid>.json and only EXECUTED questions with an artifact are cited (the conceptual s5 qids q021, q030, q034, q037 and q038 are covered through their guides instead). Every citation-sourced fact was confirmed by fetching the page text (sampler-options: the four-step shots precedence chain and the full options table; runtime-options-overview: Unset defaults, attribute vs update() setting; sampler-noise-management: DD + Pauli twirling are the only supported techniques; error-mitigation-and-suppression-techniques: TREX/ZNE/PEC as Estimator resilience options; primitive-input-output: Sampler PUB is at most three entries and must include measurements, DataBin holds one BitArray per ClassicalRegister, BitArray post-processing API; primitives: Sampler samples / Estimator estimates and supersedes backend.run; simulate-with-qiskit-sdk-primitives: the reference primitives still accept abstract instructions; tutorials/multi-product-formula: 'sampling error shrinks with 1/sqrt(N_shots)'). Primer claims not covered by a proof were re-executed on the pinned stack (qiskit 2.5.0 / aer 0.17.2 / runtime 0.48.0): SamplerOptions dataclass fields are max_execution_time/environment/simulator/default_shots/dynamical_decoupling/execution/twirling/experimental; SamplerV2.run signature is run(pubs, *, shots=None); options.update(default_shots=512) works; set_options is an AttributeError; a broadcast (4,) BitArray reports num_shots=500 per set with pooled get_counts totalling 2000; a DataBin with two registers has keys ['alice','bob'] and shape (); StatevectorSampler runs an abstract measure_all circuit and defaults to 1024 shots, matching the fake-backend local default (vs the Runtime service's 4096). Consistent with the REVIEW_LEDGER s5 pool wave and the SamplerV2-has-no-resilience_level entry; no contradictions. Task #28 figure wave (2026-07-26) appended two facts sourced from the new figure questions of this section; both were checked against the stored proof evidence.\""
  }
}
