{
  "section": "s6",
  "version": 2,
  "primers": [
    {
      "objective": "s6o1",
      "title": "The resilience_level dial: what 0, 1 and 2 buy",
      "body_md": "`resilience_level` is a single coarse dial: it abstracts away the detailed choice of method so you can reason about the cost/accuracy trade instead. The validator accepts only 0, 1 and 2, though a string `\"2\"` or `2.0` is silently coerced.\n\n| Level | What it applies | Cost |\n| --- | --- | --- |\n| 0 | no mitigation at all | none |\n| 1 (the default) | readout mitigation via Twirled Readout Error eXtinction (TREX): measurement twirling plus an inversion of the diagonalized readout-error transfer matrix | minimal |\n| 2 | everything level 1 does, PLUS zero-noise extrapolation and gate twirling | medium; typically reduces bias, but \"not guaranteed to be zero-bias\" |\n\nThere is no level 3, and no level turns on PEC — that is only the individual option `resilience.pec_mitigation`. The trade never reverses: a higher level post-processes a larger ensemble of related circuits, buying more accurate, less biased expectation values at the cost of longer processing time and more QPU usage. It never makes a job cheaper, faster or shallower, and no level changes which physical qubits you land on.\n\nOptions you set yourself are applied IN ADDITION to the level's base set. Level 0 turns `zne_mitigation` off, yet an explicit `resilience.zne_mitigation = True` re-enables ZNE. A level is a starting point, never a lock.\n\nKeep the two families straight:\n\n| Family | When it acts | Examples | How you switch it on |\n| --- | --- | --- | --- |\n| error suppression | before or during execution, to stop errors happening | dynamical decoupling pulses idling qubits so their idle-time errors approximately cancel | `options.dynamical_decoupling.enable` |\n| error mitigation | after execution, post-processing an ensemble of circuits to remove bias from expectation values | TREX, ZNE, PEA, PEC | `resilience_level` or `resilience.*_mitigation` |\n| neither | — | a higher transpiler optimization level; more shots, which shrink the STATISTICAL error, not the systematic bias | — |",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/guides/estimator-noise-management",
        "https://quantum.cloud.ibm.com/docs/guides/error-mitigation-and-suppression-techniques"
      ]
    },
    {
      "objective": "s6o1",
      "title": "The EstimatorOptions tree and its defaults",
      "body_md": "`EstimatorOptions` is a nested dataclass, and most option questions are really questions about nesting DEPTH.\n\n| Level | Fields |\n| --- | --- |\n| top | `max_execution_time`, `environment`, `simulator`, `default_precision`, `default_shots`, `resilience_level`, `seed_estimator`, `dynamical_decoupling`, `resilience`, `execution`, `twirling`, `experimental` |\n| `resilience` | `measure_mitigation`, `measure_noise_learning`, `zne_mitigation`, `zne`, `pec_mitigation`, `pec`, `layer_noise_learning`, `layer_noise_model` |\n| `resilience.zne` | `amplifier`, `noise_factors`, `extrapolator`, `extrapolated_noise_factors` |\n\nThree shapes of mistake follow: the WRONG NAME (`zne.factors`, the Mitiq-flavoured `zne.scale_factors`), the right name at the WRONG LEVEL, and — nastiest — CONFIGURING WITHOUT ENABLING. Setting `resilience.zne.noise_factors` is accepted but leaves `zne_mitigation` at `Unset`, so the job still runs without ZNE; worse, `resilience.zne_mitigation.enable = True` raises nothing at all — `Unset` is a plain singleton, so the write lands harmlessly on it. Only `resilience.zne_mitigation = True` (or `resilience_level = 2`) turns ZNE on; TREX is `resilience.measure_mitigation = True` and PEC is `resilience.pec_mitigation = True`.\n\nMemorize the documented defaults, since \"what if I touch nothing\" is a question shape.\n\n| Option | Default |\n| --- | --- |\n| `resilience_level` | 1 |\n| `resilience.measure_mitigation` | True |\n| `resilience.zne_mitigation`, `resilience.pec_mitigation` | False |\n| `resilience.zne.noise_factors` | (1, 3, 5) |\n| `resilience.zne.amplifier` | `gate_folding` |\n| `resilience.zne.extrapolator` | (exponential, linear) |\n| `dynamical_decoupling.enable`, `sequence_type` | False, `XX` |\n| `twirling.enable_gates`, `twirling.enable_measure` | False, True |\n| `default_precision` | 0.015625 — exactly 1/sqrt(4096) |\n| `max_execution_time` | 10800 seconds |\n\nAn option you never touch sits at `Unset`, meaning the server default applies. Set values three ways: an `EstimatorOptions` instance or nested dict at construction, assignment down the attribute path, or a bulk `options.update(...)`.",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/guides/estimator-options",
        "https://quantum.cloud.ibm.com/docs/guides/runtime-options-overview"
      ]
    },
    {
      "objective": "s6o2",
      "title": "The Estimator PUB: four positional slots",
      "body_md": "An Estimator PUB is a tuple of AT MOST FOUR values. The slots are positional, never searched by content, and even a single task must be wrapped in a list.\n\n| Slot | Contents | Note |\n| --- | --- | --- |\n| 1 | one `QuantumCircuit` | may contain `Parameter` objects |\n| 2 | an array of one or more observables | anything `ObservablesArrayLike`: `Pauli`, `SparsePauliOp`, `PauliList` or a bare string |\n| 3 | parameter values to bind | its LAST index runs over the circuit's parameters; omit it or pass `None` when the circuit has none |\n| 4 | a target precision (optional) | a float dropped into slot 3 gives `ValueError: Length of () inconsistent with last dimension of [0.01]` |\n\n```python\nresult = est.run([(isa, isa_obs, params, 0.01)])   # not est.run((isa, isa_obs))\n```\n\nThe output shape is decided entirely by NumPy broadcasting between the observables array and the parameter-value array, and the Estimator returns one expectation value per element of the broadcast shape.\n\n| Observables | Parameter values | `evs.shape` |\n| --- | --- | --- |\n| one observable | none | `()` |\n| flat list `[o1, o2, o3]` | none | `(3,)` |\n| column `[[o1], [o2], [o3]]` | none | `(3, 1)` |\n| row `[[o1, o2, o3]]` | none | `(1, 3)` |\n| one observable | `(4, 1)` | `(4,)` |\n| a `(4, 1)` column | `(1, 6)` | `(4, 6)` |\n\nA `SparsePauliOp` counts as a SINGLE element however many Pauli terms it holds: it returns one number, the coefficient-weighted sum — never a per-term breakdown. Commuting observables share one measurement only when grouped in the SAME PUB; two PUBs mean two measurement bases.\n\nBack comes a `PrimitiveResult` with one `PubResult` per PUB. `result[0].data` is a `DataBin` whose keys are exactly `evs` and `stds`, both real `float64`; `metadata` carries only run bookkeeping (`target_precision`, `circuit_metadata`).",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/guides/primitive-input-output",
        "https://quantum.cloud.ibm.com/docs/guides/get-started-with-estimator"
      ]
    },
    {
      "objective": "s6o2",
      "title": "Observables, layout alignment, and precision",
      "body_md": "An n-qubit observable must be written as a weighted sum of tensor products of Paulis — that is what `SparsePauliOp` stores. Only I and Z Paulis are diagonal in the measurement basis; X and Y terms need a basis change (H for X, S-dagger then H for Y), which the Estimator performs automatically.\n\n| Constructor | Takes | Watch for |\n| --- | --- | --- |\n| `SparsePauliOp(['ZZ', 'XX'], coeffs=[1.0, 0.5])` | labels plus `coeffs`, width inferred from the labels | there is no `num_qubits=` keyword here |\n| `SparsePauliOp.from_list([('ZZ', 1.0), ('XX', 0.5)])` | (label, coefficient) pairs on FULL-WIDTH labels | mixing widths raises `ValueError: The Nth Pauli is defined over k qubits` |\n| `SparsePauliOp.from_sparse_list([('Z', [0], 1.0)], num_qubits=3)` | (label, qubit_indices, coefficient) triples plus `num_qubits` | `num_qubits` is REQUIRED here, and the result is padded with identities to `'IIZ'` |\n\nTranspiling pads the circuit out to the backend's full width and may permute your qubits. The observable does not follow along, so align it: `isa_observable = observable.apply_layout(isa_circuit.layout)`. Nothing else rescues it — `measure_all()`, a string observable, or pinning `initial_layout=[0, 1]` all leave the circuit at full backend width.\n\n`precision` is a target STANDARD ERROR on the expectation value, not a shot count, which is why it is a float. Statistical error falls as 1/sqrt(N), so the shot cost goes as 1/precision^2.\n\n| Rank | Where the precision comes from |\n| --- | --- |\n| 1 | a precision inside the PUB |\n| 2 | `run(precision=...)` |\n| 3 | twirling being on by default, `num_randomizations` × `shots_per_randomization` |\n| 4 | `default_shots` |\n| 5 | `default_precision`, documented as 0.015625 = 1/sqrt(4096) |\n\nTwo guardrails: the result is not GUARANTEED to reach the requested precision, and lower precision means more QPU time.",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/guides/specify-observables-pauli",
        "https://quantum.cloud.ibm.com/docs/guides/estimator-options",
        "https://quantum.cloud.ibm.com/docs/guides/primitive-input-output"
      ]
    }
  ],
  "facts": [
    {
      "objective": "s6o1",
      "group": "core",
      "fact_md": "ZNE runs the circuit at several amplified noise rates (digital gate folding) and extrapolates back to the zero-noise limit — but it \"is not guaranteed to produce an unbiased result\".",
      "source": {
        "type": "citation",
        "ref": "https://quantum.cloud.ibm.com/docs/guides/error-mitigation-and-suppression-techniques"
      }
    },
    {
      "objective": "s6o1",
      "group": "core",
      "fact_md": "PEC writes the ideal circuit as a quasi-probability combination of noisy ones: unbiased, but with a sampling overhead scaling quadratically in gamma = sum of |quasi-probabilities|, which itself grows exponentially with circuit depth.",
      "source": {
        "type": "citation",
        "ref": "https://quantum.cloud.ibm.com/docs/guides/error-mitigation-and-suppression-techniques"
      }
    },
    {
      "objective": "s6o1",
      "group": "core",
      "fact_md": "PEC is documented as incompatible with gate-folding ZNE, with PEA and with fractional gates — enabling `pec_mitigation` and `zne_mitigation` together raises a validation error.",
      "source": {
        "type": "citation",
        "ref": "https://quantum.cloud.ibm.com/docs/guides/estimator-options"
      }
    },
    {
      "objective": "s6o1",
      "group": "core",
      "fact_md": "Error-bar length is bought with `default_precision`: 0.1 yields `stds` around 0.04–0.10 on a fake backend, 0.01 around 0.002–0.009. `options.execution.shots` is no route to it — `execution` holds only `init_qubits` and `rep_delay`.",
      "source": {
        "type": "proof",
        "ref": "s6-q041"
      }
    },
    {
      "objective": "s6o1",
      "group": "trap",
      "fact_md": "`resilience_level` accepts only 0, 1 and 2 — `3`, `4`, `5`, `-1` and `1.5` all raise a pydantic `ValidationError` about the 0..2 range.",
      "source": {
        "type": "proof",
        "ref": "s6-q014"
      }
    },
    {
      "objective": "s6o1",
      "group": "trap",
      "fact_md": "The ZNE scale factors live at `options.resilience.zne.noise_factors`; `zne.factors` (wrong name) and `resilience.noise_factors` (right name, wrong level) are both `no_such_attribute` errors.",
      "source": {
        "type": "proof",
        "ref": "s6-q020"
      }
    },
    {
      "objective": "s6o1",
      "group": "trap",
      "fact_md": "`default_precision` must be strictly positive: `0` raises `ValidationError` whether assigned or passed to `EstimatorOptions(default_precision=0)` — the constructor is not an escape hatch.",
      "source": {
        "type": "proof",
        "ref": "s6-q031"
      }
    },
    {
      "objective": "s6o1",
      "group": "trap",
      "fact_md": "`EstimatorV2.run()`'s only execution keyword is `precision=`; `shots=`, `default_precision=` and `resilience_level=` all raise `TypeError: unexpected keyword argument`, and `options.precision` is a `ValidationError`.",
      "source": {
        "type": "proof",
        "ref": "s6-q016"
      }
    },
    {
      "objective": "s6o2",
      "group": "core",
      "fact_md": "Width mismatch is the signature Estimator bug: a 2-qubit observable against a 5-qubit ISA circuit raises `ValueError: The number of qubits of the circuit (5) does not match the number of qubits of the ()-th observable (2)` — only `obs.apply_layout(isa.layout)` fixes it.",
      "source": {
        "type": "proof",
        "ref": "s6-q013"
      }
    },
    {
      "objective": "s6o2",
      "group": "core",
      "fact_md": "`SparsePauliOp('ZZ').apply_layout([2, 0], num_qubits=4)` returns `'IZIZ'` — label characters pair with the index list left-to-right while the printed label stays little-endian.",
      "source": {
        "type": "proof",
        "ref": "s6-q033"
      }
    },
    {
      "objective": "s6o2",
      "group": "core",
      "fact_md": "A multi-term `SparsePauliOp` is ONE observable: `from_list([('X', 3.0)])` on |+⟩ returns a 0-d `float64` array holding 3.0 — never a length-1 array, never one value per Pauli term.",
      "source": {
        "type": "proof",
        "ref": "s6-q022"
      }
    },
    {
      "objective": "s6o2",
      "group": "core",
      "fact_md": "Halving the target precision quadruples the shot budget: the measured standard error falls from 0.0195 at 2500 shots to 0.0104 at 4x the shots, while 2x only reaches 0.0137.",
      "source": {
        "type": "proof",
        "ref": "s6-q017"
      }
    },
    {
      "objective": "s6o2",
      "group": "trap",
      "fact_md": "`result[0].data.evs` holds the estimate(s) and `result[0].data.stds` their standard errors; `data.expectation_values` is an `AttributeError` and `metadata['evs']` a `KeyError`.",
      "source": {
        "type": "proof",
        "ref": "s6-q010"
      }
    },
    {
      "objective": "s6o2",
      "group": "trap",
      "fact_md": "Even a single task must be wrapped in a list: `est.run([(isa, iobs)])`. A bare circuit raises `ValueError: An invalid Estimator pub-like was given`, and the V1 habit `run(circuits=..., observables=...)` raises `TypeError`.",
      "source": {
        "type": "proof",
        "ref": "s6-q012"
      }
    },
    {
      "objective": "s6o2",
      "group": "trap",
      "fact_md": "The reference `StatevectorEstimator` reports `stds` of exactly `0.0` — an exact calculation has no sampling uncertainty — even when `run()` asks for a tighter precision.",
      "source": {
        "type": "proof",
        "ref": "s6-q037"
      }
    },
    {
      "objective": "s6o2",
      "group": "trap",
      "fact_md": "Sweeping `ry(theta)` over 0 to 2π against `SparsePauliOp('Z')` traces ONE full cosine period; cos(theta/2) gives half a period and cos(2*theta) two — the period tells the three apart.",
      "source": {
        "type": "proof",
        "ref": "s6-q040"
      }
    }
  ],
  "checklist": [
    "Recite the level table: 0 no mitigation, 1 the default with TREX, 2 adds ZNE and gate twirling.",
    "Expect a higher resilience level to cost more processing time and QPU usage, never less.",
    "Remember an option you set is applied in addition to the level's base set, overriding it.",
    "Put each option at its depth — `resilience.zne.noise_factors` configures ZNE, `resilience.zne_mitigation = True` enables it.",
    "Order an Estimator PUB positionally: circuit, observables, parameter values, precision — wrapped in a list.",
    "Predict `evs.shape` by broadcasting observables against parameter values; a `SparsePauliOp` counts as one element.",
    "Map every observable through `obs.apply_layout(isa.layout)` before running on a transpiled circuit.",
    "Treat `precision` as a target standard error: halving it quadruples the shots."
  ],
  "provenance": {
    "generated": "2026-08-15",
    "model": "claude-opus (v2 study rewrite agent)",
    "fact_checked": true,
    "notes": "v2 claim-preserving rewrite of fact-checked v1 (generated 2026-07-26, claude-opus-5 s6/s7 quick-study generation agent). No new factual claims: every sentence, table cell and code line restates content already present in the v1 primers or facts of this file, condensed. Every kept fact was re-read against its v1 wording before being shortened and keeps its original source unchanged; no fact was merged across two different sources. Structural changes only: the four long v1 primers became four teaching units of the same coverage (s6o1 the resilience dial + the options tree and its defaults; s6o2 the four-slot PUB and broadcasting + observables, layout alignment and precision), with the 0/1/2 resilience table, the suppression/mitigation/neither split, the EstimatorOptions nesting levels, the documented defaults, the PUB slots, the broadcasting shapes, the three SparsePauliOp constructors and the precision precedence chain moved into pipe tables. Facts 34 -> 18, triaged into core/trap. Deleted because a primer or its table now carries the claim in full: the 0/1/2 level table, the in-addition-to override callout and the higher-levels-cost-more rule (guides/estimator-noise-management, cited by the first s6o1 primer), the individual TREX/ZNE/PEC/DD switches (guides/error-mitigation-and-suppression-techniques, cited by both s6o1 primers), the documented defaults list and the Unset/attribute-path/update() rules (guides/estimator-options, cited by the second s6o1 primer and by the s6o2 precision primer), s6-q025 positional PUB slots and the third-slot float error, s6-q035 from_sparse_list identity padding, s6-q024 the from_list build and its width/TypeError traps, s6-q030 the flat-list (3,) shape, s6-q011 coefficient contraction (the one-number rule is stated in the PUB primer and s6-q022 keeps the proof), s6-q015 and s6-q023 (their shapes are rows of the broadcasting table, their little-endian and cosine readings are kept by s6-q033 and s6-q040), the PUB definition, the one-ev-per-broadcast-element rule, the SparsePauliOp-is-one-element rule and the commuting-observables-share-a-PUB rule (guides/primitive-input-output, cited by both s6o2 primers), the precision precedence chain (guides/estimator-options) and the I/Z-diagonal basis-change note (guides/specify-observables-pauli, cited by the s6o2 observables primer). The s6-q041 fact was narrowed to the stds-versus-default_precision measurement plus the options.execution.shots near miss, since s6-q016 already carries the options.precision near miss. fact_checked stays true: the v1 file was fact-checked against stored proof artifacts and fetched documentation (v1 note preserved below), and this pass introduced no claim that was not already in it. Original v1 provenance note: \"Every proof-sourced fact was checked against the stored evidence in data/proofs/<qid>.json and cites only EXECUTED questions with an artifact (the conceptual s6 qids q018, q019, q021, q026, q028, q029, q032, q034, q036, q038 and q039 are covered through their guides instead). Every citation-sourced fact was confirmed by fetching the page text: estimator-noise-management (the 0/1/2 resilience table with TREX at level 1 and 'Level 1 + ZNE and gate twirling' at level 2; the 'Important' override callout; higher levels = more accurate, longer processing); error-mitigation-and-suppression-techniques (ZNE = noise amplification by digital gate folding + extrapolation, 'not guaranteed to produce an unbiased result'; PEC unbiased with overhead quadratic in gamma, exponential in depth; measure_mitigation/zne_mitigation/pec_mitigation/dynamical_decoupling.enable as the individual switches; DD pulses idle qubits); estimator-options (full defaults table, run() takes only precision, five-step precision precedence, default_precision 0.015625 = 1/sqrt(4096), PEC incompatible with gate-folding ZNE and PEA, Unset semantics and update()); primitive-input-output (Estimator PUB is at most four values, one ev per broadcast element, SparsePauliOp counts as one element, commuting observables must share a PUB); specify-observables-pauli (only I/Z Paulis are diagonal, H and S-dagger/H basis changes performed automatically by the Estimator). Primer claims not covered by a proof were re-executed on the pinned stack (qiskit 2.5.0 / aer 0.17.2 / runtime 0.48.0, PYTHONPATH=pipeline/harness): EstimatorOptions.__dataclass_fields__ = _VERSION, max_execution_time, environment, simulator, default_precision, default_shots, resilience_level, seed_estimator, dynamical_decoupling, resilience, execution, twirling, experimental; resilience fields = measure_mitigation, measure_noise_learning, zne_mitigation, zne, pec_mitigation, pec, layer_noise_learning, layer_noise_model; zne fields = amplifier, noise_factors, extrapolator, extrapolated_noise_factors; EstimatorV2.run signature is run(pubs, *, precision=None); broadcasting measured as flat list -> (3,), column -> (3, 1), row -> (1, 3), single op -> (); a two-term SparsePauliOp returns a 0-d float64; PubResult.metadata keys are target_precision and circuit_metadata; DataBin keys are evs and stds; and enabling pec_mitigation together with zne_mitigation raises ValidationError ('cannot be simultaneously enabled'), matching the docs' incompatibility table. Consistent with the REVIEW_LEDGER s6 pool wave; no contradictions. Task #28 figure wave (2026-07-26) appended two facts sourced from the new figure questions of this section; both were checked against the stored proof evidence.\"\"\""
  }
}
