{
  "section": "s7",
  "version": 2,
  "primers": [
    {
      "objective": "s7o1",
      "title": "Result anatomy: PrimitiveResult → PubResult → DataBin → BitArray",
      "body_md": "Every V2 job returns the same container chain, so most retrieval questions are really \"at which layer does this attribute live?\".\n\n| Layer | What it is | Reach in with | Does NOT have |\n| --- | --- | --- | --- |\n| `PrimitiveResult` | list-like, one entry per PUB; `len(result)` is the PUB count | `result[0]`, `result[1]` | `.data`, `.get_counts()`, `.quasi_dists`, `.results` |\n| `PubResult` (Sampler: `SamplerPubResult`) | one PUB's output: `.data` plus `.metadata` (`shots` or `target_precision`, `circuit_metadata`) | `.data` | `get_counts()`, `evs` |\n| `DataBin` | Estimator: `evs` and `stds`. Sampler: one `BitArray` per `ClassicalRegister`, keyed by the register NAME | `.meas`, `db['meas']`, `list(db.keys())`, `values()`, `items()`, `shape`, `ndim`, `size` | `get_counts()`, `registers`, `num_bits` |\n| `BitArray` | the shot data of one register | `get_counts()`, `get_bitstrings()`, `get_int_counts()` | `.name` — the name exists only as the DataBin key |\n\n```python\ncounts = result[0].data.meas.get_counts()    # Sampler\nevs = result[0].data.evs                     # Estimator\nplot_histogram([result[0].data.meas.get_counts(),\n                result[1].data.meas.get_counts()], legend=[\"a\", \"b\"])\n```\n\n`evs` and `stds` are real `float64` arrays whose shape follows the PUB's broadcast shape, and `stds` reports the standard error actually achieved. `plot_histogram` needs one counts dict per legend entry.\n\nThe Sampler DataBin holds one `BitArray` per `ClassicalRegister`, so splitting one register into `alpha` and `beta` gives `data.alpha` and `data.beta` rather than a single flat array.\n\nA `BitArray` stores the sampled bitstrings as BYTES, shots on the left axis. Its surface is small enough to memorize, and the docs recommend post-processing on the array rather than on slow dictionaries.\n\n| Member | What it gives you |\n| --- | --- |\n| `num_shots`, `num_bits` | shots executed; CLASSICAL register width |\n| `array` | bit-PACKED `uint8`, shape `(num_shots, ceil(num_bits/8))` |\n| `get_counts()` | bitstring to integer tally, summing to `num_shots` |\n| `get_bitstrings()` | the per-shot list, length `num_shots` |\n| `get_int_counts()` | the same tallies keyed by the bitstring read as a binary INTEGER |\n| `postselect(indices, selection)` | only the shots whose listed bits match |\n| `slice_bits(indices)`, `slice_shots(indices)` | fewer bits, all shots; all bits, fewer shots — valid bit indices run 0..num_bits-1 |\n| `BitArray.concatenate_shots([a, b])`, `concatenate_bits` | pools shot sets of equal width; widens them instead |\n| `expectation_values(observable)` | diagonal expectation values straight from the shots |",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/guides/primitive-input-output",
        "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.primitives.DataBin",
        "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.primitives.PrimitiveResult",
        "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.primitives.BitArray"
      ]
    },
    {
      "objective": "s7o1",
      "title": "Getting results back after the kernel dies",
      "body_md": "Results do not live in your Python process. IBM Quantum automatically stores the results of EVERY job server-side, so dropping the `job` variable or restarting the kernel cancels nothing and loses nothing. The one handle worth keeping is the ID string from `job.job_id()`.\n\n| Goal | Call |\n| --- | --- |\n| one job back, by ID | `service.job(job_id)` returns the `RuntimeJobV2`; `.result()` gives the stored `PrimitiveResult` |\n| find jobs when the ID is lost | `service.jobs(...)`, the filtered LIST call, or the Workloads page |\n| keep a result on disk | `json.dump(result, f, cls=RuntimeEncoder)` |\n| read it back as a `PrimitiveResult` | `json.load(f, cls=RuntimeDecoder)` |\n\nNote the singular. Wrong spellings to recognise instantly: `service.result(...)`, `service.get_result(...)`, `service.retrieve_job(...)` (the V1 provider idiom), and calling `QiskitRuntimeService.job(...)` on the class rather than on an instance. Resubmitting the circuit is not retrieval: Runtime does not deduplicate, so an identical PUB becomes a new job with a new ID and freshly sampled shots.\n\n| Mistake on the disk round trip | What you get |\n| --- | --- |\n| no encoder on dump | `TypeError: Object of type PrimitiveResult is not JSON serializable` |\n| `RuntimeDecoder` where the encoder belongs | `TypeError` |\n| plain `json.load` on an encoded file | nested dicts that look fine, then `KeyError: 0` when you index the pub |\n| `json.dumps(result, f, cls=RuntimeEncoder)` | positional-argument `TypeError`: `dumps`/`loads` are the STRING variants, and everything after the first argument is keyword-only |\n| `result.to_json()` | no such method |",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/guides/save-jobs",
        "https://quantum.cloud.ibm.com/docs/guides/monitor-job"
      ]
    },
    {
      "objective": "s7o2",
      "title": "The job handle: one string, several predicates, one blocking call",
      "body_md": "`sampler.run(...)` and `estimator.run(...)` return a `RuntimeJobV2` immediately — the job itself executes server-side. Everything you do afterwards is either a cheap non-blocking query or the one blocking call.\n\n`job.status()` returns a PLAIN STRING, typed in the API reference as `Literal['INITIALIZING', 'QUEUED', 'RUNNING', 'CANCELLED', 'DONE', 'ERROR']`, which is why official examples write `if j.status() == \"DONE\"`. It is not a `JobStatus` enum member, and it carries no queue information — queue position is a `metrics()` or Workloads concern.\n\n| Call | Blocks | What it does |\n| --- | --- | --- |\n| `status()` | no | returns the state string |\n| `done()`, `errored()`, `cancelled()`, `running()` | no | booleans built on that string; `done()` is literally `status() == \"DONE\"` |\n| `in_final_state()` | no | true for `JOB_FINAL_STATES = ('DONE', 'CANCELLED', 'ERROR')` — CANCELLED is terminal |\n| `result()` | YES | waits for a terminal state, then returns the `PrimitiveResult`; never partial shots |\n| `wait_for_final_state(timeout, poll_interval)` | yes | polls to a terminal state without fetching data |\n| `cancel()` | no | asks the service to drop the job; `status()` then reads `\"CANCELLED\"` |\n| `job_id()`, `backend()`, `error_message()`, `logs()`, `metrics()`, `usage()`, `usage_estimation` | no | bookkeeping |\n\nCancelling is a state change, not a deletion, and a cancelled job hands back no partial data. There is no `job.stop()` and no `QiskitRuntimeService.cancel()`: the service FINDS jobs while the job object owns the lifecycle. Deleting your local variable leaves the job queued and still consuming your instance's time. One removal to recognise — `qiskit.tools.job_monitor` disappeared with Qiskit 1.0, so modern monitoring is `status()` in a loop, `wait_for_final_state()`, or the Workloads page.",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/api/qiskit-ibm-runtime/runtime-job-v2",
        "https://quantum.cloud.ibm.com/docs/guides/monitor-job"
      ]
    },
    {
      "objective": "s7o2",
      "title": "Watching a session, and the platform-side view",
      "body_md": "`session.details()` returns a DICTIONARY describing the SESSION, not its jobs.\n\n| Key | Meaning |\n| --- | --- |\n| `id`, `backend_name`, `mode` | which session, on what backend, in which mode |\n| `state` | one of open, active, inactive, closed |\n| `accepting_jobs` | whether new jobs are still taken |\n| `max_time` | maximum session length in seconds, subject to plan limits |\n| `interactive_timeout`, `active_timeout` | idle gap allowed between jobs; how long it may stay active |\n| `usage_time` | the QPU-committed time |\n| `started_at`, `activated_at`, `last_job_started`, `last_job_completed`, `closed_at` | lifecycle timestamps |\n\nNothing in there is per-job: a queue position belongs to a job and results to `job.result()`. In local testing mode — a `Session` built on a fake backend — `details()` returns `None`, and `session_id` is `None` too.\n\nThe two lifecycle verbs are examined on their difference. `close()` stops the session ACCEPTING new jobs while queued and running jobs finish, and the session terminates once nothing is pending; `cancel()` cancels all PENDING jobs in the session. The rest of the surface is small and shared with `Batch`: `backend()`, `status()`, `usage()`, `from_id(session_id, service)` and the `service`/`session_id` attributes. Neither class exposes a public `run` — you run through a primitive constructed with `mode=session`.\n\nSeveral official answers point at the browser instead.\n\n| Page | What it shows |\n| --- | --- |\n| Workloads | every workload you submitted, with its Status column and job ID; cancel from the row's overflow menu or a workload's Actions dropdown |\n| Instances | total plan time used and remaining |\n| Analytics | job, batch and session counts, for accounts you own or manage |",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/api/qiskit-ibm-runtime/session",
        "https://quantum.cloud.ibm.com/docs/guides/monitor-job"
      ]
    }
  ],
  "facts": [
    {
      "objective": "s7o1",
      "group": "core",
      "fact_md": "`num_bits` follows the CLASSICAL register, not the circuit: a 5-qubit circuit measuring three qubits reports `num_bits == 3`, while `len(get_bitstrings())` and `sum(get_counts().values())` both equal `num_shots`.",
      "source": {
        "type": "proof",
        "ref": "s7-q019"
      }
    },
    {
      "objective": "s7o1",
      "group": "core",
      "fact_md": "`bits.array` is bit-PACKED `uint8` with shape `(num_shots, ceil(num_bits/8))`: a 10-qubit circuit sampled 1024 times gives `(1024, 2)` — not `(1024, 10)`, `(1024, 8)` or the transpose.",
      "source": {
        "type": "proof",
        "ref": "s7-q012"
      }
    },
    {
      "objective": "s7o1",
      "group": "core",
      "fact_md": "Counts keys are little-endian bitstrings: X on qubit 0 of a 3-qubit circuit sampled 500 times gives `{'001': 500}` — integer tallies, not `{'100': 500}` and not fractions.",
      "source": {
        "type": "proof",
        "ref": "s7-q014"
      }
    },
    {
      "objective": "s7o1",
      "group": "core",
      "fact_md": "`bits.expectation_values('ZZ')` computes a diagonal expectation value straight from the shots — an ideal Bell sample returns the 0-d scalar 1.0, one value per Pauli string, with no Estimator involved.",
      "source": {
        "type": "proof",
        "ref": "s7-q025"
      }
    },
    {
      "objective": "s7o1",
      "group": "core",
      "fact_md": "A `(4, 1)` parameter array against a single observable gives `evs.shape == (4,)` — the trailing parameter axis is consumed, so it is not `(4, 1)` and not `(1, 4)`.",
      "source": {
        "type": "proof",
        "ref": "s7-q021"
      }
    },
    {
      "objective": "s7o1",
      "group": "core",
      "fact_md": "`pub.data.stds` is the standard error the primitive ACHIEVED, not the precision you asked for and not `1/sqrt(shots)`: on a noisy Bell/ZZ pub it read 0.0096 while both of those were 0.02.",
      "source": {
        "type": "proof",
        "ref": "s7-q013"
      }
    },
    {
      "objective": "s7o1",
      "group": "trap",
      "fact_md": "The missing PUB index is the classic retrieval bug: `result.data...` raises `AttributeError: 'PrimitiveResult' object has no attribute 'data'` — you must index the PUB first, as `result[0].data.meas.get_counts()`.",
      "source": {
        "type": "proof",
        "ref": "s7-q024"
      }
    },
    {
      "objective": "s7o1",
      "group": "trap",
      "fact_md": "`meas` is only the DEFAULT register name: a circuit measuring into a register called `readout` needs `result[0].data.readout.get_counts()`, and `data.meas` raises `AttributeError`.",
      "source": {
        "type": "proof",
        "ref": "s7-q032"
      }
    },
    {
      "objective": "s7o1",
      "group": "trap",
      "fact_md": "`bits.postselect([1], [0])` returns a new BitArray of only the shots where qubit 1 read 0, at FULL width; `postselect` has no `num_bits=` parameter (`TypeError`), and chaining `.slice_bits(...)` afterwards silently narrows the result.",
      "source": {
        "type": "proof",
        "ref": "s7-q016"
      }
    },
    {
      "objective": "s7o1",
      "group": "trap",
      "fact_md": "`BitArray.concatenate_shots([a, b])` pools two equal-width shot sets (200 + 200 → 400 shots, 3 bits), but `a + b` is a `TypeError` and `from_counts(a.get_counts() | b.get_counts())` LOSES shots, because dict union overwrites duplicate keys instead of summing them.",
      "source": {
        "type": "proof",
        "ref": "s7-q018"
      }
    },
    {
      "objective": "s7o1",
      "group": "trap",
      "fact_md": "`result[0].data.stds` is a silent trap when you meant `evs`: it exists, it has the same shape, and it plots — but on a `default_precision = 0.02` sweep the standard errors are about 0.005–0.018 while the expectation values span ±0.9, so the curve collapses onto the axis.",
      "source": {
        "type": "proof",
        "ref": "s7-q038"
      }
    },
    {
      "objective": "s7o2",
      "group": "core",
      "fact_md": "Cancellation is a job-handle operation: `job.cancel()` in Qiskit, or the Cancel action on the Workloads page; `cancel()` raises `RuntimeInvalidStateError` if the job is in a state that cannot be cancelled.",
      "source": {
        "type": "citation",
        "ref": "https://quantum.cloud.ibm.com/docs/api/qiskit-ibm-runtime/runtime-job-v2"
      }
    },
    {
      "objective": "s7o2",
      "group": "core",
      "fact_md": "`service.jobs()` is the listing call: `limit` defaults to 10 and it filters on `backend_name`, `pending`, `session_id`, `created_after`/`created_before` (datetime objects) and more; use it when you did not record a job ID.",
      "source": {
        "type": "citation",
        "ref": "https://quantum.cloud.ibm.com/docs/guides/monitor-job"
      }
    },
    {
      "objective": "s7o2",
      "group": "core",
      "fact_md": "`details()[\"state\"]` and `session.status()` use different vocabularies: `open`/`active`/`inactive`/`closed` for the field versus `Pending`, `In progress, accepting new jobs`, `In progress, not accepting new jobs` and `Closed` for the call.",
      "source": {
        "type": "citation",
        "ref": "https://quantum.cloud.ibm.com/docs/api/qiskit-ibm-runtime/session"
      }
    },
    {
      "objective": "s7o2",
      "group": "trap",
      "fact_md": "Executed against the class: `status()` returns a plain `str`, so `status() == JobStatus.DONE` is False and `status().name` raises `AttributeError`. `done()` is True only for `DONE`, while `in_final_state()` also covers `CANCELLED` and `ERROR` — and there is no `FAILED` state at all.",
      "source": {
        "type": "proof",
        "ref": "s7-q034"
      }
    }
  ],
  "checklist": [
    "Index the PUB first: `result[0].data.<register>.get_counts()` — `PrimitiveResult` itself has no `.data`.",
    "Ask `list(db.keys())` for the register name instead of assuming the DataBin field is called `meas`.",
    "Read `bits.array` as packed bytes: dtype `uint8`, shape `(num_shots, ceil(num_bits/8))`.",
    "Pick the post-processing method by axis: `postselect` and `slice_shots` drop shots, `slice_bits` drops bits.",
    "Save `job.job_id()`, then re-hydrate a stored result with `service.job(job_id).result()` after a restart.",
    "Round-trip a result through disk with `RuntimeEncoder` on dump and `RuntimeDecoder` on load.",
    "Compare `job.status()` against the plain string `\"DONE\"`, never against a `JobStatus` enum member.",
    "Separate the session verbs: `close()` stops new jobs being accepted, `cancel()` kills the pending ones."
  ],
  "provenance": {
    "generated": "2026-08-15",
    "model": "claude-opus (v2 study rewrite agent)",
    "fact_checked": true,
    "notes": "v2 claim-preserving rewrite of fact-checked v1 (generated 2026-07-26, claude-opus-5 s6/s7 quick-study generation agent). No new factual claims: every sentence, table cell and code line restates content already present in the v1 primers or facts of this file, condensed. Every kept fact was re-read against its v1 wording before being shortened and keeps its original source unchanged; no fact was merged across two different sources. Structural changes only: the five v1 primers became four teaching units (s7o1: the container chain plus the BitArray surface, and result retrieval/persistence; s7o2: the job handle, and sessions plus the platform pages), with the PrimitiveResult/PubResult/DataBin/BitArray layer table, the BitArray member table, the retrieval and disk-round-trip tables, the job-method blocking table, the session.details() key table and the Workloads/Instances/Analytics table carrying the detail. Facts 34 -> 15, triaged into core/trap. Deleted because a primer or its table now carries the claim in full: s7-q023 (len(result) and result[1] indexing, and the PrimitiveResult attribute errors), s7-q031 (DataBin keys/values/items and the missing get_counts/registers/num_bits, plus BitArray having no .name), s7-q022 (the result[0].data.evs path and the PubResult.evs / DataBin.get_counts near misses), s7-q020 (evs as a 0-d array, now the 'shape follows the broadcast shape' sentence plus s7-q021 and s7-q025 which both keep a 0-d proof), s7-q011 (num_shots/num_bits, kept by s7-q019 and the member table), s7-q015 (postselect on GHZ, kept by s7-q016), s7-q017 (slice_bits index range, now a table row), s7-q029 (get_int_counts, now a table row), s7-q010 (the RuntimeEncoder/RuntimeDecoder round trip and its TypeErrors, now the retrieval primer's two tables), s7-q036 (container class names, now the layer table's PrimitiveResult / SamplerPubResult rows; the BasePrimitiveJob interface listing was dropped as edge-case trivia), s7-q037 (plot_histogram overlay, now the code fence plus the one-counts-dict-per-legend-entry sentence), and the citation facts for automatic server-side storage + job_id() + service.job(id).result() (guides/save-jobs, cited by the retrieval primer), one BitArray per ClassicalRegister with the alpha/beta split (guides/primitive-input-output, now a sentence of the anatomy primer, which cites that guide), JOB_FINAL_STATES and the boolean predicates and wait_for_final_state (api/qiskit-ibm-runtime/runtime-job-v2, cited by the job primer), job.result() being the blocking call (guides/monitor-job, cited by the job primer), the status() Literal type and the j.status() == \"DONE\" idiom (same page, now the job primer's second paragraph, with s7-q034 keeping the enum trap), the session.details() key list (api/qiskit-ibm-runtime/session, cited by the session primer), close() versus cancel() (same page, now the session primer's lifecycle paragraph) and the Workloads/Instances/Analytics description (guides/monitor-job, now the platform table). fact_checked stays true: the v1 file was fact-checked against stored proof artifacts and fetched documentation (v1 note preserved below), and this pass introduced no claim that was not already in it. Original v1 provenance note: \"Every proof-sourced fact was checked against the stored evidence in data/proofs/<qid>.json and cites only EXECUTED questions with an artifact. Objective s7o2 has NO executed questions in the bank (q026, q027, q028, q030 and q033 are all proof.status conceptual), so every s7o2 fact is citation-sourced by design — no conceptual qid is dressed up with a proof badge. Every citation-sourced fact was confirmed by fetching the page text: save-jobs ('IBM Quantum automatically stores results from every job', job_id() + Workloads page, QiskitRuntimeService.jobs with created_after, service.job(job_id).result(), and the json.dump/RuntimeEncoder + json.load/RuntimeDecoder round trip, including the literal `j.status() == \"DONE\"` comparison); monitor-job (job.result() is 'a blocking call until the job completes', job.job_id(), job.status(), job = service.job(<job_id>), 'use job.cancel() to cancel a job', service.jobs() default limit 10 and its deprecated-provider note, plus the Workloads/Instances/Analytics pages); api/qiskit-ibm-runtime/runtime-job-v2 (status() return type Literal['INITIALIZING','QUEUED','RUNNING','CANCELLED','DONE','ERROR'], JOB_FINAL_STATES = ('DONE','CANCELLED','ERROR'), done/errored/cancelled/running/in_final_state/wait_for_final_state, cancel() raising RuntimeInvalidStateError); api/qiskit-ibm-runtime/session (details() key list including state/accepting_jobs/max_time/last_job_completed/usage_time, close() vs cancel() semantics); primitive-input-output (PrimitiveResult contains one PubResult per PUB, the Estimator DataBin holds evs/stds, the Sampler DataBin holds one BitArray per ClassicalRegister with 'meas' as the default name, BitArray stores shots as bytes with shots on the left axis, and the postselect/slice_bits/slice_shots/expectation_values/concatenate_bits post-processing list); api/qiskit/qiskit.primitives.BitArray (full method surface). Primer claims not covered by a proof were re-executed on the pinned stack (qiskit 2.5.0 / aer 0.17.2 / runtime 0.48.0, PYTHONPATH=pipeline/harness): BitArray's public surface is array/bitcount/concatenate/concatenate_bits/concatenate_shots/expectation_values/from_bool_array/from_counts/from_samples/get_bitstrings/get_counts/get_int_counts/ndim/num_bits/num_shots/postselect/reshape/shape/size/slice_bits/slice_shots/to_bool_array/transpose; slice_shots(range(5)).num_shots == 5; concatenate_shots doubles shots while concatenate_bits doubles bits; DataBin exposes keys/values/items/shape/ndim/size and db['meas'] works; a named register gives keys ['readout'] and data.meas raises AttributeError; PubResult.metadata is {'shots', 'circuit_metadata'} for the Sampler; the RuntimeEncoder/RuntimeDecoder round trip reproduces identical counts while a plain json.load yields a dict whose [0] is a KeyError; RuntimeJobV2.done() is literally status() == 'DONE' and cancel() sets the status string to 'CANCELLED'; QiskitRuntimeService.jobs signature is (limit=10, skip=0, backend_name, pending, program_id, instance, job_tags, session_id, created_after, created_before, descending=True). Consistent with the REVIEW_LEDGER s7 pool wave and the session.details()-is-None-locally entry; no contradictions. | official-alignment fix wave (2026-07-26): three facts added. The note above that s7o2 has NO executed questions is now OUT OF DATE — s7-q034 is executed (a RuntimeJobV2 subclass pins the state string, which is legitimate because _set_status_and_error_message short-circuits on terminal states, so status()/done()/errored()/cancelled()/in_final_state() run unmodified with no service), and it sources the new s7o2 job-state fact. The details()-vs-status() fact stays citation-sourced (api/qiskit-ibm-runtime/session, re-fetched 2026-07-26: the details() key list plus status()'s Pending / In progress, accepting new jobs / In progress, not accepting new jobs / Closed / None). The container-naming fact is sourced by the new executed s7-q036 (PrimitiveResult, SamplerPubResult with MRO SamplerPubResult->PubResult, PrimitiveJob is an instance of BasePrimitiveJob whose abstract methods are cancel/cancelled/done/in_final_state/result/running/status). Task #28 figure wave (2026-07-26) appended two facts sourced from the new figure questions of this section; both were checked against the stored proof evidence.\""
  }
}
