{
  "section": "s8",
  "version": 2,
  "primers": [
    {
      "objective": "s8o1",
      "title": "OpenQASM 3 is a typed language",
      "body_md": "OpenQASM 2 knew only two aggregate declarations, `qreg` and `creg`. OpenQASM 3 replaces them with a real type system: quantum registers become `qubit[n] name;`, classical registers become `bit[n] name;`, and a family of classical types joins them.\n\n| Type | Notes |\n| --- | --- |\n| `bool`, `int[n]`, `uint[n]`, `float[n]` | the C-like scalars, each sized by its designator |\n| `complex[float[n]]` | the designator is a FLOAT type, never a bare bit width: `complex[float[64]] c;` |\n| `bit[n]`, `angle[n]`, `duration`, `stretch` | the four \"special\" types, the ones with no C equivalent |\n| not in the language | `string`, `char`, `real`, `unsigned` |\n\nTwo things matter for the exam. First, what Qiskit EMITS: circuits export their registers as `bit[n]`/`qubit[n]`, loose clbits as bare `bit`. Second, what Qiskit can PARSE BACK: the importer supports only a subset of the type system, and the official feature table is the authority on which types survive the round trip.",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/guides/qasm-feature-table",
        "https://quantum.cloud.ibm.com/docs/api/qiskit/qasm3",
        "https://openqasm.com/versions/3.0/language/types.html"
      ]
    },
    {
      "objective": "s8o2",
      "title": "Reading v2 and v3 programs side by side",
      "body_md": "The same circuit reads differently in the two versions, and four tells answer every \"which version is this?\" question.\n\n| Feature | OpenQASM 2 | OpenQASM 3 |\n| --- | --- | --- |\n| header | `OPENQASM 2.0;` | `OPENQASM 3.0;` |\n| standard include | `include \"qelib1.inc\";` | `include \"stdgates.inc\";` |\n| registers | `qreg q[2]; creg c[2];` | `qubit[2] q; bit[2] c;` |\n| measurement | `measure q[0] -> c[0];` (arrow) | `c[0] = measure q[0];` (assignment) |\n| conditionals | whole-register equality only: `if (c == 1) ...` | real `if`/`else`, loops, per-bit conditions |\n| global phase | not expressible | `gphase` |\n\nControl flow is the deepest semantic difference, and exporters enforce these language limits at export time: OpenQASM 2 cannot express a per-bit `if_test` condition or a global phase.",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/guides/interoperate-qiskit-qasm2",
        "https://quantum.cloud.ibm.com/docs/guides/interoperate-qiskit-qasm3"
      ]
    },
    {
      "objective": "s8o3",
      "title": "The four functions, and the one asymmetry that matters",
      "body_md": "Both `qiskit.qasm2` and `qiskit.qasm3` expose the same quartet, and the argument tells the functions apart.\n\n| Function | Direction | `qiskit.qasm2` | `qiskit.qasm3` |\n| --- | --- | --- | --- |\n| `dumps(qc)` | circuit to string | native | native |\n| `dump(qc, stream)` | circuit to an OPEN text stream | native | native |\n| `loads(s)` | string to circuit | native | needs `qiskit-qasm3-import` |\n| `load(path)` | filesystem PATH to circuit | native | needs `qiskit-qasm3-import` |\n\nThe one asymmetry to memorize: OpenQASM 3 IMPORT is not part of Qiskit itself, it needs the separate `qiskit-qasm3-import` package, while OpenQASM 3 export and all of `qasm2` ship with Qiskit. Mixing up `dump` and `load` arguments — a path where a stream belongs, or the reverse — raises immediately.",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/api/qiskit/qasm3",
        "https://quantum.cloud.ibm.com/docs/api/qiskit/qasm2"
      ]
    },
    {
      "objective": "s8o4",
      "title": "Runtime over plain HTTPS",
      "body_md": "Everything the primitives do through Python can be done over plain HTTPS. Authentication is two-step: exchange your IBM Cloud API key for an IAM bearer token, then send that token plus a `Service-CRN` header identifying your instance on every Runtime call. Submitting a primitive job is one request, and results are a sub-resource of the job.\n\n| Request | What it does |\n| --- | --- |\n| `POST /api/v1/jobs` | submit a primitive job |\n| `GET /api/v1/jobs/{id}` | job details |\n| `GET /api/v1/jobs/{id}/results` | the final result |\n| `GET /api/v1/jobs/{id}/logs`, `/metrics` | diagnostics |\n| `POST /api/v1/jobs/{id}/cancel` | cancel the job |\n| `DELETE /api/v1/jobs/{id}` | remove a job already in a terminal state |",
      "citations": [
        "https://quantum.cloud.ibm.com/docs/guides/cloud-setup-rest-api",
        "https://quantum.cloud.ibm.com/docs/guides/sampler-rest-api",
        "https://quantum.cloud.ibm.com/docs/api/qiskit-runtime-rest/tags/jobs"
      ]
    }
  ],
  "facts": [
    {
      "objective": "s8o1",
      "group": "core",
      "fact_md": "Qiskit exports classical registers as `bit[n] c;` even at width 1; loose `Clbit`s become bare `bit _bit0;`.",
      "source": {
        "type": "proof",
        "ref": "s8-q010"
      }
    },
    {
      "objective": "s8o1",
      "group": "core",
      "fact_md": "An unbound `Parameter` exports to OpenQASM 3 as an `input float[64]` declaration — the circuit becomes a parameterized program.",
      "source": {
        "type": "proof",
        "ref": "s8-q020"
      }
    },
    {
      "objective": "s8o1",
      "group": "core",
      "fact_md": "Casting rules: in a binary operation the LESSER type is promoted (`complex` > `float` > `int`/`uint`, wider beats narrower); `bool` and scalar `bit` are interchangeable as values; `bit[n]` converts to `int[m]`/`uint[m]`/`angle[m]` only when `m == n`; nothing casts to or from `duration`; and a width designator must be a `const` expression.",
      "source": {
        "type": "citation",
        "ref": "https://openqasm.com/versions/3.0/language/types.html"
      }
    },
    {
      "objective": "s8o1",
      "group": "trap",
      "fact_md": "The feature table's Qiskit column documents `bit` as supported (mapped to `Clbit`/`ClassicalRegister`) while `duration` and `stretch` declarations are unparseable.",
      "source": {
        "type": "citation",
        "ref": "https://quantum.cloud.ibm.com/docs/guides/qasm-feature-table"
      }
    },
    {
      "objective": "s8o2",
      "group": "core",
      "fact_md": "OpenQASM 2 conditionals accept only whole-register equality; exporting a per-clbit `if_test` raises `QASM2ExportError` ('OpenQASM 2 only supports register-equality conditions').",
      "source": {
        "type": "proof",
        "ref": "s8-q013"
      }
    },
    {
      "objective": "s8o2",
      "group": "core",
      "fact_md": "Custom gates round-trip as `gate mygate ... { ... }` declarations in both versions — neither exporter inlines them.",
      "source": {
        "type": "proof",
        "ref": "s8-q019"
      }
    },
    {
      "objective": "s8o2",
      "group": "trap",
      "fact_md": "`qasm2.dumps` SILENTLY drops a circuit's global phase — no error, no warning — while `qasm3.dumps` emits `gphase`.",
      "source": {
        "type": "proof",
        "ref": "s8-q017"
      }
    },
    {
      "objective": "s8o3",
      "group": "core",
      "fact_md": "`QuantumCircuit.from_qasm_str`/`from_qasm_file` still exist in Qiskit 2.x for OpenQASM 2 input; the instance method `.qasm()` was removed.",
      "source": {
        "type": "proof",
        "ref": "s8-q012"
      }
    },
    {
      "objective": "s8o3",
      "group": "core",
      "fact_md": "`QuantumCircuit.from_qasm_str` keeps the program's own declarations: `qreg a[2]; creg m[2];` gives registers named `a` and `m`, never the default `q`/`c`, and `measure a -> m;` expands index by index — `a[0]` into `m[0]`, `a[1]` into `m[1]`.",
      "source": {
        "type": "proof",
        "ref": "s8-q029"
      }
    },
    {
      "objective": "s8o3",
      "group": "trap",
      "fact_md": "`qasm3.loads`/`load` raise `MissingOptionalLibraryError` unless `qiskit-qasm3-import` is installed; `qasm3.dumps`/`dump` and all of `qasm2` are native.",
      "source": {
        "type": "proof",
        "ref": "s8-q021"
      }
    },
    {
      "objective": "s8o3",
      "group": "trap",
      "fact_md": "`qasm2.loads` accepts only the `OPENQASM 2.0;` header — feeding it a v3 program raises `QASM2ParseError` immediately.",
      "source": {
        "type": "proof",
        "ref": "s8-q015"
      }
    },
    {
      "objective": "s8o4",
      "group": "core",
      "fact_md": "IAM token exchange: `POST` the IBM Cloud API key with the apikey grant type; the bearer token expires after 3600 seconds (`expires_in: 3600`).",
      "source": {
        "type": "citation",
        "ref": "https://quantum.cloud.ibm.com/docs/guides/cloud-setup-rest-api"
      }
    },
    {
      "objective": "s8o4",
      "group": "core",
      "fact_md": "Submitting a Sampler job over REST is `POST /v1/jobs` with `'program_id': 'sampler'` and your circuits inside `params.pubs`.",
      "source": {
        "type": "citation",
        "ref": "https://quantum.cloud.ibm.com/docs/guides/sampler-rest-api"
      }
    },
    {
      "objective": "s8o4",
      "group": "trap",
      "fact_md": "Every Runtime REST call after authentication carries two headers: the IAM bearer token and `Service-CRN` (the instance identifier — it is not itself a credential).",
      "source": {
        "type": "citation",
        "ref": "https://quantum.cloud.ibm.com/docs/guides/cloud-setup-rest-api"
      }
    }
  ],
  "checklist": [
    "Name the version from the header, the include file, the register keywords and the measure syntax.",
    "Declare OpenQASM 3 registers as `qubit[n]` and `bit[n]`; `qreg`/`creg` are OpenQASM 2 only.",
    "Give a `complex` declaration a float designator — `complex[float[64]] c;` — never a bare bit width.",
    "Promote the lesser type in a binary operation, and size a `bit[n]` cast to the same width.",
    "Remember only OpenQASM 3 IMPORT needs `qiskit-qasm3-import`; export and all of `qasm2` are native.",
    "Match each function to its argument: `dumps`/`loads` strings, `dump` an open stream, `load` a path.",
    "Expect OpenQASM 2 export to reject a per-bit condition and to drop a global phase silently.",
    "Send the IAM bearer token plus the `Service-CRN` header on every Runtime REST request."
  ],
  "provenance": {
    "generated": "2026-08-15",
    "model": "claude-opus (v2 study rewrite agent)",
    "fact_checked": true,
    "notes": "v2 claim-preserving rewrite of fact-checked v1 (generated 2026-07-26, claude-fable orchestrator gold example). No new factual claims: every sentence and table cell restates content already present in the v1 primers or facts of this file, condensed. Every kept fact was re-read against its v1 wording before being shortened and keeps its original source unchanged; no fact was merged across two different sources. Structural changes only: the four v1 primers stayed one per objective and gained the tables that carry the detail — the OpenQASM 3 type inventory, the v2-versus-v3 feature comparison, the dumps/dump/loads/load quartet with the qasm2/qasm3 native-versus-import columns, and the Runtime REST jobs endpoints. Facts 18 -> 14, triaged into core/trap. Deleted because a primer or its table now carries the claim in full: the type inventory fact (openqasm.com types page, now the s8o1 type table, and that URL moved into the primer's citations), s8-q014 (arrow versus assignment measurement, now a row of the comparison table), s8-q011 (dump takes an open stream while load takes a path, now the quartet table plus one sentence), and the Runtime REST endpoint fact (api/qiskit-runtime-rest/tags/jobs, now the s8o4 endpoint table, and that URL moved into the primer's citations). The global-phase fact was narrowed to the SILENT drop because the comparison table already states that OpenQASM 2 cannot express a global phase while OpenQASM 3 emits gphase. fact_checked stays true: the v1 file was fact-checked against stored proof artifacts and fetched documentation (v1 note preserved below), and this pass introduced no claim that was not already in it. Note carried forward: the openqasm.com URL was never machine-verified here (the site answers this environment's fetch tool with HTTP 403), and syllabus.json uses the unversioned https://openqasm.com/language/types.html spelling. Original v1 provenance note: \"Authored entirely from REVIEW_LEDGER-verified facts and citations already curl-verified during the s8 adversarial review; serves as the gold pattern for the s1-s7 study generation agents. | official-alignment fix wave (2026-07-26): three facts added for the two thin tasks. The two s8o1 facts (type inventory and casting rules) come from the OpenQASM 3.0 language specification's types page; its content was read from the spec source because openqasm.com answers this environment's fetch tool with HTTP 403 (a user-agent block, not a dead page) — the URL itself was NOT machine-verified here, and note that syllabus.json cites the unversioned form https://openqasm.com/language/types.html, which is the safer spelling if the link checker objects. The s8o4 endpoint fact comes from the live Runtime REST reference (api/qiskit-runtime-rest and its jobs tag page, both fetched 200 on 2026-07-26): jobs POST/GET, jobs/{id} GET+DELETE, jobs/{id}/cancel POST, jobs/{id}/logs GET, jobs/{id}/metrics GET, jobs/{id}/results GET, jobs/{id}/tags PUT. | figure wave 2 (task #28, 2026-07-27): 1 fact(s) added, each sourced by a NEW executed figure question (s8-q029); every claim was read back against the stored evidence in data/proofs/<qid>.json before being written and nothing is asserted beyond it.\""
  }
}
