{"section": {"id": "s3", "num": 3, "title": "Create quantum circuits", "weightPct": 18, "objectives": [{"id": "s3o1", "text": "Construct basic quantum circuits"}, {"id": "s3o2", "text": "Construct dynamic circuits"}, {"id": "s3o3", "text": "Construct parameterized circuits"}, {"id": "s3o4", "text": "Transpile and optimize circuits"}]}, "questions": [{"id": "s3-q010", "section": "s3", "objectives": ["s3o1"], "type": "predict-output", "difficulty": 1, "stem": "The circuit below flips only qubit 0 of a 3-qubit register and then measures every qubit with `measure_all()`. Running it on the default `AerSimulator`, which single bitstring is returned for every shot?", "code": "from qiskit import QuantumCircuit\n\nqc = QuantumCircuit(3)\nqc.x(0)\nqc.measure_all()", "options": [{"key": "A", "text": "`100`"}, {"key": "B", "text": "`010`"}, {"key": "C", "text": "`001`"}, {"key": "D", "text": "`111`"}, {"key": "E", "text": "`1`"}], "displayCount": 4, "answer": ["C"], "explanation": {"correct": "Qiskit prints bitstrings in little-endian order: the rightmost character is the classical bit holding qubit 0, and the index increases leftward. Only qubit 0 is excited, so the outcome is `001` (q2 q1 q0 = 0 0 1).", "distractors": {"A": "`100` treats the leftmost character as qubit 0 (big-endian). Qiskit is little-endian, so qubit 0 is the RIGHTMOST bit.", "B": "`010` is the outcome you would get if qubit 1 were flipped; the X acts on qubit 0.", "D": "`111` would require all three qubits to be excited; only qubit 0 has an X gate.", "E": "A 1-bit result is what you would get if `measure_all()` only measured the qubits that carry gates. It measures EVERY qubit in the circuit (that is `measure_active()`'s job), so the key is always 3 bits wide."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/construct-circuits", "https://quantum.cloud.ibm.com/docs/guides/measure-qubits"]}, "proof": {"status": "executed", "evidence": {"A": "option claims '100'; observed 0/1000 shots gave '100'; full counts={'001': 1000}", "B": "option claims '010'; observed 0/1000 shots gave '010'; full counts={'001': 1000}", "C": "option claims '001'; observed 1000/1000 shots gave '001'; full counts={'001': 1000}", "D": "option claims '111'; observed 0/1000 shots gave '111'; full counts={'001': 1000}", "E": "option claims '1'; observed 0/1000 shots gave '1'; full counts={'001': 1000}"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.66}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 1}}, {"id": "s3-q011", "section": "s3", "objectives": ["s3o1"], "type": "mcq", "difficulty": 1, "stem": "A 2-qubit circuit is created with `QuantumCircuit(2)` (no classical register). Which statement correctly describes what `qc.measure_all()` does to it?", "code": "from qiskit import QuantumCircuit\n\nqc = QuantumCircuit(2)\nqc.h(0)\nqc.cx(0, 1)\nqc.measure_all()", "options": [{"key": "A", "text": "It raises an error because the circuit has no classical bits to measure into."}, {"key": "B", "text": "By default it adds a fresh 2-bit register `meas`, inserts a barrier, then measures every qubit into it."}, {"key": "C", "text": "It adds 2 classical bits to an auto-created register named `c` and inserts no barrier before the measurements."}, {"key": "D", "text": "It measures just the qubits that already carry gates, adding a single classical bit."}, {"key": "E", "text": "It reuses the circuit's existing classical bits whenever there are enough of them, adding `meas` only as a fallback."}, {"key": "F", "text": "It creates the 2-bit `meas` register and measures every qubit, then appends the barrier after the measurements."}], "displayCount": 4, "answer": ["B"], "explanation": {"correct": "`measure_all()` creates a fresh `ClassicalRegister` named `meas` sized to the number of qubits, inserts a `barrier` across all qubits, and measures qubit i into `meas[i]`. It is the convenient one-call way to read out an entire circuit.", "distractors": {"A": "`measure_all()` never errors on a register-less circuit — it creates the classical bits for you (unlike `measure`, which needs existing bits).", "C": "The auto-created register is named `meas`, not `c`, and `measure_all()` does insert a barrier ahead of the measurements.", "D": "Measuring just the active qubits is what `measure_active()` does; `measure_all()` measures every qubit in the circuit.", "E": "Reusing existing bits is what `measure_all(add_bits=False)` does; the default is `add_bits=True`, which always appends a fresh `meas` register — even to a circuit that already has enough classical bits.", "F": "The register and the measurements are right, but the barrier goes BEFORE the measurements: it separates the state preparation from the readout so nothing is optimized across that boundary."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/construct-circuits", "https://quantum.cloud.ibm.com/docs/guides/measure-qubits"]}, "proof": {"status": "executed", "evidence": {"A": "cregs=['meas'], num_clbits=2, barrier=True, ops={'measure': 2, 'h': 1, 'cx': 1, 'barrier': 1}: measure_all did not raise -> claim(error) is False", "B": "cregs=['meas'], num_clbits=2, barrier=True, ops={'measure': 2, 'h': 1, 'cx': 1, 'barrier': 1}: register 'meas' size2 + barrier + 2 measures -> True", "C": "cregs=['meas'], num_clbits=2, barrier=True, ops={'measure': 2, 'h': 1, 'cx': 1, 'barrier': 1}: register is 'meas' not 'c' and barrier present -> False", "D": "cregs=['meas'], num_clbits=2, barrier=True, ops={'measure': 2, 'h': 1, 'cx': 1, 'barrier': 1}: all qubits measured, num_clbits=2 not 1 -> False", "E": "QuantumCircuit(2, 2).measure_all() -> num_clbits=4, cregs=['c', 'meas']: a fresh 'meas' register is appended even though 2 classical bits already existed -> reuse claim False", "F": "instruction order=['h', 'cx', 'barrier', 'measure', 'measure']: barrier sits at index 2, before the last measure at index 4 -> 'barrier after' False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.55}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q012", "section": "s3", "objectives": ["s3o1"], "type": "predict-output", "difficulty": 2, "stem": "`sub` (a 2-qubit circuit whose only gate is `cx(0, 1)`) is composed onto the 3-qubit circuit `qc` with the mapping `qubits=[0, 2]`. In the resulting circuit `comp`, which qubits does the CX act on?", "code": "from qiskit import QuantumCircuit\n\nqc = QuantumCircuit(3)\nqc.h(0)\n\nsub = QuantumCircuit(2)\nsub.cx(0, 1)\n\ncomp = qc.compose(sub, qubits=[0, 2])", "options": [{"key": "A", "text": "Control = qubit 0, target = qubit 1"}, {"key": "B", "text": "Control = qubit 1, target = qubit 2"}, {"key": "C", "text": "Control = qubit 0, target = qubit 2"}, {"key": "D", "text": "`compose` raises an error because `sub` has fewer qubits than `qc`"}, {"key": "E", "text": "Control = qubit 2, target = qubit 0"}, {"key": "F", "text": "`compose` raises an error because the `qubits` list needs to name adjacent qubits"}], "displayCount": 4, "answer": ["C"], "explanation": {"correct": "`compose(sub, qubits=[0, 2])` maps `sub`'s qubit 0 to `qc`'s qubit 0 and `sub`'s qubit 1 to `qc`'s qubit 2. The CX `sub.cx(0, 1)` therefore becomes a CX with control qubit 0 and target qubit 2 in `comp`.", "distractors": {"A": "Target qubit 1 ignores the `qubits=[0, 2]` mapping and assumes the sub-circuit lands on qubits 0 and 1.", "B": "This misreads the mapping as shifting both qubits up by one; the mapping sends sub-qubit 0 to physical qubit 0.", "D": "Composing a smaller circuit onto a subset of a larger one is exactly what the `qubits` argument is for — no error occurs.", "E": "This reads the `qubits` list right-to-left, as if the mapping were little-endian. The list is positional: entry 0 is `sub`'s qubit 0, entry 1 is `sub`'s qubit 1, so the control stays on qubit 0.", "F": "There is no adjacency requirement — `qubits` may name any subset in any order, and 0 and 2 are not neighbours here. Connectivity constraints come from the backend's coupling map at transpile time, not from `compose`."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/construct-circuits", "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.circuit.QuantumCircuit"]}, "proof": {"status": "executed", "evidence": {"A": "observed CX=(0,2); claim (0,1) -> False", "B": "observed CX=(0,2); claim (1,2) -> False", "C": "observed CX (control,target)=(0,2); claim (0,2) -> True", "D": "compose succeeded, comp ops={'h': 1, 'cx': 1}; no error -> False", "E": "observed CX (control,target)=(0,2); claim (2,0) reversed mapping -> False", "F": "compose onto the non-adjacent pair [0, 2] succeeded, comp ops={'h': 1, 'cx': 1}; claim 'must name adjacent qubits' -> False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.42}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 1}}, {"id": "s3-q013", "section": "s3", "objectives": ["s3o1"], "type": "spot-bug", "difficulty": 2, "stem": "A developer expects `base` to contain its H followed by the X from `add_on`, but after this code `base` still shows only the H gate. What is the bug and the correct fix?", "code": "from qiskit import QuantumCircuit\n\nbase = QuantumCircuit(1)\nbase.h(0)\n\nadd_on = QuantumCircuit(1)\nadd_on.x(0)\n\nbase.compose(add_on)   # intended to append X onto base\nprint(base.count_ops())", "options": [{"key": "A", "text": "`compose` cannot be used for single-qubit gates; `base.append` must be used instead."}, {"key": "B", "text": "By default `compose` returns a new circuit instead of mutating `base`; fix with `base = base.compose(add_on)` or `inplace=True`."}, {"key": "C", "text": "`add_on` has to be converted with `add_on.to_gate()` before it can be composed."}, {"key": "D", "text": "`base` needs a classical register before `compose` takes effect; without one the composed gates are typically dropped on the floor."}, {"key": "E", "text": "`compose` typically mutates the caller when both circuits have the same width; here `base` and `add_on` are both 1-qubit, so the X should be there."}, {"key": "F", "text": "`add_on` is usually invisible to `base` until its register is shared: call `base.add_register(add_on.qregs[0])` before the `compose` call."}], "displayCount": 5, "answer": ["B"], "explanation": {"correct": "By default `compose` is functional: it RETURNS a new circuit and leaves the caller untouched (`inplace=False`). Discarding the return value loses the X. Assign the result back, or pass `inplace=True` to mutate `base`.", "distractors": {"A": "`compose` works fine for single-qubit gates — the problem is only that the returned circuit was discarded; `append` is not required.", "C": "`to_gate()` is optional; you can compose whole circuits directly. It is unrelated to the in-place behavior.", "D": "Classical registers have nothing to do with composing gates, and nothing is silently dropped; the X is lost purely because the return value was ignored.", "E": "Width plays no role: `compose` is functional for every shape. Composing a 1-qubit circuit onto another 1-qubit circuit and discarding the result leaves `base` with just its H, exactly as here.", "F": "Sharing the register does not help — and `add_register` even refuses a register whose name is already taken. The X is lost purely because the returned circuit was thrown away."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/construct-circuits", "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.circuit.QuantumCircuit"]}, "proof": {"status": "executed", "evidence": {"A": "compose produced {'h', 'x'} without append -> 'must use append' is False", "B": "buggy base ops={'h'}; after reassign={'h', 'x'}; after inplace=True={'h', 'x'} -> True", "C": "compose succeeded without to_gate() (ops={'h', 'x'}) -> False", "D": "no classical register was added; ops={'h', 'x'} -> False", "E": "both circuits are 1 qubit wide, yet after a discarded compose base ops are {'h'} -> 'same width mutates the caller' is False", "F": "base.add_register(add_on.qregs[0]) -> CircuitError: 'register name \"q\" already exists'; base ops after the discarded compose are still {'h'} -> False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.39}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q014", "section": "s3", "objectives": ["s3o1"], "type": "mcq", "difficulty": 1, "stem": "You want a circuit with a 2-qubit register `q` and a 2-bit register `c`, both qubits in superposition, measuring `q[i]` into `c[i]`. Which snippet builds AND runs on `AerSimulator` without raising, producing 2-bit outcomes?", "code": null, "options": [{"key": "A", "text": "`qc=QuantumCircuit(2); qc.h([0,1]); qc.measure([0,1],[0,1])`"}, {"key": "B", "text": "`q=QuantumRegister(2,'q'); qc=QuantumCircuit(q); qc.h(q); qc.measure(q,q)`"}, {"key": "C", "text": "`q=QuantumRegister(2,'q'); c=ClassicalRegister(2,'c'); qc=QuantumCircuit(q,c); qc.h(q); qc.barrier(); qc.measure(c,q)`"}, {"key": "D", "text": "`q=QuantumRegister(2,'q'); c=ClassicalRegister(2,'c'); qc=QuantumCircuit(q,c); qc.h(q); qc.measure(q,c)`"}, {"key": "E", "text": "`q=QuantumRegister(2,'q'); c=ClassicalRegister(2,'c'); qc=QuantumCircuit(q,c); qc.h(q); qc.measure_active()`"}, {"key": "F", "text": "`q=QuantumRegister(2,'q'); c=ClassicalRegister(2,'c'); qc=QuantumCircuit(2,2); qc.add_register(q,c); qc.h(q); qc.measure(q,c)`"}], "displayCount": 5, "answer": ["D"], "explanation": {"correct": "You must declare classical bits before measuring into them. Option D creates both registers, passes them to `QuantumCircuit`, and calls `measure(qubit, clbit)` in the correct argument order — register-wide, so `q[i]` is read into `c[i]` — so it builds and runs, giving 2-bit outcomes.", "distractors": {"A": "`QuantumCircuit(2)` has NO classical bits, so `measure([0,1],[0,1])` has nowhere to store results and raises a `CircuitError`.", "B": "`measure(q, q)` passes qubits as the classical-bit argument; measurement targets must be classical bits, so this raises.", "C": "`measure(c, q)` swaps the argument order — `measure` expects `(qubit, clbit)`, not `(clbit, qubit)` — and raises; the extra `barrier()` changes nothing.", "E": "This builds and runs, but `measure_active()` appends its own `meas` register on top of `c` instead of writing into it, so the circuit ends up with 4 classical bits and two space-separated outcome registers.", "F": "`QuantumCircuit(2,2)` already owns registers named `q` and `c`, so `add_register` raises on the duplicate name. `add_register` adds new bits to a circuit; it never adopts bits that are already there."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/construct-circuits", "https://quantum.cloud.ibm.com/docs/guides/measure-qubits"]}, "proof": {"status": "executed", "evidence": {"A": "raised CircuitError: 'Index 0 out of range for size 0.'", "B": "raised CircuitError: \"Incorrect bit type: expected 'B' but got 'Qubit'\"", "C": "raised CircuitError: \"Incorrect bit type: expected 'B' but got 'Clbit'\"", "D": "built and ran; outcome bit-width=2, sample counts={'01': 54, '11': 42, '10': 48, '00': 56} -> valid=True", "E": "built and ran; outcome bit-width=5, sample counts={'01 00': 54, '11 00': 42, '10 00': 48, '00 00': 56} -> valid=False; measure_active() appended its own register on top of the declared one, so the outcome is not a 2-bit string", "F": "raised CircuitError: 'register name \"q\" already exists'; the circuit already owns registers named 'q' and 'c', so add_register hits a duplicate name"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.23}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q015", "section": "s3", "objectives": ["s3o1"], "type": "multi", "difficulty": 2, "stem": "Consider the circuit below. Select ALL statements that are TRUE about its structural properties. (Recall `width()` counts qubits + classical bits.)", "code": "from qiskit import QuantumCircuit, QuantumRegister, ClassicalRegister\n\nqr = QuantumRegister(3, 'q')\ncr = ClassicalRegister(2, 'c')\nqc = QuantumCircuit(qr, cr)\nqc.h(0)\nqc.cx(0, 1)\nqc.cx(1, 2)\nqc.measure(0, 0)\nqc.measure(2, 1)", "options": [{"key": "A", "text": "The circuit has 3 qubits and 2 classical bits."}, {"key": "B", "text": "`qc.width()` returns 3, since `width()` counts the qubits and not the classical bits."}, {"key": "C", "text": "`qc.size()` returns 5 (the total number of operations)."}, {"key": "D", "text": "`qc.depth()` equals `qc.size()`, since no two operations on distinct qubits overlap in this circuit."}, {"key": "E", "text": "`qc.num_qubits + qc.num_clbits` equals `qc.width()`."}, {"key": "F", "text": "`qc.size()` returns 3, because `size()` counts the gates in the circuit and skips the two `measure` instructions."}], "displayCount": 5, "answer": ["A", "C", "E"], "explanation": {"correct": "`num_qubits`=3 and `num_clbits`=2 (A). `size()` counts all 5 operations: 1 H + 2 CX + 2 measures (C). `width()` returns qubits + classical bits = 3 + 2 = 5, so it equals `num_qubits + num_clbits` (E).", "distractors": {"B": "`width()` returns qubits PLUS classical bits (5 here), not the qubit count. Confusing `width()` with `num_qubits` is a common slip.", "D": "`depth()` is the longest gate path (4 here), while `size()` is the total operation count (5); the H and the first CX do share a layer, so the two metrics differ.", "F": "`size()` counts every instruction that is not a directive: the two `measure` operations count, giving 5. Only directives such as `barrier` are skipped."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/construct-circuits", "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.circuit.QuantumCircuit"]}, "proof": {"status": "executed", "evidence": {"A": "num_qubits=3, num_clbits=2, width=5, size=5, depth=4: qubits=3 & clbits=2 -> True", "B": "num_qubits=3, num_clbits=2, width=5, size=5, depth=4: width()=5 (qubits+clbits), not 3 -> False", "C": "num_qubits=3, num_clbits=2, width=5, size=5, depth=4: size()=5 operations -> True", "D": "num_qubits=3, num_clbits=2, width=5, size=5, depth=4: depth=4 vs size=5 -> equal? False", "E": "num_qubits=3, num_clbits=2, width=5, size=5, depth=4: num_qubits+num_clbits=5 vs width=5 -> True", "F": "num_qubits=3, num_clbits=2, width=5, size=5, depth=4: size()=5 includes both measure instructions, so it is not 3 -> False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.28}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q016", "section": "s3", "objectives": ["s3o1"], "type": "predict-output", "difficulty": 2, "stem": "The circuit prepares a 3-qubit state. Which computational-basis states have non-zero amplitude in its statevector, and with what amplitude?", "code": "from qiskit import QuantumCircuit\nfrom qiskit.quantum_info import Statevector\n\nqc = QuantumCircuit(3)\nqc.h(0)\nqc.cx(0, 1)\nqc.cx(0, 2)\n\nsv = Statevector(qc)", "options": [{"key": "A", "text": "Only `|000⟩` and `|111⟩`, each with amplitude 1/√2 (a GHZ state)."}, {"key": "B", "text": "`|000⟩` and `|100⟩`, each with amplitude 1/√2."}, {"key": "C", "text": "All eight basis states, each with amplitude 1/√8."}, {"key": "D", "text": "`|000⟩` and `|011⟩`, each with amplitude 1/√2."}, {"key": "E", "text": "Only `|000⟩` and `|111⟩`, but with amplitude 1/2 each."}, {"key": "F", "text": "Only `|000⟩` and `|101⟩`, each with amplitude 1/√2."}], "displayCount": 5, "answer": ["A"], "explanation": {"correct": "H on qubit 0 makes (|0⟩+|1⟩)/√2. The two CX gates copy qubit 0's value onto qubits 1 and 2, so the state becomes (|000⟩ + |111⟩)/√2 — a 3-qubit GHZ state with only those two basis states populated.", "distractors": {"B": "`|100⟩` would appear only if the CX gates did nothing; they entangle qubits 1 and 2 with qubit 0, flipping them when qubit 0 is 1.", "C": "A uniform superposition over all 8 states needs an H on every qubit; here only qubit 0 gets an H and the CXs create correlation, not full superposition.", "D": "`|011⟩` misplaces the excitation — both CX gates are controlled by qubit 0, so all three qubits share the same value (000 or 111).", "E": "The populated basis states are right, but 1/2 is the PROBABILITY of each outcome, not the amplitude. Two equally weighted terms carry amplitude 1/√2, whose square 1/2 is the probability.", "F": "This assumes the second CX replaces the first, correlating only qubits 0 and 2 and leaving qubit 1 at 0. Both CX gates stay in the circuit, so qubit 1 is flipped as well."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/construct-circuits", "https://quantum.cloud.ibm.com/docs/api/qiskit/quantum_info"]}, "proof": {"status": "executed", "evidence": {"A": "nonzero basis states = {'000': (0.7071+0j), '111': (0.7071+0j)}; claim {000,111} at 1/sqrt2 -> True", "B": "nonzero = {'000': (0.7071+0j), '111': (0.7071+0j)}; claim {000,100} -> False", "C": "nonzero = {'000': (0.7071+0j), '111': (0.7071+0j)} (2 states); claim all-8-uniform -> False", "D": "nonzero = {'000': (0.7071+0j), '111': (0.7071+0j)}; claim {000,011} -> False", "E": "nonzero = {'000': (0.7071+0j), '111': (0.7071+0j)}; the amplitude on 000 is 0.7071 = 1/sqrt(2), while 1/2 is its probability -> claim 'amplitude 1/2' is False", "F": "nonzero = {'000': (0.7071+0j), '111': (0.7071+0j)}; claim {000,101} (qubit 1 left at 0) -> False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.26}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q017", "section": "s3", "objectives": ["s3o3"], "type": "predict-output", "difficulty": 2, "stem": "A list of values is passed to `assign_parameters`. What angles do the `rx` and `rz` gates end up with?", "code": "from qiskit import QuantumCircuit\nfrom qiskit.circuit import Parameter\n\ntheta = Parameter('theta')\nphi = Parameter('phi')\n\nqc = QuantumCircuit(1)\nqc.rx(theta, 0)   # added first\nqc.rz(phi, 0)     # added second\n\nbound = qc.assign_parameters([0.5, 1.5])", "options": [{"key": "A", "text": "`rx = 0.5`, `rz = 1.5` — the list is applied in the order the gates were added."}, {"key": "B", "text": "`rx = 0.5`, `rz = 0.5` — both gates receive the first value."}, {"key": "C", "text": "It raises an error: `assign_parameters` requires a dictionary, not a list."}, {"key": "D", "text": "`rx = 1.5`, `rz = 0.5` — the list is applied in alphabetical parameter order (`phi`, then `theta`)."}, {"key": "E", "text": "`bound` is `None` — `assign_parameters` binds in place by default, so `qc` carries the angles."}, {"key": "F", "text": "It raises a `TypeError`: binding from a list needs a `ParameterVector`, not separate `Parameter`s."}], "displayCount": 4, "answer": ["D"], "explanation": {"correct": "When you pass a plain list, values are bound to `qc.parameters`, which is sorted ALPHABETICALLY by name — `phi` before `theta`. So `phi`=0.5 and `theta`=1.5, giving `rz`(phi)=0.5 and `rx`(theta)=1.5. Insertion order is irrelevant.", "distractors": {"A": "This assumes the list follows the order gates were added. It actually follows `qc.parameters`, which is alphabetical (`phi` < `theta`).", "B": "Each parameter gets a distinct list entry; they are not both set to the first value.", "C": "A list is a valid argument — it binds positionally to the sorted parameters. A dict is only required for partial binding.", "E": "`assign_parameters` is functional by default: it RETURNS a bound copy and leaves `qc` parametric. Passing `inplace=True` is what mutates the caller — and that call is the one returning `None`.", "F": "Positional binding works for any parameters, however they were created: the sequence is matched against `qc.parameters`. A `ParameterVector` is only a convenience for creating many related parameters at once."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/construct-circuits", "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.circuit.Parameter"]}, "proof": {"status": "executed", "evidence": {"A": "qc.parameters order=['phi', 'theta']; observed rx=1.5, rz=0.5; claim rx0.5/rz1.5 -> False", "B": "observed rx=1.5, rz=0.5; claim both 0.5 -> False", "C": "assign_parameters([0.5,1.5]) succeeded (no error); claim 'needs dict' -> False", "D": "parameters sorted=['phi', 'theta']; observed rx=1.5, rz=0.5; claim rx1.5/rz0.5 -> True", "E": "assign_parameters returned a QuantumCircuit (not None) and qc keeps its parameters ['phi', 'theta'] -> 'binds in place, returns nothing' is False", "F": "a plain list bound two separately created Parameters without any ParameterVector (rx=1.5, rz=0.5) and raised nothing -> False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.23}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 1}}, {"id": "s3-q018", "section": "s3", "objectives": ["s3o3"], "type": "mcq", "difficulty": 1, "stem": "Three parameters are created and added to a circuit in the order `gamma`, `alpha`, `beta`. What does `[p.name for p in qc.parameters]` return?", "code": "from qiskit import QuantumCircuit\nfrom qiskit.circuit import Parameter\n\ng = Parameter('gamma'); a = Parameter('alpha'); b = Parameter('beta')\nqc = QuantumCircuit(1)\nqc.rz(g, 0)\nqc.rx(a, 0)\nqc.ry(b, 0)\n\nprint([p.name for p in qc.parameters])", "options": [{"key": "A", "text": "`['alpha', 'beta', 'gamma']` — sorted alphabetically by name."}, {"key": "B", "text": "`['gamma', 'alpha', 'beta']` — the order the parameters were added."}, {"key": "C", "text": "`['beta', 'alpha', 'gamma']` — reverse insertion order."}, {"key": "D", "text": "The order is unspecified and can vary between runs."}, {"key": "E", "text": "`['gamma', 'beta', 'alpha']` — reverse alphabetical, the order `ParameterView` sorts names in."}, {"key": "F", "text": "`qc.parameters` returns an unordered `ParameterView`, so iterating it in a comprehension raises a `TypeError`."}], "displayCount": 5, "answer": ["A"], "explanation": {"correct": "`QuantumCircuit.parameters` returns a `ParameterView` sorted alphabetically by parameter name, so it is `['alpha', 'beta', 'gamma']`. This deterministic ordering is exactly why passing a bare list to `assign_parameters` can surprise you.", "distractors": {"B": "Parameters are NOT kept in insertion order; they are sorted by name.", "C": "The ordering is alphabetical ascending, not reverse insertion order.", "D": "The order is fully deterministic (alphabetical), not run-dependent.", "E": "The sort is ascending, not descending: `alpha` comes first and `gamma` last. Reverse order would need `sorted(qc.parameters, reverse=True)`.", "F": "`ParameterView` is an ordered, iterable sequence — the comprehension runs fine and yields the three names in sorted order."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/construct-circuits", "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.circuit.Parameter"]}, "proof": {"status": "executed", "evidence": {"A": "observed order=['alpha', 'beta', 'gamma']; claim alphabetical -> True", "B": "observed order=['alpha', 'beta', 'gamma']; claim insertion order -> False", "C": "observed order=['alpha', 'beta', 'gamma']; claim reverse insertion -> False", "D": "observed order=['alpha', 'beta', 'gamma'] is deterministic across runs; claim 'unspecified' -> False", "E": "observed order=['alpha', 'beta', 'gamma']; claim reverse alphabetical -> False", "F": "iterating the ParameterView in a comprehension yielded 3 names without raising -> 'not iterable' is False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.28}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 1}}, {"id": "s3-q019", "section": "s3", "objectives": ["s3o3"], "type": "spot-bug", "difficulty": 2, "stem": "This code is meant to bind both parameters but raises a `ValueError`. What is the problem and the correct fix?", "code": "from qiskit import QuantumCircuit\nfrom qiskit.circuit import Parameter\n\na = Parameter('a'); b = Parameter('b')\nqc = QuantumCircuit(1)\nqc.rx(a, 0)\nqc.rz(b, 0)\n\nbound = qc.assign_parameters([0.7])   # intended to bind a and b", "options": [{"key": "A", "text": "`assign_parameters` accepts a dictionary here; a plain list is not a valid argument once a circuit has more than one parameter."}, {"key": "B", "text": "A list has to supply a value for each parameter; pass `[0.7, b_value]`, or use a dict like `{a: 0.7}` for partial binding."}, {"key": "C", "text": "Parameters have to be bound one at a time through `gate.assign()`."}, {"key": "D", "text": "The circuit is usually transpiled before any parameters can be assigned."}, {"key": "E", "text": "Positional binding needs the legacy API: switch to `qc.bind_parameters([0.7])`, which pads the parameters left unset."}, {"key": "F", "text": "`a` and `b` were created separately; group them as `ParameterVector('p', 2)` and one value then expands over the whole vector."}], "displayCount": 5, "answer": ["B"], "explanation": {"correct": "A list binds positionally to every entry of `qc.parameters`, so its length has to match the parameter count (2 here). Passing one value raises `ValueError`. Provide both values in a list, or pass a dict to bind a subset.", "distractors": {"A": "A list IS accepted when its length matches the parameter count; the error is the wrong length, not the list type.", "C": "There is no per-gate `gate.assign()` workflow; binding is done on the circuit via `assign_parameters`.", "D": "Parameter assignment is independent of transpilation; you can (and usually do) bind before transpiling.", "E": "`bind_parameters` was removed in Qiskit 1.0 — calling it raises `AttributeError`, and it never padded missing values either. `assign_parameters` is the only binder, and it still needs one value per parameter.", "F": "A `ParameterVector` is just a convenient way to create related parameters — its elements still bind one value each, so a single-element list raises the same `ValueError`."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/construct-circuits", "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.circuit.Parameter"]}, "proof": {"status": "executed", "evidence": {"A": "a length-2 list bound successfully (num_params after=0) -> 'a plain list is not a valid argument' is False", "B": "one value raised [ValueError: Mismatching number of values and parameters. For partial binding please pass a m]; [0.7,1.2] bound all (True); dict {a:0.7} left 1 free (True) -> True", "C": "binding is done via circuit.assign_parameters, not per-gate -> False", "D": "assignment worked with no transpilation -> False", "E": "qc.bind_parameters([0.7]) -> AttributeError: 'QuantumCircuit' object has no attribute 'bind_parameters'; the legacy binder is gone, so it cannot pad anything -> False", "F": "the same circuit built from ParameterVector('p', 2) with assign_parameters([0.7]) -> ValueError: Mismatching number of values and parameters. For partial binding pleas; one value still does not cover two parameters -> False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.27}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q020", "section": "s3", "objectives": ["s3o3"], "type": "mcq", "difficulty": 1, "stem": "After this binding step, which statement about `qc` and `bound` is correct?", "code": "from qiskit import QuantumCircuit\nfrom qiskit.circuit import Parameter\n\ntheta = Parameter('theta')\nqc = QuantumCircuit(1)\nqc.rx(theta, 0)\n\nbound = qc.assign_parameters({theta: 3.14})", "options": [{"key": "A", "text": "`qc` is now fully bound (0 free parameters), and `bound` is the same object as `qc`."}, {"key": "B", "text": "`assign_parameters` returns `None`; the bound circuit must be read back from `qc`."}, {"key": "C", "text": "By default `qc` still has 1 free parameter, and `bound` is a new circuit with 0 free parameters."}, {"key": "D", "text": "Both `qc` and `bound` have 0 free parameters, because `assign_parameters` mutates in place by default."}, {"key": "E", "text": "`qc` still has 1 free parameter and `bound` has 0, but `bound` shares `qc`'s instruction list, so editing one edits the other."}, {"key": "F", "text": "Both circuits keep their free parameter: `assign_parameters` typically records the values and substitutes them at transpile time."}], "displayCount": 5, "answer": ["C"], "explanation": {"correct": "`assign_parameters` is functional by default (`inplace=False`): it returns a NEW circuit with the values substituted and leaves the original untouched. So `qc` keeps its 1 free parameter while `bound` has 0.", "distractors": {"A": "`bound` is a distinct new circuit, not the same object as `qc`; and `qc` is not modified.", "B": "`assign_parameters` returns the bound circuit, not `None` — that is why you capture it in `bound`.", "D": "The default is `inplace=False`, so `qc` is NOT mutated; you would need `inplace=True` for that.", "E": "The free-parameter counts are right, but `bound` is an independent copy: appending a gate to it leaves `qc`'s instruction list untouched.", "F": "Substitution happens immediately — `bound` already carries the numeric angle and reports 0 free parameters, with no transpilation involved."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/construct-circuits", "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.circuit.Parameter"]}, "proof": {"status": "executed", "evidence": {"A": "qc.num_parameters=1, bound.num_parameters=0, bound is qc=False: bound is a separate object and qc keeps its parameter -> False", "B": "assign_parameters returned a circuit (is None=False) -> False", "C": "qc.num_parameters=1, bound.num_parameters=0, bound is qc=False: original unchanged, new circuit fully bound -> True", "D": "qc.num_parameters=1, bound.num_parameters=0, bound is qc=False: qc was NOT mutated (default inplace=False) -> False", "E": "appending a gate to a bound copy left qc at size 1 -> 'bound shares qc's instruction list' is False", "F": "qc.num_parameters=1, bound.num_parameters=0, bound is qc=False: bound already carries the numeric angle 3.14 with no transpilation -> 'substituted only at transpile time' is False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.25}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q021", "section": "s3", "objectives": ["s3o3"], "type": "predict-output", "difficulty": 2, "stem": "The gates use parameter EXPRESSIONS. After binding `t = π`, what numeric angles do `rx` and `rz` receive?", "code": "import numpy as np\nfrom qiskit import QuantumCircuit\nfrom qiskit.circuit import Parameter\n\nt = Parameter('t')\nqc = QuantumCircuit(1)\nqc.rx(2 * t, 0)\nqc.rz(t + np.pi / 2, 0)\n\nbound = qc.assign_parameters({t: np.pi})", "options": [{"key": "A", "text": "`rx = 2π ≈ 6.283`, `rz = 3π/2 ≈ 4.712`."}, {"key": "B", "text": "`rx = π ≈ 3.142`, `rz = π ≈ 3.142` — the expressions are ignored and `t` is substituted directly."}, {"key": "C", "text": "`rx = 2π ≈ 6.283`, `rz = π/2 ≈ 1.571` — just the coefficient is applied; the added constant is dropped."}, {"key": "D", "text": "It raises an error because gate angles must typically be plain `Parameter`s, not expressions."}, {"key": "E", "text": "`rx = π ≈ 3.142`, `rz = 3π/2 ≈ 4.712` — the multiplier on `t` is dropped while the added constant survives."}, {"key": "F", "text": "It raises a `TypeError`: adding `np.pi` to a `Parameter` first needs an explicit `ParameterExpression` wrapper."}], "displayCount": 4, "answer": ["A"], "explanation": {"correct": "Parameter expressions are evaluated at bind time. With `t = π`: `2*t = 2π ≈ 6.283`, and `t + π/2 = 3π/2 ≈ 4.712`. Qiskit fully supports arithmetic on `Parameter` objects inside gate arguments.", "distractors": {"B": "The coefficient and additive constant are part of the expression and ARE applied; `t` is not substituted raw.", "C": "The additive constant `π/2` is not dropped — `rz` gets `π + π/2 = 3π/2`.", "D": "Expressions like `2*t` and `t + π/2` are valid gate arguments; no error occurs.", "E": "The mirror image of dropping the constant: `2 * t` is a full expression too, so `rx` gets 2π, not π.", "F": "`Parameter` overloads the arithmetic operators, so `t + np.pi / 2` builds a `ParameterExpression` by itself — no explicit wrapper and no error."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/construct-circuits", "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.circuit.Parameter"]}, "proof": {"status": "executed", "evidence": {"A": "observed rx=6.2832, rz=4.7124; expect 2pi=6.2832, 3pi/2=4.7124 -> True", "B": "observed rx=6.2832, rz=4.7124; claim both=pi -> False", "C": "observed rz=4.7124; claim rz=pi/2=1.5708 (constant dropped) -> False", "D": "binding expressions succeeded (rx=6.2832, rz=4.7124); claim 'error' -> False", "E": "observed rx=6.283; the 2* multiplier IS applied (2pi=6.283), so the claim rx=pi=3.142 is False", "F": "t + np.pi/2 built a ParameterExpression with no explicit wrapper and bound to rz=4.712 -> claim TypeError is False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.26}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q022", "section": "s3", "objectives": ["s3o3"], "type": "multi", "difficulty": 2, "stem": "A circuit is built with a `ParameterVector`. Select ALL true statements.", "code": "from qiskit import QuantumCircuit\nfrom qiskit.circuit import ParameterVector\n\nx = ParameterVector('x', 3)\nqc = QuantumCircuit(3)\nfor i in range(3):\n    qc.ry(x[i], i)", "options": [{"key": "A", "text": "`qc.num_parameters` is 3."}, {"key": "B", "text": "The parameters are named `x[0]`, `x[1]`, `x[2]`."}, {"key": "C", "text": "`qc.assign_parameters({x: [0.1, 0.2, 0.3]})` binds all three parameters in one call."}, {"key": "D", "text": "`x[0]` and `x[1]` are the same `Parameter` object."}, {"key": "E", "text": "A `ParameterVector` can only be bound one element at a time, never as a whole."}, {"key": "F", "text": "Indexing past the end (`x[3]`) transparently grows the vector to four parameters."}], "displayCount": 5, "answer": ["A", "B", "C"], "explanation": {"correct": "A `ParameterVector('x', 3)` creates 3 distinct parameters named `x[0..2]` (A, B). Using the vector itself as a single dict key mapped to a list of values binds all three of them at once (C).", "distractors": {"D": "Each index is a distinct `Parameter`; `x[0]` and `x[1]` are different objects that bind to different values.", "E": "A `ParameterVector` can be bound in one call (`{x: [...]}`) OR element by element — not only element-by-element.", "F": "A `ParameterVector` has a fixed length until you call `resize()`; `x[3]` on a length-3 vector raises `IndexError`."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/construct-circuits", "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.circuit.ParameterVector"]}, "proof": {"status": "executed", "evidence": {"A": "num_parameters=3 -> True", "B": "parameter names=['x[0]', 'x[1]', 'x[2]'] -> True", "C": "assign_parameters({x:[...]}) left 0 free -> whole-vector bind works True", "D": "x[0] is x[1] -> False", "E": "whole-vector bind succeeded (free=0) so element-by-element is NOT the only way -> False", "F": "indexing past the declared length: IndexError: list index out of range; the vector still holds 3 parameters -> False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.25}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q023", "section": "s3", "objectives": ["s3o2"], "type": "predict-output", "difficulty": 2, "stem": "This dynamic circuit measures qubit 0 and, only if that result is 1, applies X to qubit 1; then it measures qubit 1. Over many shots (bit order `c1 c0`), which outcomes appear?", "code": "from qiskit import QuantumCircuit, QuantumRegister, ClassicalRegister\n\nqr = QuantumRegister(2, 'q')\ncr = ClassicalRegister(2, 'c')\nqc = QuantumCircuit(qr, cr)\nqc.h(0)\nqc.measure(0, 0)\nwith qc.if_test((cr[0], 1)):\n    qc.x(1)\nqc.measure(1, 1)", "options": [{"key": "A", "text": "Only `01` and `00`, roughly 50/50."}, {"key": "B", "text": "All four of `00`, `01`, `10`, `11`, roughly 25% each."}, {"key": "C", "text": "Only `10` and `11`, roughly 50/50."}, {"key": "D", "text": "Only `00` and `11`, roughly 50/50 each."}, {"key": "E", "text": "Only `01` and `10`, roughly 50/50 each."}, {"key": "F", "text": "`00` and `11`, but `11` in roughly a quarter of the shots."}], "displayCount": 4, "answer": ["D"], "explanation": {"correct": "The H puts qubit 0 into an equal superposition, so its measurement `c0` is 0 or 1 with ~50% each. The feedforward flips qubit 1 exactly when `c0`=1, so `c1` always equals `c0`. The only outcomes are `00` and `11`.", "distractors": {"A": "`01` would need `c1 != c0`; the X is applied precisely when `c0`=1, keeping the two bits equal.", "B": "Qubit 1 is not independent — it is conditioned on qubit 0's result, so the outcomes are correlated, not uniform.", "C": "`10` requires qubit 1 to be 1 while qubit 0 is 0, but X is only applied when `c0`=1.", "E": "This is what you would see if the X ran when `c0`=0. The condition `(cr[0], 1)` fires on the value 1, so the two bits agree rather than differ.", "F": "The right pair of outcomes with the wrong weights: `c0` is 1 on about half the shots and the X fires on every one of them, so `11` is ~50%, not ~25%."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/classical-feedforward-and-control-flow", "https://quantum.cloud.ibm.com/docs/guides/construct-circuits"]}, "proof": {"status": "executed", "evidence": {"A": "observed outcomes={'00': 2000, '11': 2000}; claim {01,00} -> False", "B": "observed outcomes={'00': 2000, '11': 2000}; claim all four -> False", "C": "observed outcomes={'00': 2000, '11': 2000}; claim {10,11} -> False", "D": "observed outcomes={'00': 2000, '11': 2000}; only {00,11} ~50/50 -> True", "E": "observed outcomes={'00': 2000, '11': 2000}; claim {01,10} -> False", "F": "observed outcomes={'00': 2000, '11': 2000}; the 11 share is 0.50, not a quarter -> False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.27}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q024", "section": "s3", "objectives": ["s3o2"], "type": "mcq", "difficulty": 1, "stem": "After measuring qubit 0 into `cr[0]`, you want to apply X to qubit 1 only when that bit equals 1. Which snippet is valid in Qiskit 2.x and produces a conditional (`if_else`) operation?", "code": "# qr = QuantumRegister(2,'q'); cr = ClassicalRegister(2,'c')\n# qc has already done: qc.h(0); qc.measure(0, 0)", "options": [{"key": "A", "text": "`qc.x(1).c_if(cr[0], 1)`"}, {"key": "B", "text": "`with qc.if_test(cr[0], 1):`\n    `qc.x(1)`"}, {"key": "C", "text": "`with qc.if_test((cr[0], 1)):`\n    `qc.x(1)`"}, {"key": "D", "text": "`qc.if_test((cr[0], 1), qc.x, [1])`"}, {"key": "E", "text": "`with qc.if_test(cr[0] == 1):`\n    `qc.x(1)`"}, {"key": "F", "text": "`with qc.while_loop((cr[0], 1)):`\n    `qc.x(1)`"}], "displayCount": 4, "answer": ["C"], "explanation": {"correct": "The modern control-flow API is a context manager whose single argument is a `(classical_resource, value)` TUPLE: `with qc.if_test((cr[0], 1)):`. Gates added inside the block become the conditional body, producing an `if_else` operation.", "distractors": {"A": "`.c_if()` was REMOVED in Qiskit 2.0; calling it raises `AttributeError`. This is the classic v1-to-v2 migration trap.", "B": "`if_test` takes ONE positional argument (the condition tuple); passing `cr[0], 1` as two arguments raises a `TypeError`.", "D": "There is no `(condition, callable, qubits)` form; used without a `with` block and body qubits it raises a `CircuitError`.", "E": "`cr[0] == 1` compares a `Clbit` to an int and returns a plain `bool`, which `if_test` then tries to unpack as a `(resource, value)` pair — `TypeError: 'bool' object is not subscriptable`.", "F": "`while_loop` takes the same condition tuple but builds a `while_loop` operation that repeats its body while the condition holds — not the one-shot `if_else` asked for."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/classical-feedforward-and-control-flow", "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.circuit.QuantumCircuit"]}, "proof": {"status": "executed", "evidence": {"A": "raised AttributeError: 'InstructionSet' object has no attribute 'c_if'", "B": "raised TypeError: 'qiskit.circuit.Clbit' object is not subscriptable", "C": "built successfully; ops={'h': 1, 'measure': 1, 'if_else': 1}; has if_else=True", "D": "raised CircuitError: \"When using 'if_test' with a body, you must pass qubits and clbits.\"", "E": "raised TypeError: 'bool' object is not subscriptable", "F": "built successfully; ops={'h': 1, 'measure': 1, 'while_loop': 1}; has if_else=False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.25}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 1}}, {"id": "s3-q025", "section": "s3", "objectives": ["s3o2"], "type": "spot-bug", "difficulty": 2, "stem": "This conditional code was written for an older Qiskit release and now fails with `AttributeError` on Qiskit 2.x. What is the correct fix?", "code": "from qiskit import QuantumCircuit, QuantumRegister, ClassicalRegister\n\nqr = QuantumRegister(1, 'q')\ncr = ClassicalRegister(1, 'c')\nqc = QuantumCircuit(qr, cr)\nqc.h(0)\nqc.measure(0, 0)\nqc.x(0).c_if(cr, 1)   # AttributeError on Qiskit 2.x", "options": [{"key": "A", "text": "Replace it with the control-flow context manager `with qc.if_test((cr, 1)):` and put `qc.x(0)` in its body."}, {"key": "B", "text": "Change `c_if(cr, 1)` to `c_if(cr[0], 1)`; the condition must reference a single bit rather than a whole register."}, {"key": "C", "text": "Call `qc = qc.decompose()` first; `.c_if()` is usually available again once composite gates are expanded."}, {"key": "D", "text": "Use `qc.x(0, condition=(cr, 1))`; the `condition` keyword replaces `.c_if()`."}, {"key": "E", "text": "Set the condition as an attribute instead: `qc.x(0).condition = (cr, 1)`, which is what `.c_if()` did internally."}, {"key": "F", "text": "Guard the gate with a plain Python `if cr[0] == 1:` block, since the classical bit is typically readable while building."}], "displayCount": 5, "answer": ["A"], "explanation": {"correct": "The `InstructionSet.c_if` method was removed in Qiskit 2.0. Conditionals are now built with the control-flow context manager `with qc.if_test((cr, 1)):`, whose body holds the conditioned gates.", "distractors": {"B": "`.c_if()` no longer exists at all — indexing the register to `cr[0]` still raises `AttributeError`; the method itself is gone.", "C": "`decompose()` expands composite gates and has nothing to do with conditionals; it never restores `.c_if()`.", "D": "Gate methods like `x()` have no `condition` keyword; passing one raises a `TypeError`. Conditions go through `if_test`.", "E": "`x()` returns an `InstructionSet`, which has no `condition` attribute — the assignment raises `AttributeError`. The whole `condition` mechanism went away with `c_if` in Qiskit 2.0.", "F": "A classical bit has no value at build time: `cr[0] == 1` is just a `False` comparison, so the body never runs and no gate — conditional or otherwise — is added."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/classical-feedforward-and-control-flow", "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.circuit.QuantumCircuit"]}, "proof": {"status": "executed", "evidence": {"A": "bug=AttributeError: 'InstructionSet' object has no attribute 'c_if'; fix with if_test -> ops={'h': 1, 'measure': 1, 'if_else': 1} -> valid conditional True", "B": "c_if(cr[0],1) -> raised AttributeError: 'InstructionSet' object has no attribute 'c_if' -> False", "C": "decompose then c_if -> raised AttributeError: 'InstructionSet' object has no attribute 'c_if' -> False", "D": "x(0, condition=...) -> raised TypeError: QuantumCircuit.x() got an unexpected keyword argument 'condition' -> False", "E": "assigning .condition on the InstructionSet returned by x(0) -> raised AttributeError: 'InstructionSet' object has no attribute 'condition' -> valid conditional False", "F": "plain Python 'if cr[0] == 1' around the gate -> ops={'h': 1, 'measure': 1} -> valid conditional False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.24}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q026", "section": "s3", "objectives": ["s3o2"], "type": "predict-output", "difficulty": 3, "stem": "This dynamic circuit branches on `cr[0]`: if it is 0 it applies X to qubit 1, ELSE it applies Z to qubit 1; then it measures qubit 1. Which outcomes appear (bit order `c1 c0`)?", "code": "from qiskit import QuantumCircuit, QuantumRegister, ClassicalRegister\n\nqr = QuantumRegister(2, 'q')\ncr = ClassicalRegister(2, 'c')\nqc = QuantumCircuit(qr, cr)\nqc.h(0)\nqc.measure(0, 0)\nwith qc.if_test((cr[0], 0)) as else_:\n    qc.x(1)\nwith else_:\n    qc.z(1)\nqc.measure(1, 1)", "options": [{"key": "A", "text": "Only `00` and `11`, roughly 50/50."}, {"key": "B", "text": "Only `10` and `01`, roughly 50/50."}, {"key": "C", "text": "Only `11` and `01`, roughly 50/50."}, {"key": "D", "text": "All four outcomes, roughly 25% each."}, {"key": "E", "text": "Only `10` and `11`, roughly 50/50."}, {"key": "F", "text": "It raises a `CircuitError`: the `else_` context manager has to be entered inside the `if_test` block."}], "displayCount": 4, "answer": ["B"], "explanation": {"correct": "When `c0`=0 the X flips qubit 1 to `|1⟩`, so `c1`=1 → `10`. When `c0`=1 the Z acts on `|0⟩` and leaves it unchanged, so `c1`=0 → `01`. The two branches give `10` and `01`, each ~50%.", "distractors": {"A": "`00`/`11` would require the two bits to match; here the X-branch makes them differ and the Z-branch leaves qubit 1 at 0 while `c0`=1, so they always differ.", "C": "`11` never occurs: qubit 1 becomes 1 only in the `c0`=0 branch, which pairs with `c0`=0, giving `10`.", "D": "The outcome of qubit 1 is fully determined by `c0`, so only two of the four combinations occur.", "E": "This treats the `else` branch's Z as a bit flip. Z leaves `|0⟩` unchanged (it only flips the phase of `|1⟩`), so the `c0`=1 branch still measures `c1`=0.", "F": "`with qc.if_test(...) as else_:` yields the else-builder for use AFTER the if-block closes; entering it as a sibling `with` statement is exactly the supported syntax."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/classical-feedforward-and-control-flow", "https://quantum.cloud.ibm.com/docs/guides/construct-circuits"]}, "proof": {"status": "executed", "evidence": {"A": "observed={'10': 2000, '01': 2000}; claim {00,11} -> False", "B": "observed={'10': 2000, '01': 2000}; only {10,01} ~50/50 -> True", "C": "observed={'10': 2000, '01': 2000}; claim {11,01} -> False", "D": "observed={'10': 2000, '01': 2000}; claim all four -> False", "E": "observed={'10': 2000, '01': 2000}; claim {10,11} (Z treated as a bit flip) -> False", "F": "the sibling 'with else_:' block built and ran, producing {'10': 2000, '01': 2000} without raising -> claim CircuitError is False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.24}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 1}}, {"id": "s3-q027", "section": "s3", "objectives": ["s3o2"], "type": "mcq", "difficulty": 1, "stem": "Which scenario genuinely REQUIRES a dynamic circuit (a mid-circuit measurement whose classical result feeds forward to control a later gate), rather than a plain static circuit?", "code": null, "options": [{"key": "A", "text": "Applying a fixed gate sequence to every qubit and measuring only at the end, with no classical result feeding forward."}, {"key": "B", "text": "Sweeping a rotation angle across many values to plot an expectation curve."}, {"key": "C", "text": "Reducing two-qubit gate count by letting the transpiler cancel adjacent gates, which typically shortens the circuit."}, {"key": "D", "text": "Measuring an ancilla mid-circuit and applying a correction gate to a data qubit when that measurement returns 1."}, {"key": "E", "text": "Choosing which circuit to submit next from the previous job's classical result, which typically arrives after that job finished."}, {"key": "F", "text": "Post-selecting on the ancilla measurement result after the job returns — a classical filter that usually discards some shots."}], "displayCount": 4, "answer": ["D"], "explanation": {"correct": "Dynamic circuits exist precisely for real-time classical feedforward: measure a qubit partway through, then condition subsequent quantum operations on that classical outcome (as in teleportation corrections or error-correction syndrome handling). Option D is exactly that pattern.", "distractors": {"A": "Measuring at the end of a fixed gate sequence is a static circuit — no classical result is fed forward mid-execution.", "B": "Parameter sweeps are handled by parameterized circuits and array broadcasting, not by classical feedforward.", "C": "Cancelling adjacent gates is a transpiler optimization; it needs no measurement-conditioned control flow.", "E": "That is classical feedback BETWEEN jobs (the variational-loop pattern), not feedforward inside one execution. Nothing is conditioned mid-circuit, so a static circuit per iteration is enough.", "F": "Post-selection is classical filtering of results that already came back; the quantum execution never branched on the measurement, so no dynamic circuit is involved."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/classical-feedforward-and-control-flow", "https://quantum.cloud.ibm.com/docs/guides/construct-circuits"]}, "proof": {"status": "conceptual"}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q028", "section": "s3", "objectives": ["s3o2"], "type": "predict-output", "difficulty": 2, "stem": "The `for_loop` below repeats its body over `range(3)`, applying X to qubit 0 three times, then measures. What does the measurement return?", "code": "from qiskit import QuantumCircuit\n\nqc = QuantumCircuit(1, 1)\nwith qc.for_loop(range(3)):\n    qc.x(0)\nqc.measure(0, 0)", "options": [{"key": "A", "text": "Always `0` — the repeated X gates cancel out to the identity."}, {"key": "B", "text": "It raises an error because `for_loop` requires a classical register argument."}, {"key": "C", "text": "Always `1` — X is applied an odd number of times (3), flipping `|0⟩` to `|1⟩`."}, {"key": "D", "text": "`0` and `1` with roughly 50% probability each."}, {"key": "E", "text": "It raises a `CircuitError`: the loop body must declare `as i`."}], "displayCount": 4, "answer": ["C"], "explanation": {"correct": "The loop body runs 3 times, so X is applied 3 times. Three X gates equal a single X (odd count), flipping `|0⟩` to `|1⟩`, so every shot measures `1`.", "distractors": {"A": "Three X gates do not cancel — an even count cancels to identity, but 3 is odd, leaving one net flip.", "B": "`for_loop` takes an index set like `range(3)`; it does not require a classical register.", "D": "There is no superposition here — X is deterministic, so the outcome is a definite `1`, not 50/50.", "E": "The loop parameter is optional: `with qc.for_loop(range(3)):` is valid whenever the body does not use the index. It only needs `as i` if a gate consumes the loop value."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/classical-feedforward-and-control-flow", "https://quantum.cloud.ibm.com/docs/guides/construct-circuits"]}, "proof": {"status": "executed", "evidence": {"A": "observed counts={'1': 1000}; claim always 0 -> False", "B": "for_loop(range(3)) built and ran without error -> claim 'error' is False", "C": "observed counts={'1': 1000}; all shots = '1' -> True", "D": "observed counts={'1': 1000}; claim ~50/50 -> False", "E": "the for_loop body was written without 'as i' and still built and ran (counts={'1': 1000}) -> claim CircuitError is False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.21}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 1}}, {"id": "s3-q029", "section": "s3", "objectives": ["s3o2"], "type": "multi", "difficulty": 2, "stem": "Select ALL statements that correctly describe control flow in Qiskit 2.x.", "code": null, "options": [{"key": "A", "text": "`if_test` is used as a context manager whose condition is a `(classical_resource, value)` tuple."}, {"key": "B", "text": "`.c_if()` is still available on gate instructions as the recommended way to add a conditional."}, {"key": "C", "text": "Control-flow blocks such as `if_else` and `for_loop` appear as single operations in `count_ops()`."}, {"key": "D", "text": "A `for_loop` body may contain at most one gate."}, {"key": "E", "text": "A `switch` block can define a fallback branch using `case(case.DEFAULT)`."}, {"key": "F", "text": "An `if_test` condition may reference a single `Clbit`, but never a whole `ClassicalRegister`."}], "displayCount": 5, "answer": ["A", "C", "E"], "explanation": {"correct": "`if_test((resource, value))` is the context-manager API (A). Each control-flow block is stored as one composite instruction, so `count_ops()` shows `if_else`, `for_loop`, etc. (C). `switch` supports a default branch via `case(case.DEFAULT)` (E).", "distractors": {"B": "`.c_if()` was REMOVED in Qiskit 2.0; the supported API is the `if_test` context manager, not `.c_if()`.", "D": "A `for_loop` body can contain any number of gates, not just one.", "F": "Both work: `if_test((clbit, 1))` compares one bit and `if_test((creg, 1))` compares the register's integer value. Register-valued conditions are exactly how multi-bit syndromes are tested."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/classical-feedforward-and-control-flow", "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.circuit.QuantumCircuit"]}, "proof": {"status": "executed", "evidence": {"A": "if_test((clbit,1)) produced {'measure': 1, 'if_else': 1} -> True", "B": "gate.c_if(...) available -> False (AttributeError means removed)", "C": "for_loop op count = {'for_loop': 1} -> single-op block True", "D": "for_loop body with 3 gates accepted; 'at most one gate' -> False", "E": "switch with case.DEFAULT produced {'measure': 1, 'switch_case': 1} -> True", "F": "if_test((ClassicalRegister, 1)) produced {'measure': 1, 'if_else': 1}; register-valued conditions work -> 'single Clbit only' is False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.22}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 1}}, {"id": "s3-q030", "section": "s3", "objectives": ["s3o4"], "type": "mcq", "difficulty": 1, "stem": "You build `pm = generate_preset_pass_manager(optimization_level=2, backend=backend)`, which returns a `StagedPassManager`. What are its stages, in execution order?", "code": "from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager\n\npm = generate_preset_pass_manager(optimization_level=2, backend=backend)\nprint(pm.stages)", "options": [{"key": "A", "text": "`('layout', 'routing', 'translation', 'optimization')` — four stages, no init and no scheduling."}, {"key": "B", "text": "`('init', 'layout', 'routing', 'optimization', 'measurement')`."}, {"key": "C", "text": "`('sampling', 'layout', 'routing', 'translation', 'scheduling')`."}, {"key": "D", "text": "`('init', 'layout', 'routing', 'translation', 'optimization', 'scheduling')` — all six, by default."}, {"key": "E", "text": "`('init', 'layout', 'routing', 'translation', 'optimization')` — scheduling needs timing info."}, {"key": "F", "text": "`('init', 'translation', 'layout', 'routing', 'optimization', 'scheduling')` — basis fixed first."}], "displayCount": 5, "answer": ["D"], "explanation": {"correct": "A preset `StagedPassManager` runs six stages in order: init, layout, routing, translation, optimization, and scheduling. Layout+routing map the circuit to hardware qubits/connectivity, translation rewrites to the basis gates, optimization simplifies, and scheduling adds timing.", "distractors": {"A": "This omits the `init` and `scheduling` stages; the preset pipeline has six named stages, not four.", "B": "`measurement` is not a transpiler stage, and this list drops `translation` and `scheduling`.", "C": "`sampling` is a runtime/primitive concept, not a transpiler stage; the pipeline never contains it.", "E": "`scheduling` is always one of the six stage slots of a preset `StagedPassManager`; it simply runs an empty pass set when no scheduling method was asked for.", "F": "The six names are right but the order is not: layout and routing come before translation, because the router works on the abstract two-qubit gates before they are rewritten into the basis."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/transpiler-stages", "https://quantum.cloud.ibm.com/docs/guides/transpile-with-pass-managers"]}, "proof": {"status": "executed", "evidence": {"A": "observed pm.stages=('init', 'layout', 'routing', 'translation', 'optimization', 'scheduling'); option=('layout', 'routing', 'translation', 'optimization') -> match=False", "B": "observed pm.stages=('init', 'layout', 'routing', 'translation', 'optimization', 'scheduling'); option=('init', 'layout', 'routing', 'optimization', 'measurement') -> match=False", "C": "observed pm.stages=('init', 'layout', 'routing', 'translation', 'optimization', 'scheduling'); option=('sampling', 'layout', 'routing', 'translation', 'scheduling') -> match=False", "D": "observed pm.stages=('init', 'layout', 'routing', 'translation', 'optimization', 'scheduling'); option=('init', 'layout', 'routing', 'translation', 'optimization', 'scheduling') -> match=True", "E": "observed pm.stages=('init', 'layout', 'routing', 'translation', 'optimization', 'scheduling') (6 slots incl. scheduling); option=('init', 'layout', 'routing', 'translation', 'optimization') -> match=False", "F": "observed pm.stages=('init', 'layout', 'routing', 'translation', 'optimization', 'scheduling'); option=('init', 'translation', 'layout', 'routing', 'optimization', 'scheduling') reorders translation -> match=False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.25}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q031", "section": "s3", "objectives": ["s3o4"], "type": "mcq", "difficulty": 2, "stem": "In Qiskit 2.x, you call `generate_preset_pass_manager(backend=backend)` WITHOUT passing `optimization_level`. Which optimization level does it use by default?", "code": "from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager\n\npm = generate_preset_pass_manager(backend=backend)   # no optimization_level given", "options": [{"key": "A", "text": "0"}, {"key": "B", "text": "2"}, {"key": "C", "text": "1"}, {"key": "D", "text": "3"}, {"key": "E", "text": "There is no default — `optimization_level` is a required argument."}, {"key": "F", "text": "It depends on the backend: the level is chosen from the target's qubit count."}], "displayCount": 4, "answer": ["B"], "explanation": {"correct": "`generate_preset_pass_manager` defaults to `optimization_level=2` — a balanced level with light-weight but effective optimization. (In Qiskit 2.x the legacy `transpile()` resolves to the same default, so level 2 is what you get either way; level 1 was `transpile()`'s default back in 1.x.)", "distractors": {"A": "Level 0 is minimal (only what is needed to run) and is not the default; you must ask for it explicitly.", "C": "Level 1 is a lighter, mapping-focused level that you have to request explicitly. It was the default of the legacy `transpile()` in Qiskit 1.x, but in 2.x `transpile()` resolves to 2 as well.", "D": "Level 3 is the heaviest optimization and is never applied unless you request it.", "E": "The parameter has a default in the signature, so the call is valid without it — which is exactly why the default value matters.", "F": "The default is a fixed constant in the signature, not a function of the target: a 5-qubit and a 9-qubit device both get the same level."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/set-optimization", "https://quantum.cloud.ibm.com/docs/guides/transpile-with-pass-managers"]}, "proof": {"status": "executed", "evidence": {"A": "signature default optimization_level=2; claim 0 -> False", "B": "signature default=2; default pm output matches explicit level 2 -> True", "C": "signature default=2; claim 1 (that is transpile()'s default, not this) -> False", "D": "signature default=2; claim 3 -> False", "E": "the signature carries optimization_level=2 as a default value, so the argument is optional -> 'no default / required' is False", "F": "the same default reproduced explicit level 2 on a 9-qubit device and level 2 on the 5-qubit one -> 'depends on the backend' is False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.26}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 1}}, {"id": "s3-q032", "section": "s3", "objectives": ["s3o4"], "type": "predict-output", "difficulty": 3, "stem": "A circuit whose only gate is `cx(0, 4)` is transpiled onto a 5-qubit LINE backend (connectivity 0-1-2-3-4, so qubits 0 and 4 are NOT directly connected). Comparing `optimization_level=0` with `optimization_level=1`, what happens to the number of CX gates?", "code": "from qiskit import QuantumCircuit\nfrom qiskit.transpiler import CouplingMap\nfrom qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager\nfrom qiskit.providers.fake_provider import GenericBackendV2\n\nbackend = GenericBackendV2(5, coupling_map=CouplingMap.from_line(5), seed=1)\nqc = QuantumCircuit(5)\nqc.cx(0, 4)\n\nfor lvl in (0, 1):\n    isa = generate_preset_pass_manager(optimization_level=lvl,\n                                       backend=backend, seed_transpiler=1).run(qc)\n    print(lvl, isa.count_ops())", "options": [{"key": "A", "text": "Level 0 keeps the trivial layout and routes with SWAPs (many CX); level 1 picks an adjacent pair, needing one CX."}, {"key": "B", "text": "Both levels typically produce one CX; the optimization level does not influence qubit layout."}, {"key": "C", "text": "Level 0 produces exactly one CX; level 1 inserts SWAPs to satisfy the line coupling map and ends up with more CX gates."}, {"key": "D", "text": "Both levels raise an error because a CX between non-adjacent qubits is not routable on a line."}, {"key": "E", "text": "Level 0 keeps the trivial layout and routes with SWAPs; level 1 typically routes the same way, then cancels the redundant CX pairs."}, {"key": "F", "text": "Level 0 needs 3 CX (one SWAP plus the original gate) and level 1 needs 1; SWAP cost usually does not grow with distance."}], "displayCount": 4, "answer": ["A"], "explanation": {"correct": "At level 0 the layout is trivial (virtual qubit i -> physical qubit i), so the CX between physical 0 and 4 must be routed with SWAPs, decomposed into many CX gates. At level 1 the layout pass picks two adjacent physical qubits for the interacting virtual qubits, so a single CX suffices. This is why layout/optimization level dramatically affects gate count.", "distractors": {"B": "Layout selection is part of the optimization level: level 0 uses a trivial layout while level 1+ actively searches for a good one, so the CX counts differ.", "C": "It is the other way around — the higher level reduces the count by choosing a better layout; level 0 is the expensive one here.", "D": "Routing on a line succeeds by inserting SWAPs; a single CX never makes the router give up.", "E": "Level 1's saving comes from the LAYOUT, not from cancellation: it starts the two interacting virtual qubits on a connected physical pair, so the router never inserts a SWAP at all.", "F": "Distance matters: physical qubits 0 and 4 are four hops apart on the line, so the router inserts three SWAPs — nine CX plus the original one, not a single SWAP."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/set-optimization", "https://quantum.cloud.ibm.com/docs/guides/transpiler-stages"]}, "proof": {"status": "executed", "evidence": {"A": "level0 cx=10, level1 cx=1; many-vs-one -> True", "B": "level0 cx=10, level1 cx=1; both==1? -> False", "C": "level0 cx=10, level1 cx=1; level1 more? -> False", "D": "both levels transpiled without error (cx: 10, 1) -> False", "E": "level0 initial layout=[0, 1, 2, 3, 4] (trivial), level1 initial layout=[0, 4, 2, 3, 1]; level 1 moved the interacting qubits onto a connected pair instead of cancelling SWAPs -> False", "F": "level0 cx=10, i.e. three SWAPs plus the original gate over four hops, not 3 -> False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.24}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q033", "section": "s3", "objectives": ["s3o4"], "type": "mcq", "difficulty": 2, "stem": "A backend's target has basis gates {cx, rz, sx, x} on a linear coupling map (qubit i connected only to i-1 and i+1). Which circuit is ALREADY an ISA circuit for this backend — needing no further transpilation?", "code": null, "options": [{"key": "A", "text": "A circuit using only `rz`, `sx`, and `cx`, with every `cx` on a physically-adjacent pair."}, {"key": "B", "text": "A circuit that applies an `h` gate to qubit 0."}, {"key": "C", "text": "A circuit with a `cx` between qubit 0 and qubit 4, which are not adjacent on the linear coupling map."}, {"key": "D", "text": "A circuit containing a `ccx` (Toffoli) gate."}, {"key": "E", "text": "A circuit of `h` and `cx` gates that has been expanded into its component gates with `qc.decompose()`."}, {"key": "F", "text": "A 6-qubit circuit of `rz`, `sx` and `cx` gates whose `cx` gates all sit on adjacent qubits of the line."}], "displayCount": 5, "answer": ["A"], "explanation": {"correct": "An ISA (Instruction Set Architecture) circuit uses ONLY the backend's basis gates AND respects its coupling map. Option A satisfies both: `rz`/`sx`/`cx` are in the basis and each `cx` is on connected qubits, so no translation or routing is needed.", "distractors": {"B": "`h` is not in the basis {cx, rz, sx, x}; a translation pass must rewrite it, so the circuit is not yet ISA.", "C": "A `cx` on non-adjacent qubits violates the coupling map; the router must insert SWAPs first, so it is not ISA.", "D": "`ccx` is neither a basis gate nor a two-qubit gate; it must be decomposed and routed, so it is not ISA.", "E": "`decompose()` unrolls one level of gate definitions; it knows nothing about this target, so `h` becomes a `u` gate that is still outside the basis. Only transpilation produces an ISA circuit.", "F": "Basis and connectivity are fine, but the circuit is wider than the device: ISA also means the circuit fits the target's qubit count, and 6 virtual qubits cannot map onto 5 physical ones."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/transpile", "https://quantum.cloud.ibm.com/docs/guides/transpiler-stages"]}, "proof": {"status": "executed", "evidence": {"A": "is_isa -> True (only basis gates on adjacent qubits)", "B": "is_isa -> False (h not in basis ['cx', 'delay', 'id', 'measure', 'reset', 'rz', 'sx', 'x'])", "C": "is_isa -> False (cx on non-adjacent (0, 4))", "D": "is_isa -> False (ccx not in basis ['cx', 'delay', 'id', 'measure', 'reset', 'rz', 'sx', 'x'])", "E": "decompose() gave ops={'u': 1, 'cx': 1}; is_isa -> False (u not in basis ['cx', 'delay', 'id', 'measure', 'reset', 'rz', 'sx', 'x'])", "F": "is_isa -> False (circuit width 6 exceeds the 5-qubit device)"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.22}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q034", "section": "s3", "objectives": ["s3o4"], "type": "spot-bug", "difficulty": 2, "stem": "This code transpiles a 4-qubit circuit for a 3-qubit backend and raises a `TranspilerError`. What is the root cause?", "code": "from qiskit import QuantumCircuit\nfrom qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager\nfrom qiskit.providers.fake_provider import GenericBackendV2\n\nbackend = GenericBackendV2(num_qubits=3, seed=42)\nqc = QuantumCircuit(4)\nqc.h(range(4))\nqc.measure_all()\n\npm = generate_preset_pass_manager(optimization_level=1, backend=backend)\nisa = pm.run(qc)   # raises TranspilerError", "options": [{"key": "A", "text": "`measure_all()` must be called AFTER transpilation, not before."}, {"key": "B", "text": "The circuit declares 4 qubits but `GenericBackendV2(num_qubits=3)` has only 3, so the layout stage cannot map every virtual qubit."}, {"key": "C", "text": "`optimization_level=1` is too low to map 4 qubits; raising it to level 3 fixes it."}, {"key": "D", "text": "`GenericBackendV2(num_qubits=3)` must be given an explicit `coupling_map`, or the pass manager always fails on this circuit."}, {"key": "E", "text": "`GenericBackendV2(num_qubits=3)` is fine — pass `initial_layout=[0, 1, 2, 3]` to pin the mapping by hand."}, {"key": "F", "text": "`measure_all()` adds 4 clbits but a 3-qubit backend has only 3 readout channels, so layout refuses the circuit."}], "displayCount": 5, "answer": ["B"], "explanation": {"correct": "Transpilation maps each virtual qubit onto a distinct physical qubit. With 4 virtual qubits and only 3 physical qubits, no valid layout exists, so the layout stage raises `TranspilerError: Number of qubits greater than device.` The fix is a backend with at least 4 qubits.", "distractors": {"A": "The position of `measure_all()` is irrelevant to the qubit-count mismatch; the error occurs during layout, not measurement.", "C": "No optimization level can invent extra physical qubits; higher levels still fail on the same mismatch.", "D": "`GenericBackendV2` supplies a default coupling map; the failure is the qubit count, not a missing coupling map.", "E": "A hand-written layout cannot invent a fourth physical qubit — the pass manager still fails, because the device simply does not have a qubit 3.", "F": "Classical bits are never the constraint: the same 4-qubit circuit fails without any measurements, and the same `measure_all()` circuit transpiles fine on a 4-qubit backend."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/transpile", "https://quantum.cloud.ibm.com/docs/guides/set-optimization"]}, "proof": {"status": "executed", "evidence": {"A": "removing measure_all still fails on 3q backend (fixed=False) -> False", "B": "bug=TranspilerError: 'Number of qubits greater than device.'; a 4-qubit backend transpiles fine (ops={'rz': 8, 'sx': 4, 'measure': 4, 'barrier': 1}) -> True", "C": "optimization_level=3 on 3q backend still fails (fixed=False) -> False", "D": "GenericBackendV2 had a default coupling map; the real cause is qubit count -> False", "E": "pinning the layout by hand on the 3-qubit backend -> TranspilerError: 'HighLevelSynthesis is unable to synthesize \"measure\"' -> fixed=False", "F": "the same 4-qubit circuit without any classical bits still fails (fixed=False), and the measured circuit transpiles fine on a 4-qubit backend (ops={'rz': 8, 'sx': 4, 'measure': 4, 'barrier': 1}) -> False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.22}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q035", "section": "s3", "objectives": ["s3o4"], "type": "mcq", "difficulty": 2, "stem": "You have no backend object, but you know the target's basis gates `['cx','rz','sx','x']` and its coupling map `cmap`. Which call builds a pass manager that transpiles `qc` into an ISA circuit for that target?", "code": "from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager\n# cmap is a CouplingMap; qc is an abstract circuit containing an h gate\nbasis = ['cx', 'rz', 'sx', 'x']", "options": [{"key": "A", "text": "`generate_preset_pass_manager(optimization_level=1).run(qc)` — the basis and coupling map are inferred automatically."}, {"key": "B", "text": "`transpile(qc)` with no other arguments already returns an ISA circuit."}, {"key": "C", "text": "`generate_preset_pass_manager(1, backend='ibm_target')` using the target's name as a string."}, {"key": "D", "text": "`generate_preset_pass_manager(optimization_level=1, basis_gates=basis, coupling_map=cmap).run(qc)`."}, {"key": "E", "text": "`generate_preset_pass_manager(optimization_level=1, target=basis).run(qc)` — the basis list doubles as the target."}, {"key": "F", "text": "`generate_preset_pass_manager(optimization_level=1, basis_gates=basis).run(qc)` — connectivity is read off the circuit."}], "displayCount": 4, "answer": ["D"], "explanation": {"correct": "You can transpile without a backend object by passing the target constraints directly: `basis_gates` and `coupling_map` (and/or a `Target`). The pass manager then translates gates to the basis and routes to the connectivity, yielding an ISA circuit.", "distractors": {"A": "With no target information, the pass manager cannot know the basis or connectivity; the `h` gate stays untranslated, so the result is not ISA.", "B": "`transpile(qc)` with no target leaves gates like `h` in place — it does not produce an ISA circuit for any particular device.", "C": "`backend` must be a `BackendV2` object, not a string; passing a string raises `AttributeError` (no `.target`).", "E": "`target` expects a `Target` object describing gates, qubits and connectivity; handing it a list of gate names fails. Loose constraints go in through `basis_gates`/`coupling_map` instead.", "F": "The basis alone gets the gates translated but leaves routing unconstrained: two-qubit gates survive on non-connected pairs, so the result is not ISA for this coupling map."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/transpile-with-pass-managers", "https://quantum.cloud.ibm.com/docs/guides/transpile"]}, "proof": {"status": "executed", "evidence": {"A": "produced circuit ops={'cx': 3, 'h': 1}; is_isa=False", "B": "produced circuit ops={'cx': 3, 'h': 1}; is_isa=False", "C": "raised AttributeError: 'str' object has no attribute 'target'", "D": "produced circuit ops={'cx': 6, 'rz': 2, 'sx': 1}; is_isa=True", "E": "raised AttributeError: 'list' object has no attribute 'build_coupling_map'", "F": "produced circuit ops={'cx': 3, 'rz': 2, 'sx': 1}; is_isa=False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.23}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 1}}, {"id": "s3-q036", "section": "s3", "objectives": ["s3o4"], "type": "multi", "difficulty": 2, "stem": "Select ALL statements that are TRUE about transpiler optimization levels 0-3.", "code": null, "options": [{"key": "A", "text": "Level 0 does the minimum needed to make the circuit runnable on the target, with essentially no optimization."}, {"key": "B", "text": "Level 3 applies the most aggressive optimization and typically takes the longest to run."}, {"key": "C", "text": "A higher optimization level is guaranteed to produce fewer two-qubit gates for every circuit."}, {"key": "D", "text": "`generate_preset_pass_manager` uses optimization level 1 by default."}, {"key": "E", "text": "All four levels target the same basis gates and coupling constraints; they differ in how hard they work to optimize."}, {"key": "F", "text": "Level 0 skips routing altogether, so two-qubit gates on non-adjacent qubits come back unrouted."}], "displayCount": 5, "answer": ["A", "B", "E"], "explanation": {"correct": "Level 0 is minimal mapping/translation with no real optimization (A). Level 3 is the most aggressive and usually the slowest (B). Every level produces a circuit valid for the SAME target constraints — the difference is optimization effort, not the constraints (E).", "distractors": {"C": "Optimization uses heuristics; a higher level USUALLY helps but is NOT guaranteed to beat a lower level on every circuit.", "D": "`generate_preset_pass_manager` defaults to level 2, not level 1. (Level 1 was the legacy `transpile()` default in Qiskit 1.x; in 2.x `transpile()` also resolves to 2.)", "F": "Every level routes — level 0 just uses a trivial layout and then inserts whatever SWAPs that layout forces, which is why it often produces the MOST two-qubit gates."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/set-optimization", "https://quantum.cloud.ibm.com/docs/guides/transpile-with-pass-managers"]}, "proof": {"status": "conceptual"}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 1}}, {"id": "s3-q037", "section": "s3", "objectives": ["s3o4"], "type": "predict-output", "difficulty": 2, "stem": "The circuit has `h` and `t` gates on qubit 0. After transpiling to a `GenericBackendV2` whose basis is {cx, id, rz, sx, x, ...}, which statement about the resulting ISA circuit is TRUE?", "code": "from qiskit import QuantumCircuit\nfrom qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager\nfrom qiskit.providers.fake_provider import GenericBackendV2\n\nbackend = GenericBackendV2(num_qubits=5, seed=11)\nqc = QuantumCircuit(2)\nqc.h(0)\nqc.t(0)\nqc.cx(0, 1)\n\nisa = generate_preset_pass_manager(1, backend, seed_transpiler=11).run(qc)\nprint(isa.count_ops())", "options": [{"key": "A", "text": "It still contains a `t` gate, because `t` is typically kept as a standard gate."}, {"key": "B", "text": "It keeps the `h` gate but the `t` gate is removed entirely."}, {"key": "C", "text": "Neither `h` nor `t` survives; both are rewritten into the target basis (e.g. `rz`/`sx`)."}, {"key": "D", "text": "Transpilation raises an error because `t` is not one of the basis gates."}, {"key": "E", "text": "Neither `h` nor `t` survives, and the `cx` is typically rewritten too."}, {"key": "F", "text": "`h` and `t` are usually merged into one universal `u` gate."}], "displayCount": 4, "answer": ["C"], "explanation": {"correct": "The translation stage rewrites every gate into the target's basis. Since neither `h` nor `t` is a basis gate, both are decomposed into `rz`/`sx` (and the two-qubit `cx` stays), so the ISA circuit contains no `h` or `t`.", "distractors": {"A": "`t` is not in this backend's basis, so it cannot survive — it is translated away like any other non-basis gate.", "B": "`h` is also absent from the basis and is likewise rewritten; it does not remain either.", "D": "A gate outside the basis is translated, not rejected — transpilation succeeds without error.", "E": "`cx` IS in this backend's basis, so translation leaves it alone — only the gates outside the basis (`h`, `t`) are rewritten.", "F": "`u` is not in this target's basis either; the single-qubit rotations are emitted as `rz`/`sx` sequences, and no `u` gate appears in the result."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/transpiler-stages", "https://quantum.cloud.ibm.com/docs/guides/transpile"]}, "proof": {"status": "executed", "evidence": {"A": "transpiled ops={'rz': 2, 'sx': 1, 'cx': 1}; contains 't'? False -> False", "B": "transpiled ops={'rz': 2, 'sx': 1, 'cx': 1}; keeps 'h'? False -> False", "C": "transpiled ops={'rz': 2, 'sx': 1, 'cx': 1}; no h/t and all in basis=True -> True", "D": "transpilation succeeded (ops={'rz': 2, 'sx': 1, 'cx': 1}); claim 'error' -> False", "E": "transpiled ops={'rz': 2, 'sx': 1, 'cx': 1}; cx is in the basis and survived (has cx=True) -> False", "F": "transpiled ops={'rz': 2, 'sx': 1, 'cx': 1}; no 'u' gate appears (has u=False) -> False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.25}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q038", "section": "s3", "objectives": ["s3o4"], "type": "mcq", "difficulty": 2, "stem": "In the preset transpilation pipeline, which stage inserts SWAP gates so that every two-qubit gate acts on a physically-connected pair of qubits?", "code": null, "options": [{"key": "A", "text": "The layout stage."}, {"key": "B", "text": "The routing stage."}, {"key": "C", "text": "The translation stage."}, {"key": "D", "text": "The optimization stage."}, {"key": "E", "text": "The scheduling stage."}, {"key": "F", "text": "The init stage."}], "displayCount": 4, "answer": ["B"], "explanation": {"correct": "Routing is the stage that makes a circuit satisfy the coupling map: when a two-qubit gate is requested between non-adjacent physical qubits, the router inserts SWAP gates to bring the qubit states together. Layout only assigns the initial virtual->physical mapping.", "distractors": {"A": "The layout stage chooses which physical qubit each virtual qubit starts on; it does not insert SWAPs to move them during the circuit.", "C": "Translation rewrites gates into the target's basis set; it has nothing to do with connectivity.", "D": "Optimization simplifies and cancels gates; SWAP insertion for connectivity is the router's job, not optimization's.", "E": "Scheduling assigns timing (delays, alignment) to an already routed and translated circuit; it never changes which qubits a gate acts on.", "F": "The init stage runs preliminary passes such as unrolling three-qubit gates before layout; connectivity is not known to it and no SWAP is inserted there."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/transpiler-stages", "https://quantum.cloud.ibm.com/docs/guides/transpile"]}, "proof": {"status": "conceptual"}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 1}}, {"id": "s3-q039", "section": "s3", "objectives": ["s3o4"], "type": "spot-bug", "difficulty": 1, "stem": "This call raises. What is the reason?", "code": "from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager\nfrom qiskit.providers.fake_provider import GenericBackendV2\n\nbackend = GenericBackendV2(num_qubits=5, seed=42)\npm = generate_preset_pass_manager(optimization_level=4, backend=backend)", "options": [{"key": "A", "text": "Valid optimization levels are 0, 1, 2, and 3; level 4 does not exist and raises a `ValueError`."}, {"key": "B", "text": "Level 4 exists but is only allowed for real hardware backends, not fake ones."}, {"key": "C", "text": "`optimization_level` has to be passed as a string, e.g. `'4'`."}, {"key": "D", "text": "Level 4 is valid but usually requires `seed_transpiler` to be set first."}, {"key": "E", "text": "Valid levels are 1–4, but level 4 typically needs the `qiskit-ibm-transpiler` add-on."}, {"key": "F", "text": "The keyword order is usually wrong: `backend` has to come before `optimization_level`."}], "displayCount": 4, "answer": ["A"], "explanation": {"correct": "The preset pass managers define exactly four optimization levels: 0, 1, 2, and 3. Passing 4 is out of range and raises `ValueError: Invalid optimization level 4`.", "distractors": {"B": "There is no level 4 for any backend type; the range is 0-3 regardless of real or fake hardware.", "C": "`optimization_level` is an integer argument; passing a string is not the fix and level 4 still would not exist.", "D": "`seed_transpiler` is unrelated and only fixes the random seed; even with a seed, level 4 is invalid.", "E": "The range starts at 0, not 1: level 0 builds fine here, and no add-on adds a level 4 to the preset pass managers.", "F": "Keyword arguments are order-independent in Python; putting `backend` first raises exactly the same `ValueError` about the level."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/set-optimization", "https://quantum.cloud.ibm.com/docs/guides/transpile-with-pass-managers"]}, "proof": {"status": "executed", "evidence": {"A": "level 4 -> ValueError: Invalid optimization level 4; levels [0, 1, 2, 3] all built OK -> True", "B": "level 4 fails on this backend with ValueError: Invalid optimization level 4; there is no hardware-only level 4 -> False", "C": "optimization_level is an int; the failure is out-of-range, not a type issue (ValueError: Invalid optimization level 4) -> False", "D": "seed_transpiler does not enable a level 4; ValueError: Invalid optimization level 4 -> False", "E": "levels [0, 1, 2, 3] all built, so the range starts at 0 and level 4 is simply out of it (ValueError: Invalid optimization level 4) -> 'levels are 1-4' is False", "F": "passing the backend keyword first still gives ValueError: Invalid optimization level 4; keyword order is irrelevant -> False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.23}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q040", "section": "s3", "objectives": ["s3o4"], "type": "mcq", "difficulty": 1, "stem": "You have built `pm = generate_preset_pass_manager(optimization_level=1, backend=backend)`. Which call actually transpiles `qc` into an ISA circuit using this pass manager?", "code": "from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager\n\npm = generate_preset_pass_manager(optimization_level=1, backend=backend)\n# isa = ?", "options": [{"key": "A", "text": "`isa = pm.transpile(qc)`"}, {"key": "B", "text": "`isa = pm.run(qc)`"}, {"key": "C", "text": "`isa = pm(qc)`"}, {"key": "D", "text": "`isa = transpile(qc, pm)`"}, {"key": "E", "text": "`isa = pm.run([qc])`"}, {"key": "F", "text": "`isa = qc.transpile(pm)`"}], "displayCount": 4, "answer": ["B"], "explanation": {"correct": "A pass manager executes its passes on a circuit via `.run(circuit)`. For a preset `StagedPassManager`, `pm.run(qc)` returns the transpiled ISA circuit.", "distractors": {"A": "`StagedPassManager` has no `.transpile()` method; calling it raises `AttributeError`. `.run()` is the method.", "C": "A pass manager is not callable; `pm(qc)` raises `TypeError`.", "D": "`transpile()` expects a backend/target, not a pass-manager object; passing `pm` there raises an error.", "E": "`run` mirrors its input: hand it a list and you get a LIST of circuits back, so `isa` is not a circuit. Pass the circuit itself (or index the result).", "F": "`QuantumCircuit` has no `transpile` method — transpilation is driven by the pass manager (`pm.run`) or the standalone `transpile()` function."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/transpile-with-pass-managers", "https://quantum.cloud.ibm.com/docs/guides/transpile"]}, "proof": {"status": "executed", "evidence": {"A": "raised AttributeError: 'StagedPassManager' object has no attribute 'transpile'", "B": "returned QuantumCircuit; is a transpiled circuit=True", "C": "raised TypeError: 'StagedPassManager' object is not callable", "D": "raised AttributeError: 'StagedPassManager' object has no attribute 'num_qubits'", "E": "returned list; is a transpiled circuit=False", "F": "raised AttributeError: 'QuantumCircuit' object has no attribute 'transpile'"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.21}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 1}}, {"id": "s3-q041", "section": "s3", "objectives": ["s3o4"], "type": "predict-output", "difficulty": 3, "stem": "A single Toffoli `ccx(0, 1, 2)` is transpiled onto a 3-qubit LINE backend (basis {cx, rz, sx, x}; connectivity 0-1-2). How many two-qubit (`cx`) gates does the resulting ISA circuit contain?", "code": "from qiskit import QuantumCircuit\nfrom qiskit.transpiler import CouplingMap\nfrom qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager\nfrom qiskit.providers.fake_provider import GenericBackendV2\n\nbackend = GenericBackendV2(3, coupling_map=CouplingMap.from_line(3), seed=7)\nqc = QuantumCircuit(3)\nqc.ccx(0, 1, 2)\n\nisa = generate_preset_pass_manager(1, backend, seed_transpiler=7).run(qc)\nprint(isa.count_ops().get('cx'))", "options": [{"key": "A", "text": "0 — the backend typically executes a 3-qubit Toffoli natively, so no CX gates are needed."}, {"key": "B", "text": "Exactly 1 CX."}, {"key": "C", "text": "Between 2 and 5 CX."}, {"key": "D", "text": "6 or more CX — the Toffoli decomposition alone needs six."}, {"key": "E", "text": "Exactly 6 CX — the six-CX decomposition typically fits the line without any routing."}, {"key": "F", "text": "3 CX — the transpiler usually rewrites a Toffoli into three two-qubit gates on a line."}], "displayCount": 4, "answer": ["D"], "explanation": {"correct": "`ccx` is neither a basis gate nor a two-qubit gate, so it is decomposed. A Toffoli already needs 6 CX in an all-to-all decomposition, and routing it on a linear coupling map inserts additional CX (via SWAPs), giving well over 6 CX in practice.", "distractors": {"A": "No IBM backend offers a native 3-qubit gate; `ccx` sits outside the basis and has to be decomposed into CX-based gates.", "B": "One CX is nowhere near enough to realize a Toffoli — the standard decomposition alone needs 6.", "C": "The minimal Toffoli decomposition already uses 6 CX; on a line with routing the count is even higher, so it is not in the 2-5 range.", "E": "Six is the count for an all-to-all decomposition; on a line the router adds more, so the ISA circuit ends up above six.", "F": "No three-CX Toffoli exists: three two-qubit gates cannot implement a doubly-controlled X, and the transpiled circuit shows far more than three."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/transpiler-stages", "https://quantum.cloud.ibm.com/docs/guides/transpile"]}, "proof": {"status": "executed", "evidence": {"A": "observed cx count=9; claim 0 -> False", "B": "observed cx count=9; claim 1 -> False", "C": "observed cx count=9; claim 2-5 -> False", "D": "observed cx count=9; claim >=6 -> True", "E": "observed cx count=9; claim exactly 6 with no routing overhead -> False", "F": "observed cx count=9; claim 3 -> False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.23}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q042", "section": "s3", "objectives": ["s3o4"], "type": "multi", "difficulty": 2, "stem": "For a `StagedPassManager` from `generate_preset_pass_manager(...)`, select ALL names that are actual stages of the pipeline (they appear in `pm.stages`).", "code": null, "options": [{"key": "A", "text": "`layout`"}, {"key": "B", "text": "`routing`"}, {"key": "C", "text": "`mitigation`"}, {"key": "D", "text": "`scheduling`"}, {"key": "E", "text": "`sampling`"}, {"key": "F", "text": "`translation`"}], "displayCount": null, "answer": ["A", "B", "D", "F"], "explanation": {"correct": "The preset pipeline's stages are init, layout, routing, translation, optimization, and scheduling — so `layout`, `routing`, `translation`, and `scheduling` are all real stages of `pm.stages`.", "distractors": {"C": "`mitigation` is an error-mitigation concept applied at runtime by the primitives, not a transpiler stage.", "E": "`sampling` refers to the Sampler primitive's execution, not a transpilation stage."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/transpiler-stages", "https://quantum.cloud.ibm.com/docs/guides/transpile-with-pass-managers"]}, "proof": {"status": "executed", "evidence": {"A": "pm.stages=('init', 'layout', 'routing', 'translation', 'optimization', 'scheduling'); is 'layout' a stage? True", "B": "pm.stages=('init', 'layout', 'routing', 'translation', 'optimization', 'scheduling'); is 'routing' a stage? True", "C": "pm.stages=('init', 'layout', 'routing', 'translation', 'optimization', 'scheduling'); is 'mitigation' a stage? False", "D": "pm.stages=('init', 'layout', 'routing', 'translation', 'optimization', 'scheduling'); is 'scheduling' a stage? True", "E": "pm.stages=('init', 'layout', 'routing', 'translation', 'optimization', 'scheduling'); is 'sampling' a stage? False", "F": "pm.stages=('init', 'layout', 'routing', 'translation', 'optimization', 'scheduling'); is 'translation' a stage? True"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.24}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q043", "section": "s3", "objectives": ["s3o4"], "type": "mcq", "difficulty": 3, "stem": "A 3-qubit circuit is transpiled with `initial_layout=[2, 3, 4]` onto a 5-qubit backend. What does this argument do?", "code": "from qiskit import transpile\n\nisa = transpile(qc, backend, initial_layout=[2, 3, 4], optimization_level=0)", "options": [{"key": "A", "text": "It sets the optimization level, which may be given as the list `[2, 3, 4]`."}, {"key": "B", "text": "It adds measurements of physical qubits 2, 3, and 4 at the end of the transpiled circuit."}, {"key": "C", "text": "It pins the starting layout: virtual qubits 0, 1, 2 go to physical qubits 2, 3, 4."}, {"key": "D", "text": "It restricts the job so it runs only when physical qubits 2, 3, 4 are idle."}, {"key": "E", "text": "It pins the map the other way round: physical qubit 0 is generally sent to virtual 2, physical 1 to virtual 3."}, {"key": "F", "text": "It restricts the transpiler to physical qubits 2, 3 and 4, which it may then assign in whichever order it likes."}], "displayCount": 4, "answer": ["C"], "explanation": {"correct": "`initial_layout` pins the initial virtual-to-physical qubit mapping: the i-th entry is the physical qubit that virtual qubit i is assigned to. Here virtual 0->physical 2, virtual 1->physical 3, virtual 2->physical 4.", "distractors": {"A": "The optimization level is a separate integer argument (`optimization_level=0` here); `initial_layout` is a qubit mapping, not a list of levels.", "B": "`initial_layout` places qubits; it does not add measurements. Measurements come from `measure`/`measure_all`.", "D": "`initial_layout` is a compile-time mapping choice, not a runtime scheduling/idle constraint.", "E": "The list is indexed by VIRTUAL qubit: entry i names the physical qubit that virtual qubit i starts on. Reading it the other way round inverts the mapping.", "F": "The order is part of the instruction, not a free choice: transpiling the same circuit with `[4, 3, 2]` produces the reversed assignment, so the list is an ordered map and not a set of allowed qubits."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/transpiler-stages", "https://quantum.cloud.ibm.com/docs/guides/transpile"]}, "proof": {"status": "executed", "evidence": {"A": "optimization_level was a separate argument; initial_index_layout=[2, 3, 4] is a qubit map, not a level -> False", "B": "transpiled circuit has 0 measurements (none added by initial_layout) -> False", "C": "virtual->physical initial layout = [2, 3, 4]; matches [2,3,4] -> True", "D": "initial_layout is a compile-time mapping (observed [2, 3, 4]), not a runtime idle constraint -> False", "E": "entry i of the list is the physical qubit of virtual i: observed virtual->physical [2, 3, 4] -> the reversed reading is False", "F": "the same circuit with the list reversed gave [4, 3, 2] instead of [2, 3, 4], so the order fixes the assignment -> 'assign them in whichever order' is False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.22}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q044", "section": "s3", "objectives": ["s3o4"], "type": "mcq", "difficulty": 2, "stem": "Why must an abstract quantum circuit be transpiled before it can be run on IBM Quantum hardware through the Runtime primitives?", "code": null, "options": [{"key": "A", "text": "Hardware runs only ISA circuits: the transpiler is designed to put every gate in the native basis and route two-qubit gates onto connected pairs."}, {"key": "B", "text": "Transpilation is optional: the V2 primitives silently convert any abstract circuit at submission time, so skipping it only costs a little extra runtime."}, {"key": "C", "text": "Transpilation is needed only to draw the circuit nicely; execution accepts any circuit unchanged."}, {"key": "D", "text": "Transpilation exists to convert the circuit into OpenQASM 2, the only format the hardware accepts."}, {"key": "E", "text": "Transpilation is what packs the circuit into the primitive's PUB format; without that step `SamplerV2` cannot read the circuit at all, whatever gates it uses."}, {"key": "F", "text": "Hardware accepts any gate set, but the transpiler is needed to insert the dynamical-decoupling pulses that keep idle qubits alive between the gates."}], "displayCount": 4, "answer": ["A"], "explanation": {"correct": "Real devices have a fixed native gate set and a limited qubit connectivity. Transpilation maps virtual qubits to physical ones, routes two-qubit gates onto connected pairs (inserting SWAPs), and translates gates into the basis set — producing an ISA circuit, which is what the V2 primitives require as input.", "distractors": {"B": "The V2 primitives do NOT silently transpile arbitrary circuits; they expect ISA circuits and will error on non-ISA input. Transpiling is a required step, not a mere optimization.", "C": "Drawing has nothing to do with it; an un-transpiled circuit generally cannot execute on hardware at all.", "D": "Hardware execution is not about OpenQASM 2; the requirement is native basis gates plus valid connectivity (an ISA circuit).", "E": "PUBs are built when you call the primitive (circuit + parameters + observables); transpilation is a separate, earlier step about gates and connectivity. A PUB made from an abstract circuit is well-formed but still rejected as non-ISA.", "F": "Dynamical decoupling is an optional error-suppression option on the primitive, not the reason to transpile. The real requirement is the native basis plus a valid coupling map."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/transpile", "https://quantum.cloud.ibm.com/docs/guides/get-started-with-sampler"]}, "proof": {"status": "conceptual"}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q045", "section": "s3", "objectives": ["s3o1"], "type": "predict-output", "difficulty": 2, "stem": "Qubit 0 is flipped to `|1⟩`. The measurements deliberately map q0 -> c1 and q1 -> c0. What bitstring (order `c1 c0`) is observed on every shot?", "code": "from qiskit import QuantumCircuit\n\nqc = QuantumCircuit(2, 2)\nqc.x(0)\nqc.measure(0, 1)   # qubit 0 -> classical bit 1\nqc.measure(1, 0)   # qubit 1 -> classical bit 0", "options": [{"key": "A", "text": "`01`"}, {"key": "B", "text": "`11`"}, {"key": "C", "text": "`10`"}, {"key": "D", "text": "`00`"}, {"key": "E", "text": "`2`"}], "displayCount": 4, "answer": ["C"], "explanation": {"correct": "Qubit 0 is `1`, qubit 1 is `0`. The mapping stores qubit 0's result in c1 and qubit 1's result in c0, so c1=1 and c0=0. Printed as `c1 c0`, that is `10`.", "distractors": {"A": "`01` assumes the default mapping q0->c0, q1->c1; here the mapping is deliberately swapped, sending qubit 0's value to c1.", "B": "`11` would require both qubits to be 1, but only qubit 0 was flipped.", "D": "`00` would require both qubits to be 0; qubit 0 carries an X.", "E": "Integer keys come from `BitArray.get_int_counts()`; `get_counts()` (and the Sampler's bitstring view) always key the histogram by bitstrings, never by the integer value of the register."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/measure-qubits", "https://quantum.cloud.ibm.com/docs/guides/construct-circuits"]}, "proof": {"status": "executed", "evidence": {"A": "option='01'; observed 0/1000; full={'10': 1000} -> False", "B": "option='11'; observed 0/1000; full={'10': 1000} -> False", "C": "option='10'; observed 1000/1000; full={'10': 1000} -> True", "D": "option='00'; observed 0/1000; full={'10': 1000} -> False", "E": "option='2'; observed 0/1000; full={'10': 1000} -> False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.21}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 1}}, {"id": "s3-q046", "section": "s3", "objectives": ["s3o1"], "type": "predict-output", "difficulty": 3, "stem": "Circuit `a` applies X to its single qubit; circuit `b` applies identity to its single qubit. Which two-qubit state does `a.tensor(b)` prepare (bit order `q1 q0`)?", "code": "from qiskit import QuantumCircuit\nfrom qiskit.quantum_info import Statevector\n\na = QuantumCircuit(1)\na.x(0)      # a's qubit -> |1>\n\nb = QuantumCircuit(1)\nb.id(0)     # b's qubit -> |0>\n\ncombined = a.tensor(b)\nsv = Statevector(combined)", "options": [{"key": "A", "text": "`|01⟩` — `b` (identity) is usually placed on the higher-order qubit and `a` (X) on the lower one."}, {"key": "B", "text": "`|11⟩` — both qubits end up excited."}, {"key": "C", "text": "An equal superposition of all four basis states."}, {"key": "D", "text": "`|10⟩` — `a` takes the higher-order qubit, so q1 is 1 and q0 stays 0."}, {"key": "E", "text": "A single-qubit circuit — `tensor` typically multiplies the operand matrices instead of widening the register."}, {"key": "F", "text": "It raises a `CircuitError`, because `tensor` is generally defined for `Operator` objects and not for circuits."}], "displayCount": 4, "answer": ["D"], "explanation": {"correct": "In `a.tensor(b)` the left operand `a` is placed on the MORE significant (higher-index) qubits and `b` on the less significant ones. So `a`'s X acts on q1 (giving 1) and `b`'s identity leaves q0 at 0, producing `|10⟩`.", "distractors": {"A": "`|01⟩` reverses the tensor convention; `a` (the left operand), not `b`, takes the higher-order qubit.", "B": "`b` applies identity, so q0 remains 0 — the state is not `|11⟩`.", "C": "Neither circuit contains an H or other superposition-creating gate, so the result is a definite basis state.", "E": "Matrix multiplication of same-size operators is `compose`/`dot`. `tensor` builds the Kronecker product, so the result is 2 qubits wide, not 1.", "F": "`QuantumCircuit.tensor` exists and returns a new, wider circuit; you do not have to convert to an `Operator` first."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/construct-circuits", "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.circuit.QuantumCircuit"]}, "proof": {"status": "executed", "evidence": {"A": "nonzero amplitudes={'10': '(1+0j)'}; claim |01> -> False", "B": "nonzero={'10': '(1+0j)'}; claim |11> -> False", "C": "nonzero={'10': '(1+0j)'} (1 states); claim superposition -> False", "D": "nonzero={'10': '(1+0j)'}; single |10> (a on high qubit) -> True", "E": "a.tensor(b) returned a 2-qubit circuit, so the registers were widened rather than multiplied -> False", "F": "tensor ran on two QuantumCircuits and returned a 2-qubit circuit (nonzero={'10': '(1+0j)'}) -> claim CircuitError is False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.2}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q047", "section": "s3", "objectives": ["s3o1"], "type": "spot-bug", "difficulty": 1, "stem": "This code raises a `CircuitError` at the `measure` call. What is the fix?", "code": "from qiskit import QuantumCircuit\n\nqc = QuantumCircuit(2)          # 2 qubits, 0 classical bits\nqc.h(0)\nqc.cx(0, 1)\nqc.measure([0, 1], [0, 1])     # CircuitError", "options": [{"key": "A", "text": "The circuit has no classical bits; create it as `QuantumCircuit(2, 2)` or add a `ClassicalRegister`."}, {"key": "B", "text": "`measure` does not accept parallel lists of qubits and clbits; each qubit typically needs its own separate call."}, {"key": "C", "text": "The `measure` method does not exist on `QuantumCircuit`; you have to call `qc.measure_all()` instead."}, {"key": "D", "text": "The qubits have to be measured before the CX gate, not after it."}, {"key": "E", "text": "`measure([0, 1], [0, 1])` is ambiguous; naming the arguments as `qc.measure(qubit=[0,1], cbit=[0,1])` is generally preferred."}, {"key": "F", "text": "Registers may be attached later: call `qc.measure(...)` first, then `qc.add_register(ClassicalRegister(2))` to store the results."}], "displayCount": 4, "answer": ["A"], "explanation": {"correct": "`QuantumCircuit(2)` creates 2 qubits and ZERO classical bits, so `measure` has nowhere to write. Declaring classical bits — `QuantumCircuit(2, 2)` or adding a `ClassicalRegister` — resolves it.", "distractors": {"B": "`measure` accepts parallel lists of qubits and clbits; the list form is fine once classical bits exist.", "C": "`measure` absolutely exists; `measure_all` is just a convenience that also creates its own register.", "D": "Gate ordering is irrelevant here — the error is the missing classical bits, not the position of the measurement.", "E": "The positional form is unambiguous, and naming the arguments changes nothing: the same `CircuitError` appears because the circuit still owns no classical bits.", "F": "The bits have to exist when `measure` runs: the call raises before `add_register` is ever reached, so the order is the whole problem."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/measure-qubits", "https://quantum.cloud.ibm.com/docs/guides/construct-circuits"]}, "proof": {"status": "executed", "evidence": {"A": "bug=CircuitError: 'Index 0 out of range for size 0.'; with QuantumCircuit(2,2) measure([0,1],[0,1]) ran, counts={'11': 96, '00': 104} -> True", "B": "the list form measured 2 bits successfully once clbits existed -> 'lists not allowed' is False", "C": "QuantumCircuit.measure exists=True -> claim 'no measure method' is False", "D": "reordering the measurement does not add classical bits; irrelevant to the error -> False", "E": "the same call with named arguments on a circuit with no classical bits -> CircuitError: 'Index 0 out of range for size 0.' -> False", "F": "measuring first and adding the register only afterwards -> CircuitError: 'Index 0 out of range for size 0.'; the register was attached too late to help -> False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.22}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q048", "section": "s3", "objectives": ["s3o1"], "type": "mcq", "difficulty": 1, "stem": "What is the practical benefit of building a circuit from named registers such as `QuantumRegister(3, 'data')` and `QuantumRegister(1, 'anc')` instead of the bare `QuantumCircuit(4)` form?", "code": null, "options": [{"key": "A", "text": "Named registers usually make the circuit execute faster on hardware."}, {"key": "B", "text": "They let you address qubits by role, e.g. `data[0]` and `anc[0]`, which keeps larger circuits readable."}, {"key": "C", "text": "Only qubits that belong to a named register can be measured; anonymous qubits cannot."}, {"key": "D", "text": "Named registers are required by the transpiler, which refuses a circuit built with the bare `QuantumCircuit(4)` form."}, {"key": "E", "text": "Named registers usually let the transpiler keep the `data` qubits contiguous on the device, which improves the layout."}, {"key": "F", "text": "Named registers typically survive transpilation, so the ISA circuit still shows `data` and `anc` labels instead of `q`."}], "displayCount": 4, "answer": ["B"], "explanation": {"correct": "Registers are an organizational tool. Naming them lets you reference qubits/bits by role (`data`, `anc`) and index within each group, which keeps larger circuits readable and lets one circuit contain multiple distinct registers. They carry no performance or execution requirement.", "distractors": {"A": "Register naming is purely structural metadata; it has no effect on execution speed.", "C": "Any qubit can be measured regardless of whether it lives in a user-named register; `QuantumCircuit(4)` qubits are measurable too.", "D": "Circuits built with the integer form transpile perfectly well; the transpiler never inspects register names.", "E": "The layout pass optimizes on connectivity and error rates; it never reads register names, so grouping by register buys no locality.", "F": "A transpiled circuit is defined over the device's physical qubits and carries a single register named `q`; your names live on in the layout, not in the ISA circuit's registers."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/construct-circuits", "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.circuit.QuantumCircuit"]}, "proof": {"status": "conceptual"}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q049", "section": "s3", "objectives": ["s3o3"], "type": "predict-output", "difficulty": 1, "stem": "A 3-parameter circuit is partially bound with a dict that supplies only `q`. How many free parameters does `partial` have, and which remain?", "code": "from qiskit import QuantumCircuit\nfrom qiskit.circuit import Parameter\n\np = Parameter('p'); q = Parameter('q'); r = Parameter('r')\nqc = QuantumCircuit(1)\nqc.rx(p, 0)\nqc.ry(q, 0)\nqc.rz(r, 0)\n\npartial = qc.assign_parameters({q: 1.0})", "options": [{"key": "A", "text": "0 — assigning any dict binds all of the circuit's parameters at once."}, {"key": "B", "text": "1 — only `q` remains free."}, {"key": "C", "text": "2 — `p` and `r` remain free; only `q` was bound."}, {"key": "D", "text": "It raises, because a dict must supply a value for every parameter."}, {"key": "E", "text": "2 — `p` and `q` remain free; the dict bound `r`, the last parameter of the circuit."}, {"key": "F", "text": "It raises a `CircuitError`: dict keys have to be parameter names like `'q'`, not the `Parameter` objects the circuit holds."}], "displayCount": 4, "answer": ["C"], "explanation": {"correct": "A dict binds exactly the parameters it names. Passing `{q: 1.0}` binds only `q`, leaving `p` and `r` free — so `partial.num_parameters` is 2. This is partial binding.", "distractors": {"A": "A dict binds only the keys you provide, not every parameter; the others stay symbolic.", "B": "`q` is the one that gets bound, so it is NOT among the remaining free parameters.", "D": "A dict is exactly how you do partial binding; it does not require a value for every parameter (a list would).", "E": "The count is right but the wrong parameter is bound: a dict binds the parameter its KEY refers to, and the key here is `q`, so `p` and `r` are what remain.", "F": "`Parameter` objects are the canonical keys (that is what the code uses, and it works). Name strings are accepted as a convenience — neither form is mandatory."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/construct-circuits", "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.circuit.Parameter"]}, "proof": {"status": "executed", "evidence": {"A": "partial.num_parameters=2, remaining=['p', 'r']; claim 0 -> False", "B": "remaining=['p', 'r']; claim only q -> False", "C": "num_parameters=2, remaining=['p', 'r']; only q bound -> True", "D": "partial dict binding succeeded (remaining=['p', 'r']); claim 'raises' -> False", "E": "remaining=['p', 'r']: the key q was the one bound, so p and r stay free -> False", "F": "the Parameter object as a dict key bound fine (remaining=['p', 'r']); the name string form also works (left 2 free) -> claim 'raises' is False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.19}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 1}}, {"id": "s3-q050", "section": "s3", "objectives": ["s3o3"], "type": "multi", "difficulty": 3, "stem": "Select ALL true statements about parameterized circuits and how they interact with binding and transpilation.", "code": null, "options": [{"key": "A", "text": "The values in a LIST passed to `assign_parameters` are matched to `qc.parameters` in alphabetical name order, not in gate-insertion order."}, {"key": "B", "text": "A parameterized circuit can be transpiled first and have its parameters assigned afterward; the ISA circuit keeps the parameters until you bind them."}, {"key": "C", "text": "`assign_parameters` must be called before transpilation, otherwise the parameters are lost in the layout stage."}, {"key": "D", "text": "A dict passed to `assign_parameters` may bind only a subset of parameters (partial binding)."}, {"key": "E", "text": "Reusing the same `Parameter` object on two gates raises an error."}, {"key": "F", "text": "Binding an ISA circuit's parameters invalidates its layout, so the bound circuit has to be transpiled a second time."}], "displayCount": 5, "answer": ["A", "B", "D"], "explanation": {"correct": "List binding follows `qc.parameters`, which is sorted alphabetically by name (A). Parameters survive transpilation, so you can transpile once and bind many times afterward (B). A dict binds only the named parameters (D).", "distractors": {"C": "Parameters are preserved through transpilation; you do NOT have to bind before transpiling (in fact binding after is the efficient pattern).", "E": "Reusing one `Parameter` across gates is fully supported — it ties those gates to a shared value, not an error.", "F": "Binding only substitutes numbers into existing gate arguments: the bound ISA circuit keeps the same operations and the same layout, and is ready to run as-is."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/construct-circuits", "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.circuit.Parameter"]}, "proof": {"status": "executed", "evidence": {"A": "list [0.5,1.5] -> rx(theta)=1.5, rz(phi)=0.5 (alphabetical) -> True", "B": "transpiled ISA had 1 param(s); bound to 0 -> True", "C": "binding after transpile worked, so pre-transpile binding is not required -> False", "D": "dict {u:0.2} left 1 free -> True", "E": "reusing one Parameter on 2 gates gave num_parameters=1, raised=False", "F": "after binding, the ISA circuit still has ops={'rz': 3, 'sx': 2, 'measure': 2, 'cx': 1, 'barrier': 1} and keeps a layout (True) -> 're-transpile needed' is False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.2}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q051", "section": "s3", "objectives": ["s3o3"], "type": "predict-output", "difficulty": 3, "stem": "The SAME parameter `th` is used on two different gates. How many free parameters does `qc` have, and what happens when `th` is bound?", "code": "from qiskit import QuantumCircuit\nfrom qiskit.circuit import Parameter\n\nth = Parameter('th')\nqc = QuantumCircuit(2)\nqc.rx(th, 0)\nqc.rx(th, 1)\n\nbound = qc.assign_parameters({th: 0.5})", "options": [{"key": "A", "text": "`qc` has 2 free parameters — each gate gets its own independent copy of `th`."}, {"key": "B", "text": "`qc` has 1 free parameter, but binding sets only the first `rx`; the second stays symbolic."}, {"key": "C", "text": "It raises, because a `Parameter` object may not be reused across two gates."}, {"key": "D", "text": "`qc` has 1 free parameter; binding `th` sets BOTH `rx` gates to 0.5 at once."}, {"key": "E", "text": "`qc` has 1 free parameter, and a second `Parameter('th')` built elsewhere typically binds it too, since parameters are matched by name."}], "displayCount": 4, "answer": ["D"], "explanation": {"correct": "A `Parameter` is a single symbolic object. Reusing `th` on both gates means `qc.num_parameters` is 1, and assigning `th` substitutes that one value into every gate that references it — so both `rx` gates become `0.5`. This is how weight-sharing ansätze tie gates together.", "distractors": {"A": "Reusing the same object does not clone it; there is one parameter, not two.", "B": "Binding a parameter updates every occurrence of it, not just the first gate.", "C": "Reusing a parameter across gates is fully valid and common; it does not raise.", "E": "Parameters are matched by identity, not by name: binding a dict keyed with a different `Parameter('th')` object raises `CircuitError`, even though the names agree."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/construct-circuits", "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.circuit.Parameter"]}, "proof": {"status": "executed", "evidence": {"A": "qc.num_parameters=1; claim 2 independent copies -> False", "B": "after bind rx angles=[0.5, 0.5]; claim only first set -> False", "C": "reusing th did not raise (num_parameters=1) -> False", "D": "num_parameters=1; both rx angles=[0.5, 0.5] set to 0.5 -> True", "E": "binding with a different Parameter object of the same name -> CircuitError: 'Cannot bind parameters (th) not present in the circuit.'; identity, not the name, is what matches -> False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.28}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q052", "section": "s3", "objectives": ["s3o2"], "type": "predict-output", "difficulty": 2, "stem": "Qubit 0 is set to `|1⟩` and measured into c0. A `switch` on c0 applies X to qubit 1 in `case(0)` and identity in the DEFAULT case. Qubit 1 is then measured. What outcome (order `c1 c0`) appears on every shot?", "code": "from qiskit import QuantumCircuit, QuantumRegister, ClassicalRegister\n\nqr = QuantumRegister(2, 'q')\ncr = ClassicalRegister(2, 'c')\nqc = QuantumCircuit(qr, cr)\nqc.x(0)\nqc.measure(0, 0)\nwith qc.switch(cr[0]) as case:\n    with case(0):\n        qc.x(1)\n    with case(case.DEFAULT):\n        qc.id(1)\nqc.measure(1, 1)", "options": [{"key": "A", "text": "`01` on every shot — c0=1 takes the DEFAULT branch (identity), so qubit 1 stays 0."}, {"key": "B", "text": "`11` on every shot — the `case(0)` branch runs and applies X to qubit 1."}, {"key": "C", "text": "`10` on every shot."}, {"key": "D", "text": "A mix of `00` and `11`, close to 50/50."}, {"key": "E", "text": "It raises a `CircuitError`: `case(case.DEFAULT)` has to be declared first."}, {"key": "F", "text": "`00` on every shot — reading `cr[0]` in the `switch` resets the bit to 0."}], "displayCount": 4, "answer": ["A"], "explanation": {"correct": "c0 is deterministically 1 (qubit 0 was flipped). The `switch` matches value 1 against `case(0)` (no match) and falls to `case(DEFAULT)`, which applies identity, leaving qubit 1 at 0. So c1=0, c0=1 → `01` every shot.", "distractors": {"B": "`case(0)` only runs when c0=0; here c0=1, so the X branch is NOT taken.", "C": "`10` would require qubit 1 to be 1 while c0=0; neither holds.", "D": "There is no superposition — qubit 0 is a definite 1 — so the outcome is deterministic, not 50/50.", "E": "The default branch may appear anywhere in the `switch` body — the builder collects the cases and matches them by value, taking DEFAULT only when nothing else matched.", "F": "Classical bits are not consumed by being read: `c0` keeps the value 1 that the measurement wrote, which is exactly why the DEFAULT branch is the one that runs."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/classical-feedforward-and-control-flow", "https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.circuit.QuantumCircuit"]}, "proof": {"status": "executed", "evidence": {"A": "counts={'01': 1000}; all '01' (default/identity branch)? -> True", "B": "counts={'01': 1000}; all '11'? -> False", "C": "counts={'01': 1000}; all '10'? -> False", "D": "counts={'01': 1000}; mix of 00/11? -> False", "E": "the switch with case(case.DEFAULT) declared last built and ran, giving counts={'01': 1000} -> claim 'DEFAULT must come first' is False", "F": "counts={'01': 1000}; c0 still reads 1 after the switch, so nothing was consumed -> claim '00 every shot' is False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.27}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}, {"id": "s3-q053", "section": "s3", "objectives": ["s3o2"], "type": "mcq", "difficulty": 3, "stem": "Both qubits 0 and 1 are set to `|1⟩` and measured into the 2-bit register `c`. An `if_test` guarded by `expr.equal(c, 3)` flips qubit 2, which is read into register `o`. What value does `o` hold?", "code": "from qiskit import QuantumCircuit, QuantumRegister, ClassicalRegister\nfrom qiskit.circuit.classical import expr\n\nqr = QuantumRegister(3, 'q')\nc = ClassicalRegister(2, 'c')\no = ClassicalRegister(1, 'o')\nqc = QuantumCircuit(qr, c, o)\nqc.x(0); qc.x(1)\nqc.measure(0, 0); qc.measure(1, 1)\nwith qc.if_test(expr.equal(c, 3)):\n    qc.x(2)\nqc.measure(2, o[0])", "options": [{"key": "A", "text": "`o = 0` — `expr.equal` compares individual bits, so it can never match the whole-register value 3."}, {"key": "B", "text": "`o = 1` — `c` holds binary `11` = 3, so the condition is always true and qubit 2 is flipped."}, {"key": "C", "text": "It raises — an `if_test` condition must be a `(register, value)` tuple; an `expr` is not accepted."}, {"key": "D", "text": "`o` is random 0 or 1 because qubit 2 was never given a definite initial state."}, {"key": "E", "text": "It raises a `TypeError`: `expr.equal` needs both operands lifted with `expr.lift`, so a bare Python `3` is rejected."}], "displayCount": 4, "answer": ["B"], "explanation": {"correct": "`expr.equal(c, 3)` builds a classical expression that is true when the register `c`, interpreted as an integer, equals 3. With both bits set, `c` = binary `11` = 3, so the condition holds and X is applied to qubit 2, making `o` = 1.", "distractors": {"A": "`expr.equal` compares the whole register's integer value, not bit-by-bit; it matches 3 exactly here.", "C": "`if_test` accepts either a `(resource, value)` tuple OR a classical `expr`; passing `expr.equal(...)` is valid.", "D": "Qubit 2 starts in the definite state `|0⟩`; the conditional flips it deterministically, so `o` is not random.", "E": "`expr.equal` lifts plain Python values for you and infers the width from the register operand — `expr.equal(c, 3)` is the documented form."}, "citations": ["https://quantum.cloud.ibm.com/docs/guides/classical-feedforward-and-control-flow", "https://quantum.cloud.ibm.com/docs/api/qiskit/circuit_classical"]}, "proof": {"status": "executed", "evidence": {"A": "counts={'1 11': 1000}; o always 0? -> False", "B": "counts={'1 11': 1000}; c=11=3 so condition fired, o always 1? -> True", "C": "if_test(expr.equal(...)) built and ran without error -> claim 'raises' is False", "D": "counts={'1 11': 1000}; o deterministic (not random) -> claim 'random' is False", "E": "expr.equal(c, 3) with a bare int built and ran (counts={'1 11': 1000}), so no manual lift was needed -> claim TypeError is False"}, "versions": {"qiskit": "2.5.0", "qiskit-aer": "0.17.2", "qiskit-ibm-runtime": "0.48.0", "numpy": "2.5.1"}, "verifiedOn": "2026-07-26", "wallSeconds": 1.23}, "provenance": {"generated": "2026-07-19", "model": "claude-opus (generation agent, section s3)", "adversarialRounds": 2}}]}
